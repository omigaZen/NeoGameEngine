[package]
name = "platform_winit"
version = "0.1.0"
edition = "2021"

[dependencies]
engine_platform = { path = "../engine_platform" }
winit = { version = "0.30", features = ["rwh_06"] }
raw-window-handle = "0.6"

