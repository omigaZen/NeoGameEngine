# NeoGameEngine Asset Goal Audit Report

Last updated: 2026-06-16

## Scope Audited

Authoritative sources checked this turn:

- `docs/asset_goal.md`
- `docs/engine_asset_api.md`
- Current workspace `Cargo.toml`
- New `Asset/engine_asset` crate
- New `examples/asset_smoke` example
- `docs/asset_goal_coverage_matrix.md`

This is a progress audit, not final completion. The goal remains open because richer editor UI integration, real import/cook conversion, deeper module-level feature pruning, ECS system integration, broader packaging acceptance coverage, and broad acceptance tests are not complete.

## Implemented Evidence

Runtime MVP slice now exists in `engine_asset`:

- Stable runtime identity/path/key pieces: `id.rs`, `path.rs`, `metadata.rs`, `registry.rs`.
- Typed public runtime access: `asset.rs`, `handle.rs`, `ref_asset.rs`, `storage.rs`, `server.rs`, `prelude.rs`.
- Direct typed storage coverage for count/contains, ready vs CPU-only access, mutation/iteration, remove, metadata/count/error fields, and usage tracking.
- Real synchronous load progression through queue, IO, decode, dependency wait, GPU-upload handoff, ready/failed/unload states.
- Runtime observability through `AssetEvent`, `AssetEventCursor`, `state`, `error_by_id`, `dependency_graph`, `memory_stats`, `memory_info`, `memory_report`, explicit ready-asset insertion, and upload drain/finish APIs.
- Loader registry/context coverage for custom loader registration, extension/type fallback resolution, explicit dependencies, subresource registration, serialized `AssetRef<T>` dependency fallback harvesting, type mismatch failures, and loader-not-found diagnostics.
- Real `MemoryAssetIo` and `FileSystemAssetIo`.
- FileSystem IO coverage for range reads, recursive normalized listing, missing/read-range/metadata action mapping, non-file/list source messages, path normalization, and composite diagnostics with filesystem layers.
- Built-in runtime assets/loaders for texture, validated text mesh, stage-labelled shader, dependency-producing material with error-path diagnostics, minimal text `AudioClip` payloads, documented `AnimationClip` and `Skeleton` payloads plus source-import support with duplicate bone-name and mismatched explicit inverse-bind rejection, `Font`, and `PhysicsMesh` text payloads, documented `SceneAsset` text payloads with dependency paths, and documented `Prefab` text payloads with dependency paths.
- Built-in data types for audio, animation, skeleton, scene, prefab, font, and physics mesh.
- Public API shapes for importer/cooker/bundle/streaming/hot reload/ECS, marked partial in the matrix where behavior is not complete.
- Editor baseline through `AssetDatabase`: source scan, incremental added/changed/unchanged/removed classification including OBJ material-library context-hash invalidation, importer dispatch, importer validation failure context, sorted importer settings persistence, built-in material dependency extraction, documented model manifest generated subresources, importer-produced dependency/generated metadata persistence, generated byte persistence for cooking, stable path/id import, registry save/load, cooker dispatch, cooker validation failure context, and metadata hash updates.
- `.meta` sidecar metadata save/load, source scan reconciliation, stable `AssetId` preservation when a source path moves, missing/stale metadata diagnostics, dry-run and write-back registry/sidecar metadata migration reports, per-file write observability, 14-field metadata with importer settings, 12/13-field V1-compatible metadata migration classification, migration-only V0 registry/sidecar classification, rewrite to current 14-field text while preserving generated paths and dependencies, unsupported-version/invalid migration diagnostics without overwriting bad files, generated metadata sidecar path/label/dependency preservation, and dependency metadata restoration.
- Registry and sidecar V1 compatibility diagnostics for malformed headers/field layouts with line/file context.
- Bundle baseline through V2 chunk-layout manifest metadata, V1 manifest compatibility, compression codec reports, codec-aware unsupported-compression diagnostics, uncompressed bundle writer/reader, real RLE codec-backed bundle writer/reader execution, default-feature Zstd writer/reader execution through a pure Rust backend, corrupted RLE/Zstd chunk diagnostics, deterministic max-size multi-chunk partitioning, eager/on-demand/limited-on-demand chunk decode policies, explicit chunk/path prefetch APIs, LRU bounded cache eviction stats, chunk/range read reports, write-to-file convenience, manifest dependency lookup, `BundleAssetIo`, named-layer `CompositeAssetIo`, missing entry error mapping, default and source/mod/patch/base override precedence, served-layer diagnostics, and metadata lookup following the same first-layer override precedence as reads, runtime load from bundle IO, `MountedBundleRegistry` save/load snapshots, `AssetPackageRegistry` package order/enabled-state/layer-kind/payload-path/package-version/min-runtime/package-dependency/manifest persistence with V1/V2 migration defaults, `AssetPackageArtifactStore` install/verify/build/remove lifecycle, package conflict reports, semantic asset override reports for overridden ids/type/hash/dependency/provider changes, package update reports with runtime-too-old, version-downgrade, missing dependency provider, dependency-too-old, dependency-too-new, default asset-type mismatch, and strict asset-compatibility diagnostics, package-driven composite IO construction with payload-manifest validation, and `AssetServer` mount/preload/unmount plus mounted-registry/package-registry save/load/restore/preview/transactional-activate/artifact-verify/artifact-activate APIs. Artifact-backed registry restore/activate now has direct evidence that disabling a package preserves ready assets already loaded from the active bundle set, and package-driven composite IO metadata lookup now follows the same first-layer override precedence as reads.
- Streaming region baseline through `AssetServer` register-by-path, mounted-bundle manifest registration, explicit bundle subset registration with id validation, priority-aware preload, real progress/state, resident flags, region unload behavior, shared residency counts across multiple regions, resident-region removal, and memory-budget interaction.
- Hot reload rollback, watcher, dependency-planning, rollback-policy reporting, editor/debug policy snapshots, per-type rollback overrides, watch backend/status reporting, and configurable dependent-execution baseline through explicit reload queue APIs, read-only direct/transitive dependent reload plans, default direct dependent execution, opt-in transitive dependent execution, CPU/GPU rollback retention reports for built-in and custom asset types, policy snapshots with watches/queue/last poll/path status, polling `AssetIo::metadata` watches, async notification watches with start/stop lifecycle, pending/received/delivered/dropped counters, notification metadata error reports, duplicate pending-path debounce, batch poll diagnostics, handle-stable successful reload, visible `Reloading` state, old CPU asset retention while reloading/uploading, failed decode/GPU rollback that keeps the previous ready asset, opt-out failure behavior that leaves an asset `Failed`, stale error clearing on later success, reload event ordering, dependent material/scene reload, direct `reload_by_id` missing-asset rejection, direct `reload_by_path` recovery after failures, direct `queue_hot_reload_id` missing-asset rejection, reload-path metadata path stability after successful reloads, and missing-id/missing-path reload calls leaving asset metadata paths unchanged.
- GC and memory-budget baseline through automatic Drop-based strong/weak retain/release for server-created handles, rich per-asset/per-type memory reports, observable strong/weak/dependency counts, weak-only unused unload, cloned/dropped handle count refresh, dependency protection and release on dependent unload, resident protection, unload events, global oldest-first budget eviction, and per-asset-type total/CPU/GPU budget eviction from CPU/GPU byte accounting.
- Built-in cooker baseline through source-byte cook context, canonical texture/material/audio/skeleton/animation cookers for normalized runtime bytes and source-document conversion where applicable, pass-through shader/font/physics-mesh cookers, `MeshCooker` validation/decode coverage for imported text or binary mesh bytes into deterministic `NGA_MESH_BINARY_V1` cooked output with desktop `u32` indices plus mobile/web exact referenced-vertex compaction/deduplication and `u16` index blocks when possible, cooked `u16` mesh runtime upload metadata/bytes preservation, cooked file output, cooked hash/path metadata updates, and runtime texture/mesh/shader/material/audio/skeleton/animation/font/physics-mesh load from cooked output.
- Database bundle-build baseline through `AssetDatabaseBundleBuild`, uncompressed, RLE-compressed, and Zstd-compressed single/multi-chunk bundle bytes produced from cooked metadata/files, cooked hash validation, manifest dependency preservation, missing cooked input diagnostics, runtime mount/preload verification through `AssetServer`, and uncompressed/compressed bundle-backed streaming region verification.
- Scheduler cancellation baseline through public id/path/group cancellation APIs, `Cancelled` state and event visibility, queued-work cancellation before decode, requeue after cancellation, and priority/de-duplication coverage.
- Load-group progress baseline through deterministic group state aggregation, per-state progress counters including cancelled assets, CPU+GPU resident byte accounting, observable group tracking, tracking-only release semantics, and mixed-state/release tests.
- GPU upload handoff depth through initial upload failure diagnostics, per-frame drain throttling with retained pending commands, and dependency failure propagation when a dependency upload fails.
- Dependency graph depth through whole-graph and scoped root reports, stable text/DOT/JSON/self-contained-HTML report exports, runtime/database save-to-file helpers with metadata path/type labels for HTML, direct/reverse/topological/dependent queries, deterministic cycle-safe transitive dependency/dependent traversal, built-in material import edges, dependency failure propagation, load-time cycle detection with visible `CyclicDependency` errors, and database/server export coverage for model-generated skeleton/animation edges in both whole-graph and scoped text/DOT/JSON/HTML reports, including saved file output, readback verification, and missing-root save diagnostics.
- Runtime loading now preserves loadable registry/bundle metadata dependencies by merging them with loader-context and asset-provided dependencies into the `AssetServer` dependency graph, while manifest-only external dependency ids with no current path/state remain in metadata for audit/package reports without blocking unrelated loads. Database dependency reports now also pin model-generated skeleton and animation edges directly in the graph.
- Generic loaded-asset dependency harvesting through `AssetDependencyReference`, `AssetRef<T>::visit_dependency`, `LoadedAsset::new_with_asset_dependencies`, `with_dependency_handles`, and `with_dependency_refs`, merging custom `AssetDependencies` references with loader-context dependencies into runtime dependency graph/metadata, registering fallback paths from serialized references, recursively queueing known dependency paths, waiting for readiness, propagating dependency failure, and failing unknown asset-provided dependency ids visibly instead of waiting forever.
- ECS helper baseline through documented system order descriptors, stage ordering helpers, component handle exposure, readiness helpers, non-mutating scene/prefab instantiation plans, stable command exports, scene/prefab component `instantiate` helpers that directly emit instantiation commands to an `InstantiationSink`, public component-schema asset-reference export for known serialized scene/prefab component fields, generic `HostInstantiationSink` host mapping that creates host entity handles, resolves serialized parent indexes to host parents, reports created roots/entities/component counts, marks scene/prefab instance components loaded only after successful host instantiation, leaves failed parent mapping as a visible `HostInstantiationError`, and `TypedHostInstantiationSink` / `EcsComponentInstance` materialization that converts known serialized components into handle-owning `MeshRendererComponent`, `SkinnedMeshRendererComponent`, `AudioSourceComponent`, `PhysicsColliderComponent`, `SceneInstanceComponent`, and `PrefabInstanceComponent` values while passing unknown components through unchanged. Typed host instantiation now validates parent indexes and materializes all components before calling host spawn/attach, so component materialization failures do not leave partially spawned host entities or mark the instance loaded; the explicit scene/prefab typed-host parent-failure, sink-error, and generic host sink-error regressions now pin that preflight behavior.
- Workspace smoke baseline through `examples/asset_smoke`, which exercises renderer/audio/physics-facing component readiness from `AssetServer` handles without owning the loaded resources, converts loaded `Mesh`/`Material`/`Texture` assets into `engine_render::Mesh`/`Texture`/`Material`, builds an `engine_render::RenderScene` and `RenderQueue` with observable scene and queue statistics, creates headless `engine_renderer` mesh/texture/material resources from the same ready assets with observable `resource_status`, `mesh_info`, `texture_info`, and resident memory stats, converts a loaded `PhysicsMesh` into `engine_physics::TriMeshDesc`, creates a real `PhysicsWorld` mesh/collider and ray-query-visible physics shape, scene/prefab instantiation command export, raw sink application and observable traces, typed host scene/prefab instantiation with host entity parent mapping, `EcsComponentInstance` materialization, typed host error-path propagation, unknown component passthrough, loaded-state lifecycle checks, material dependency readiness, load-group progress/state, event cursor observation, an editor-to-runtime database scan/import/cook/bundle/mount/load flow for texture/mesh/material/audio/physics/skeleton/animation/scene/prefab assets with scene/prefab component-field dependencies and cooked audio/physics source assets that are loaded back into `AssetServer`/`PhysicsWorld`, and now `AudioSourceComponent` bridges over two loaded audio clips plus standalone `Skeleton`/`AnimationClip` readiness, `SkinnedMeshRendererComponent` scene/prefab runtime coverage, editor smoke evidence that `PhysicsColliderComponent`, `SceneInstanceComponent`, and `PrefabInstanceComponent` are ready and expose their asset handles, and the corresponding model-generated mesh/material/skeleton/animation/physics_mesh subresources loaded from a built bundle.
- Built-in importer coverage: `TextureImporter` pass-throughs binary runtime texture payloads and converts documented `NGA_TEXTURE_SOURCE_V1` text sources into runtime texture bytes; `AudioImporter` pass-throughs runtime audio/WAV payloads, converts documented `NGA_AUDIO_SOURCE_V1` text sources into canonical runtime audio bytes, and applies `force_mono` then `normalize` importer settings with persisted settings metadata and invalid boolean diagnostics; `AudioCooker` canonicalizes runtime audio/WAV payloads into normalized `NGA_AUDIO_V1` cooked bytes; `SkeletonCooker` and `AnimationCooker` canonicalize runtime or documented `NGA_SKELETON_SOURCE_V1` / `NGA_ANIMATION_SOURCE_V1` payloads into validated runtime skeleton/animation cooked bytes with `VersionHash(2)`; `FontImporter` pass-throughs runtime bitmap font payloads and converts documented `NGA_FONT_SOURCE_V1` text sources into deterministic runtime font bytes; `PhysicsMeshImporter` pass-throughs runtime physics-mesh payloads and converts documented `NGA_PHYSICS_MESH_SOURCE_V1` text sources into deterministic runtime physics-mesh bytes; `MeshImporter` pass-throughs runtime mesh payloads, validates/pass-throughs documented `NGA_MESH_BINARY_V1` binary mesh payloads with skin-weight validation and optional `u16` index blocks, and converts documented `NGA_MESH_SOURCE_V1` text sources into deterministic runtime mesh bytes with optional normals/UVs/secondary UV sets/tangents/four-influence skin joints and positive normalized weights; runtime mesh loading packs text and binary mesh payloads into deterministic interleaved binary vertex buffers, rejects zero or unnormalized skin weights, and exposes renderer-facing `GpuUploadMetadata::Mesh`, preserving decoded `u16` binary mesh index format through upload metadata, upload bytes, and GPU byte accounting; `MeshCooker` now decodes imported text or binary mesh payloads and writes deterministic `NGA_MESH_BINARY_V1` cooked bytes for database bundles and runtime loading with mobile/web exact vertex compaction and `u16` index blocks for `Android`/`Ios`/`Web` targets when possible; `ShaderImporter` pass-throughs runtime shader source and converts documented `NGA_SHADER_SOURCE_V1` documents into validated runtime WGSL, GLSL, or SPIR-V with `stage` and `entry` metadata validation, including case-insensitive `ENTRY` and `entry` matching against `fn`/`void` function definitions; plus includes WGSL parse-location plus source diagnostics on compile errors; `MaterialImporter` validates/canonicalizes runtime material text and parses `shader` and texture asset references into registry-backed dependency metadata without treating sampler/transform/bump/color/channel/boost/blend/color-space/projection/resolution metadata or material state fields or typed custom properties as assets, and preserves typed custom values and alpha texture source-channel metadata in canonical runtime bytes; `SceneImporter` and `PrefabImporter` parse documented runtime scene/prefab documents and register explicit dependency paths plus known component asset-reference fields into metadata; `ModelImporter` parses inline and multiline-block `NGA_MODEL_V1` manifests plus `.obj`/`NGA_MODEL_OBJ_V1` OBJ subset sources into generated mesh/material/skeleton/animation/physics_mesh metadata, runtime-loadable mesh/material/physics_mesh bytes with settings-controlled generated kind filters including `import_physics_meshes`, finite positive mesh/physics-mesh scale, OBJ tangent generation, generated mesh optimization with degenerate-triangle culling, deterministic generated LOD meshes with degenerate-triangle culling, and expanded OBJ `(position, uv, normal)` tuples, homogeneous OBJ vertex coordinates, parameter-space OBJ vertices, validated/ignored OBJ point/line elements, validated/ignored OBJ free-form curve/surface/connection attributes, validated/ignored OBJ display/render, texture-map library/use-map, render-object, approximation-technique, and merging-group attributes, positive and relative OBJ face indices, non-zero OBJ `vt` w diagnostics, trailing `#` inline comment stripping, OBJ/MTL backslash line continuation parsing, external material dependencies, standalone generated material/skeleton/animation payload diagnostics, local generated `depends=` dependencies with generated dependency list syntax diagnostics including typed dependency syntax and empty comma-entry diagnostics plus typed `mesh:`/`material:`/`skeleton:`/`animation:`/`physics_mesh:` target validation, material block generated mesh target metadata with unknown-label/non-mesh/repeated-field diagnostics, manifest generated mesh-to-material dependencies with unknown-label/type diagnostics, mesh block generated physics_mesh dependencies with unknown-label/non-physics-mesh diagnostics, generated mesh-to-skeleton skin dependencies with skeleton decode validation, duplicate-bone and explicit inverse-bind diagnostics, required skin joint/weight attributes, joint-range validation, declared skin joint and influence limit validation, duplicate active skin-joint validation, multi-root skin skeleton root-scope validation, skin root-bone subtree validation, and positive normalized skin-weight validation, typed generated dependency metadata, generated animation explicit skeleton target metadata, and track-shape/keyframe timing validation, generated path collision diagnostics, generated dependency id remapping across reimport, OBJ generated mesh-to-material dependencies, per-material mesh splitting for multi-material OBJ objects, loaded-MTL `usemtl` validation, OBJ UV/normal face tuple validation, `mtllib` provenance comments, resolved sidecar case-insensitive MTL base color/emissive/alpha/metallic/roughness properties including transparent `d`/`Tr` alpha-to-blend material state, `map_d`/`map_Tr`/`map_opacity`/`map_alpha`/`map_transparency` alpha texture-to-`alpha_mode=blend` material state, `d -halo` option case-insensitive dissolve-halo alpha/custom material state mapping, `sharpness` custom material property, `map_bump`/`map_Kn`/`map_normal` normal map alias mapping, `map_aat` texture antialias custom bool preservation with case-insensitive bool values, MTL `-colorspace` texture color-space metadata preservation, MTL `-imfchan` source-channel case-insensitive canonicalization including `a`/`alpha`, MTL `-type` projection case-insensitive canonicalization, and `map_Tf`/`map_Ni` transmission/IOR texture dependency mapping, plus `map_Ks -imfchan blue` specular source-channel preservation and `map_Ns -imfchan green` roughness source-channel preservation, Exocortex PBR scalar/texture material extension mapping, `map_RMA`/`map_ORM` packed PBR texture channel preservation, `map_MR`/`map_metallicroughness`/`map_metallic_roughness`/`map_roughnessmetallic`/`map_roughness_metallic`/`map_MRA`/`map_ARM` packed PBR texture alias preservation, `map_AO`/`map_Occlusion`/`map_ambient_occlusion` ambient-occlusion texture alias preservation, `Pr`/`roughness`, plus `Ka`/`Ks`/`Tf`/`Ni`/`illum` typed custom material properties, duplicate MTL material-name diagnostics, registry-backed case-insensitive common alpha/opacity texture aliases (`map_opacity`/`map_alpha`/`map_transparency`), descriptive PBR texture aliases (`map_diffuse`/`map_albedo`/`map_basecolor`/`map_base_color`/`map_specular`/`map_emissive`/`map_emission`/`map_roughness`/`map_metallic`/`map_metalness`/`map_normalgl`/`map_normaldx`), descriptive PBR scalar/color aliases (`diffuse`/`albedo`/`basecolor`/`base_color`/`ambient`/`ambient_color`/`specular`/`specular_color`/`emissive`/`emission`/`emissive_color`/`emission_color`/`opacity`/`alpha`/`transparency`/`transmission_filter`/`transmission_color`/`metalness`/`clear_coat`/`clearcoatroughness`/`clear_coat_roughness`/`anisotropyrotation`), packed PBR long-name texture aliases (`map_occlusionroughnessmetallic`/`map_occlusion_roughness_metallic`/`map_metallic_roughness`/`map_roughnessmetallic`/`map_roughness_metallic`), plus common and legacy MTL texture map dependencies including `map_Ns` roughness maps and `disp`/`decal`/`refl` displacement/decal/reflection maps with deterministic case-insensitive map option parsing/path extraction plus `-clamp` sampler-address with case-insensitive bool values, `-o`/`-s`/`-t`/`-bm` transform/bump payload preservation, `-mm`/`-imfchan`/`-boost` color-remap/channel/boost payload preservation with case-insensitive source-channel values, and `-blendu`/`-blendv`/`-cc` case-insensitive bool payload preservation plus `-colorspace`/`-type`/`-texres` color-space/projection/resolution payload preservation with case-insensitive texture-map directive matching, texture option names, and projection values, and root-to-generated subresource dependency edges.
- ModelImporter animation dependency coverage records explicit `skeleton=`/`target_skeleton=` metadata and validates skeleton-dependent `track=bone:<BoneName>`, `track=node:<NodeName>`, and `track=node_index:<u32>` targets against generated skeleton bones plus generated animation track shape and keyframe times, with missing-bone/node, out-of-range node index, empty-track, duplicate-target, non-finite/negative/out-of-duration keyframe, unsorted keyframe, and standalone generated animation payload diagnostics at import time.
- Feature flag baseline through explicit feature status/require helpers, default feature groups for filesystem/serde/bundle/hot-reload/streaming/editor/importers/cookers/zstd, cfg-gated serde derives for serialization-facing ids/paths/references/metadata, cfg-gated concrete built-in importer/cooker implementations and registration paths, cfg-gated zstd bundle codec support with visible disabled-codec diagnostics, cfg-pruned database bundle-build internals with a public `Unsupported` stub when `bundle` is disabled, cfg-pruned `AssetServer` bundle registry/reader state, hot-reload queue/watch state, and streaming region/residency state, visible disabled-feature errors for filesystem-backed IO, disabled bundle mount/registry operations, database bundle build, hot-reload queue/watch/poll/dependency planning, and streaming registration/lifecycle/progress entry points, and gate-aware feature tests for individual filesystem/bundle/hot-reload/streaming/editor/importer/cooker builds.
- Loading feature policy and reusable async worker-pool baseline through `AssetLoadingPolicyReport`, `AssetAsyncWorkerPoolReport`, `validate_loading_policy`, fallible async/parallel config setters, disabled-feature `Unsupported` diagnostics for requested async/parallel capabilities, `WorkerAsync` mode reporting when `async_loading` is enabled, effective async worker counts from `parallel` worker settings, an `update_loading` dispatch budget that uses the stricter of IO and CPU per-frame limits, long-lived background worker IO/decode dispatch, result collection on later updates, worker reuse, idempotent explicit shutdown, in-flight cancellation, dependency/subresource metadata merge, and preserved GPU handoff/event semantics.
- Error model coverage for IO/decode/import/cook/bundle/unsupported/GPU/dependency/cycle/type mismatch/loader missing/`NotLoaded` paths, including `AssetIoError` action/path/source context surfaced through runtime-facing `AssetError::Io` and concrete `AlreadyLoaded` production from `AssetServer::insert_loaded*` live replacement rejection.
- Smoke example: `examples/asset_smoke`.

## Verification Commands

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_material_cooker_canonicalizes_runtime_and_source_bytes
```

Result: `MaterialCooker` now has direct runtime/source round-trip evidence: runtime material bytes stay unchanged, normalized source documents canonicalize to the same runtime bytes, and both paths report `VersionHash(2)`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_font_cooker_canonicalizes_runtime_and_source_bytes
```

Result: `FontCooker` now has direct runtime/source pass-through evidence: runtime bitmap font bytes stay unchanged, validated bitmap source documents pass through unchanged, and both paths report `VersionHash(2)`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_shader_cooker_canonicalizes_runtime_and_source_bytes
```

Result: `ShaderCooker` now has direct runtime/source round-trip evidence: runtime shader bytes stay unchanged, `NGA_SHADER_SOURCE_V1` source documents canonicalize to the same runtime bytes, and both paths report `VersionHash(2)`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_audio_cooker_canonicalizes_runtime_and_wav_bytes
```

Result: `AudioCooker` now has direct runtime/WAV round-trip evidence: runtime audio bytes stay unchanged, IEEE-float WAV bytes and binary PCM16 WAV bytes canonicalize to the same runtime bytes, both paths report `VersionHash(8)`, and the binary PCM16 WAV path now also pins the exact cooked output bytes.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_animation_and_skeleton_cookers_canonicalize_runtime_and_source_bytes
```

Result: `AnimationCooker` and `SkeletonCooker` now have direct runtime/source round-trip evidence: runtime animation/skeleton bytes stay unchanged, validated source documents canonicalize to the same runtime bytes, and both paths report `VersionHash(2)`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_texture_cooker_canonicalizes_runtime_and_source_bytes
```

Result: TextureCooker now has direct runtime/source round-trip evidence: runtime texture bytes stay unchanged, NGA_TEXTURE_SOURCE_V1 source documents canonicalize to the same runtime bytes, and both paths report VersionHash(2).

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_font_and_physics_mesh_cookers_pass_through_runtime_bytes
```

Result: `FontCooker` and `PhysicsMeshCooker` now have direct pass-through round-trip evidence: cooked bytes are preserved unchanged, `FontCooker` reports `VersionHash(2)`, and `PhysicsMeshCooker` reports `VersionHash(1)`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_scene_and_prefab_cookers_pass_through_runtime_bytes
```

Result: `SceneCooker` and `PrefabCooker` now have direct pass-through round-trip evidence: scene and prefab runtime bytes are preserved unchanged and both cookers report `VersionHash(1)`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_scene_prefab_and_physics_mesh_cookers_pass_through_runtime_and_source_bytes
```

Result: `SceneCooker`, `PrefabCooker`, and `PhysicsMeshCooker` now have direct runtime/source pass-through evidence: runtime and source bytes are preserved unchanged, `SceneCooker` and `PrefabCooker` report `VersionHash(1)`, and `PhysicsMeshCooker` reports `VersionHash(1)`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_mesh_cooker_canonicalizes_runtime_and_source_bytes
```

Result: `MeshCooker` now has direct runtime/source round-trip evidence: runtime binary mesh bytes stay unchanged, text mesh source documents canonicalize to the same runtime bytes, and both paths report `VersionHash(4)`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_ambient_occlusion_texture_colorspace
```

Result: `map_AO`/`map_Occlusion`/`map_ambient_occlusion` ambient-occlusion texture regression now preserves `texture.occlusion.color_space=non_color` in generated material text, sidecars, bundle manifests, and runtime `Material` loads; source-channel preservation remains covered; the focused database test passed.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_occlusion_and_emissive_texture_colorspace
```

Result: `map_Ka`/`map_Ke` ambient-emissive texture regression now preserves `texture.occlusion.color_space=non_color` and `texture.emissive.color_space=non_color` in generated material text, sidecars, bundle manifests, and runtime `Material` loads; source-channel preservation remains covered; the focused database test passed.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_parses_obj_roughness_extensions
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_ambient_and_specular_texture_maps
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_parses_obj_quoted_material_texture_paths
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_transmission_and_ior_texture_maps
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: roughness, ambient/specular, quoted specular, and transmission/IOR texture-map regressions now preserve the expected `Non-Color` texture color-space metadata through generated material text, sidecars, bundle manifests, and runtime `Material` loads; full database passed 235 tests; full engine_asset passed cleanly under quiet mode; no-default-features check passed; whitespace check passed.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset
```

Result: 320 integration tests passed.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database treats_obj_smoothing_off_case_insensitively
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database smoothing
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
```

Result: formatting passed; focused OBJ `s OFF` smoothing-off import passed; smoothing group coverage passed 2 database tests; full database passed 154 tests; full engine_asset passed 320 integration tests; model_importer-only no-default feature check passed.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database dissolve_halo
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
git diff --check
```

Result: formatting passed; focused OBJ MTL `d -HALO` dissolve-halo import passed through generated material bytes, sidecars, bundle manifest, runtime bundle preload, and runtime `Material` custom alpha state; full database passed 154 tests; full engine_asset passed 320 integration tests; model_importer-only no-default feature check passed; whitespace check passed.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database shader_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features shader_importer
git diff --check
```

Result: formatting passed; focused ShaderImporter database coverage passed, including WGSL/GLSL/SPIR-V source-document conversion, ShaderImporter version 3 metadata, visible invalid `stage`/`entry` diagnostics, and WGSL parse-failure assertions with `line X, column Y`; full database passed 154 tests; full engine_asset passed 320 integration tests; shader_importer-only no-default feature check passed; whitespace check passed.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe fmt -p asset_smoke
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database scene_and_prefab
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p asset_smoke
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe check -p asset_smoke
```

Result: formatting passed; runtime passed 54 tests including scene/prefab component-field dependencies and invalid field diagnostics; focused scene/prefab database coverage passed; feature flags passed 9 tests; asset_smoke passed 3 tests; full engine_asset passed 320 integration tests; asset_smoke check passed.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime mesh
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database mesh_cooker
```

Result: formatting passed; 11 focused runtime mesh tests passed including `u16` binary mesh upload metadata/bytes, and 2 focused database mesh cooker tests passed including Web cooked `u16` mesh runtime upload preservation.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test dependency
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_dependency_report_saves_text_dot_json_and_html_exports
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database metadata_migration
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database importer_dependencies_and_generated_subresources_are_recorded_in_metadata
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_builtin_model_importer_generates_labeled_subresources_and_runtime_outputs
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database texture_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database material_importer
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features texture_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features texture_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features material_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features material_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database audio_importer
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features audio_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features audio_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database font_importer
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features importers
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features importers --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database physics_mesh_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database mesh_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database mesh_cooker
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_cooker
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_cooker --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database shader_importer
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features shader_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features shader_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database obj
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database homogeneous_vertices
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database smoothing_group_normals
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_parses_obj_inline_comments
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database usemtl_across_object_groups
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database roughness_extensions
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database legacy_texture_map_extensions
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database material_custom_properties
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database material_importer_canonicalizes
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database transparency_to_alpha_mode
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database dissolve_halo
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database sharpness_and_bump_alias
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database texture_antialiasing
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database transmission_and_ior_texture_maps
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database alpha_texture_to_alpha_mode
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database pbr_material_extensions
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database invalid_obj_material_library_property
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime custom_propert
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime material
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database optimize
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database lod
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database animation_track_shape
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database physics_mesh_subresources
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database mesh_physics_mesh_binding
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database physics_mesh_target_mesh
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database skin_influence_limit
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database duplicate_active_skin
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database requires_skin_root_for_multi_root
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database duplicate_generated_dependency_metadata
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database invalid_typed_generated_dependency_syntax
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database standalone_generated_payloads
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime animation
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
```

Result: dependency/audit tests passed with whole-graph and scoped HTML shape, escaped metadata labels, scoped root filtering, missing-root errors, and database HTML file export; metadata migration tests passed V1 field-layout upgrades, migration-only V0 registry/sidecar dry-run and write-back, unsupported-version/invalid-file non-overwrite behavior; importer/model tests passed root-to-generated dependency persistence through registry, sidecars, reports, scoped HTML, runtime bundle preload, multiline model block parsing, generated-to-generated dependencies, invalid generated dependency list and typed syntax diagnostics, standalone generated material/skeleton/animation payload diagnostics, typed generated dependency metadata including `physics_mesh:<Label>` plus mesh physics_mesh binding metadata with wrong-kind diagnostics, material target mesh dependency metadata with invalid target diagnostics, declared skin joint and influence limit metadata with invalid limit diagnostics, manifest generated mesh-to-material, explicit mesh LOD, mesh-to-physics_mesh, and physics_mesh-to-mesh target dependency metadata with invalid binding diagnostics, generated mesh-to-skeleton skin dependency metadata with invalid binding, explicit inverse-bind, duplicate active skin-joint, multi-root skin skeleton root-scope, skin root-bone subtree, skin influence limit, and weight normalization diagnostics, generated dependency id remapping across reimport, OBJ source fan triangulation, inline comment parsing, homogeneous vertex coordinate parsing, smoothing-group normal generation including case-insensitive `s off` flat normals, relative face-index parsing, generated OBJ mesh/material metadata with normals/UVs/tangents, model import settings filtering/scale/tangent/mesh optimization and LOD generation including degenerate-triangle culling and all-degenerate diagnostics/physics-mesh generation/mesh physics binding diagnostics, OBJ MTL context-hash scan invalidation, generated mesh-to-material bundle dependencies, binary mesh cooked bundle payloads, platform-aware Web/mobile `u16` mesh index, exact vertex-compacted cooked payloads, and runtime `Uint16` GPU upload metadata/bytes preservation, `mtllib` provenance comments, resolved sidecar case-insensitive MTL base color/emissive/alpha/metallic/roughness properties including transparent `d`/`Tr` alpha-to-blend material state, `map_d`/`map_Tr`/`map_opacity`/`map_alpha`/`map_transparency` alpha texture-to-`alpha_mode=blend` material state, `d -halo` option case-insensitive dissolve-halo alpha/custom material state mapping, `sharpness` custom material property, `map_bump`/`map_Kn`/`map_normal` normal map alias mapping, `map_aat` texture antialias custom bool preservation with case-insensitive bool values, MTL `-colorspace` texture color-space metadata preservation, MTL `-imfchan` source-channel case-insensitive canonicalization including `a`/`alpha`, MTL `-type` projection case-insensitive canonicalization, and `map_Tf`/`map_Ni` transmission/IOR texture dependency mapping, plus `map_Ks -imfchan blue` specular source-channel preservation and `map_Ns -imfchan green` roughness source-channel preservation, Exocortex PBR scalar/texture material extension mapping, `Pr`/`roughness`, plus `Ka`/`Ks`/`Tf`/`Ni`/`illum` typed custom material properties, duplicate MTL material-name diagnostics, loaded-MTL `usemtl` validation, persistent OBJ `usemtl` binding across `o`/`g` labels, per-material OBJ mesh splitting, registry-backed case-insensitive common alpha/opacity texture aliases (`map_opacity`/`map_alpha`/`map_transparency`), descriptive PBR texture aliases (`map_diffuse`/`map_albedo`/`map_basecolor`/`map_base_color`/`map_specular`/`map_emissive`/`map_emission`/`map_roughness`/`map_metallic`/`map_metalness`/`map_normalgl`/`map_normaldx`), plus common and legacy MTL texture map dependencies including `map_Ns` roughness maps and `disp`/`decal`/`refl` displacement/decal/reflection maps with deterministic case-insensitive map option parsing/path extraction plus `-clamp` sampler-address with case-insensitive bool values, `-o`/`-s`/`-t`/`-bm` transform/bump payload preservation, `-mm`/`-imfchan`/`-boost` color-remap/channel/boost payload preservation with case-insensitive source-channel values, and `-blendu`/`-blendv`/`-cc` case-insensitive bool payload preservation plus `-colorspace`/`-type`/`-texres` color-space/projection/resolution payload preservation with case-insensitive texture-map directive matching, texture option names, and projection values, invalid OBJ vertex/UV/normal/material-library/material-texture diagnostics, sidecar preservation, and bundle manifest dependency preservation; texture importer tests passed text-source conversion to runtime bytes, cook/runtime load, invalid-source diagnostics, and texture-importer-only feature builds; material importer tests passed canonicalized source persistence, cook/runtime load, dependency metadata, and material-importer-only feature builds; audio importer tests passed source conversion to canonical runtime bytes, cook/runtime load, invalid sample-count diagnostics, and audio-importer-only feature builds; font importer tests passed deterministic source conversion to runtime bytes, cook/runtime load, invalid bitmap diagnostics, and aggregate importer feature builds; physics-mesh importer tests passed deterministic source conversion to runtime bytes, cook/runtime load, invalid index diagnostics, and aggregate importer feature builds; mesh importer tests passed deterministic source conversion to runtime bytes with optional normals/UVs/secondary UV sets/tangents/skinning attributes, binary mesh source validation/pass-through, MeshCooker binary output including platform-aware vertex compaction, index-width selection, and runtime `Uint16` upload preservation, cook/runtime load, invalid index, attribute-count, and binary-source diagnostics, and model-importer-only feature builds; shader importer tests passed source document conversion to runtime WGSL/GLSL/SPIR-V, cook/runtime load, invalid empty-body diagnostics, invalid `stage`/`entry` metadata diagnostics, and shader-importer-only feature builds; the feature-independent report/migration APIs compiled under no-default features.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test error_model
```

Result: 5 error-model tests passed, including `insert_loaded*` ready-state insertion, metadata/events/memory observability, unload-then-reinsert behavior, queued-state replacement rejection, `NotLoaded`, bundle parse/compression diagnostics, and displayable/comparable `AlreadyLoaded`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test serde
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features serde --test serde
```

Result: serde-gated roundtrip coverage passed for `AssetRef<T>`, `AssetId`, `AssetTypeId`, `AssetTypeName`, `ContentHash`, `VersionHash`, `AssetPath`, and `AssetMetadata` under default features and explicit serde-only features.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test io
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime missing_path_fails_with_visible_state_error_and_event
```

Result: focused IO/runtime error-context coverage passed, including read/read-range/metadata/list action tags, logical paths, source messages for non-file/list failures, and runtime-facing `AssetError::Io` path/action text.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle bundle_compression_codec_report_and_zstd_feature_diagnostics
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_asset_override_report_tracks_semantic_policy_issues
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_builds_rle_compressed_bundle_and_runtime_preloads_it
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_builds_zstd_compressed_bundle_and_runtime_preloads_it
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test streaming streaming_region_preloads_rle_compressed_mounted_bundle_assets
```

Result: 27 bundle/package tests passed, covering RLE/Zstd codec execution, range report observability, corrupted RLE/Zstd diagnostics, zstd disabled-feature diagnostics, bounded chunk cache prefetch/eviction, package registry persistence/conflicts/version-policy/dependency-policy/semantic-override-policy/artifact lifecycle/runtime restore/transactional activation, scene/prefab semantic override diagnostics, database-built compressed bundle preload, and compressed bundle-backed streaming preload focused coverage.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle bundle_chunk_partition_policy_and_on_demand_cache_are_observable
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features bundle --test bundle bundle_compression_codec_report_and_zstd_feature_diagnostics
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features bundle --test bundle asset_package_asset_override_report_tracks_semantic_policy_issues
```

Result: deterministic max-size bundle chunk partitioning, invalid chunk-policy diagnostics, on-demand cached decode misses/hits, `BundleChunkCacheStats`, `BundleAssetIo` chunk cache observability, bundle-without-zstd disabled-codec diagnostics, and no-default bundle semantic-override diagnostics passed focused coverage.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --test feature_flags
```

Result: 5 disabled-feature integration tests passed, including importer/cooker, runtime disabled paths, zstd feature status, and async/parallel unsupported diagnostics.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test scheduler
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test gc
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test hot_reload
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database audio_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test loader
```

Result: 9 default feature policy tests passed with gate-aware filesystem/bundle/hot-reload/streaming/import/cook assertions, scheduler coverage verified that CPU job budget limits synchronous loading, 11 GC/lifecycle/memory-report/per-type-budget tests passed including Drop-based cloned/dropped handle counts, GC transitions, rich memory reports, budget-visible bytes, per-type budget eviction, and protection rules, 17 hot-reload tests passed including async notification watch lifecycle coverage, 154 database/editor tests passed including metadata migration write-back/V0 transform coverage, dependency HTML export, OBJ MTL context-hash scan invalidation, texture source conversion, material source canonicalization, scene/prefab source conversion with component asset-reference dependencies preserved in metadata/cooked manifests/runtime graphs, animation/skeleton source conversion, AnimationCooker/SkeletonCooker source-document canonicalization, audio source conversion with force-mono/normalize/streaming/compression settings and invalid setting diagnostics, WAV audio runtime decode/pass-through, mesh/shader/font/physics-mesh source conversion, mesh normal/UV/tangent/secondary-UV/skinning conversion with invalid skin-weight diagnostics, binary mesh source validation/pass-through, MeshCooker binary cooked output from direct/model/OBJ/skinned/generated mesh sources, platform-aware Web/mobile `u16` mesh index, exact vertex-compacted cooked output, and runtime `Uint16` GPU upload metadata/bytes preservation, binary vertex buffer layout checks, and invalid attribute-count/binary-source diagnostics, model generated-path collision diagnostics, model block manifest generated dependency remapping, invalid generated dependency list and typed syntax diagnostics, standalone generated material/skeleton/animation payload diagnostics, typed generated dependency metadata including `physics_mesh:<Label>` plus duplicate generated dependency metadata and mesh physics_mesh binding metadata with wrong-kind diagnostics, material target mesh dependency metadata with invalid target diagnostics, declared skin joint and influence limit metadata with invalid limit diagnostics, duplicate active skin-joint and multi-root skin skeleton root-scope diagnostics, manifest mesh-to-material, explicit mesh LOD, mesh-to-physics_mesh, and physics_mesh-to-mesh target dependency metadata with invalid binding diagnostics, generated mesh-to-skeleton skin dependency metadata with unknown-skeleton, invalid inverse-bind, missing-skin-attribute, joint-range, skin root-bone missing/without-skin/repeated/outside-subtree diagnostics, skin influence limit no-skin/repeated/zero/over-four/too-many-influence diagnostics, zero-weight/unnormalized-weight, explicit animation skeleton metadata with invalid target diagnostics, and skeleton-dependent animation target/track-shape/keyframe timing diagnostics, OBJ directive case-insensitive parsing, OBJ face-outline alias parsing, OBJ/MTL backslash line continuation parsing, OBJ/MTL alpha source-channel preservation, OBJ homogeneous vertex coordinate parsing, OBJ texture-coordinate non-zero `w` diagnostics, MTL bool value case-insensitive canonicalization, MTL source-channel case-insensitive canonicalization, MTL projection case-insensitive canonicalization, MTL texture-map directive case-insensitive canonicalization, MTL texture option-name case-insensitive parsing, MTL material property directive case-insensitive parsing, OBJ inline comment parsing, model mesh optimization degenerate triangle culling, generated LOD degenerate triangle culling, OBJ smoothing-group normal generation including case-insensitive `s off` flat-normal parsing, relative face-index import, model import settings filtering/scale/tangent/mesh optimization and LOD generation including degenerate-triangle culling and all-degenerate diagnostics/physics-mesh generation/mesh physics binding diagnostics, OBJ model generated mesh/material dependency coverage with resolved MTL material/emissive properties, `d`/`Tr` alpha-to-blend material state coverage, `map_d`/`map_Tr`/`map_opacity`/`map_alpha`/`map_transparency` alpha texture-to-`alpha_mode=blend` material state coverage, `map_d -imfchan blue` alpha texture source-channel preservation, `d -halo` option case-insensitive dissolve-halo alpha/custom material state coverage, `sharpness` custom material property, `map_bump`/`map_Kn`/`map_normal` normal map alias coverage, `map_aat` texture antialias custom bool preservation with case-insensitive bool values, `map_Kd -imfchan luminance` and `map_Kd -imfchan depth` albedo source-channel preservation, and `map_Tf`/`map_Ni` transmission/IOR texture dependency mapping, plus `map_Ks -imfchan blue` specular source-channel preservation and `map_Ns -imfchan green` roughness source-channel preservation, Exocortex PBR scalar/texture material extension coverage, `Pr`/`roughness` scalar roughness, `map_Ns -imfchan green` roughness source-channel preservation, legacy `disp`/`map_Disp`/`map_disp`/`map_displacement`/`decal`/`map_decal`/`refl`/`map_refl` texture map dependency coverage, plus PBR texture alias coverage for `map_sheen`/`map_clearcoat`/`map_clearcoat_roughness`/`map_anisotropy`/`map_anisotropy_rotation`, and MTL `Ka`/`Ks`/`Tf`/`Ni`/`illum` typed custom material property coverage plus `ior` alias coverage for the refraction scalar, duplicate MTL material-name diagnostics, loaded-MTL `usemtl` validation, persistent OBJ `usemtl` binding across `o`/`g` labels, per-material OBJ mesh splitting, and common alpha/opacity texture aliases (`map_opacity`/`map_alpha`/`map_transparency`) plus common and legacy texture map dependencies including `map_Ns` roughness maps, `map_Ka` occlusion maps, and `map_Ks` specular maps, plus `map_Ke` emissive maps, and `disp`/`decal`/`refl` displacement/decal/reflection maps with deterministic case-insensitive map option parsing/path extraction plus `-clamp` sampler-address with case-insensitive bool values, `-o`/`-s`/`-t`/`-bm` transform/bump payload preservation, `-mm`/`-imfchan`/`-boost` color-remap/channel/boost payload preservation with case-insensitive source-channel values, and `-blendu`/`-blendv`/`-cc` case-insensitive bool payload preservation plus `-colorspace`/`-type`/`-texres` color-space/projection/resolution payload preservation with case-insensitive texture-map directive matching, texture option names, and projection values, and OBJ UV/normal/tangent/material-library/material-texture diagnostics, 54 runtime tests passed including binary mesh upload metadata, `u16` binary mesh upload metadata/bytes, mesh secondary UV/skinning preservation, `NGA_MESH_BINARY_V1` runtime loading, invalid binary mesh diagnostics, explicit skeleton bind-pose matrix decoding, inverse-bind validation, duplicate skeleton bone diagnostics, mesh tangent/skin-count/skin-weight decode diagnostics, animation track-shape/finite-number/keyframe timing diagnostics, shader bind-group/vertex-input reflection and malformed source diagnostics, material texture metadata/color-space/state-field diagnostics, WAV PCM16/float32 runtime loading and invalid-WAV diagnostics, typed custom material properties, invalid material alpha-mode/custom-property diagnostics, scene/prefab component asset-field dependency registration, invalid empty asset fields, type-mismatch diagnostics, and 7 loader tests passed including custom `AssetDependencies` harvesting, serialized `AssetRef<T>` fallback dependency loading, and unknown-id diagnostics. Additionally, the bundle gate smoke now also round-trips the mounted-bundle and asset-package registries after building an empty bundle, so the gate verifies wrapper save/load behavior instead of only the empty manifest itself.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features async_loading --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features parallel --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features "async_loading,parallel" --test feature_flags
```

Result: enabled async/parallel feature diagnostics, zstd disabled-feature status, `WorkerAsync` reporting, and effective worker-count behavior passed in focused feature builds.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features bundle --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features hot_reload --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features streaming --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features filesystem --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features editor --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features importers --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features cookers --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features texture_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features texture_cooker --test feature_flags
```

Result: individual no-default feature-flag test builds passed, proving enabled gates reach normal validation paths while disabled sibling gates still return user-visible `Unsupported` or disabled-filesystem IO errors before inner work.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features async_loading
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --features async_loading
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --features async_loading --test scheduler
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --features "async_loading,parallel" --test scheduler
```

Result: async loading compiled in no-default and default feature builds; scheduler coverage passed with worker dispatch/result collection, reusable worker-pool lifecycle reporting, idempotent shutdown, in-flight cancellation, and async+parallel multi-worker dispatch.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features bundle
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features zstd
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features "bundle,zstd"
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features streaming
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features hot_reload
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features "bundle,streaming"
```

Result: pruned server runtime fields, zstd codec gating, and feature-specific stubs compile in disabled and individual runtime-feature builds.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features material_importer
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_cooker
```

Result: direct feature-only builds compiled after cfg-pruning concrete importer/cooker implementations.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe check -p asset_smoke
```

Result: example crate compiled.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p asset_smoke
```

Result: 3 smoke integration tests passed, covering non-owning renderer/audio/physics readiness, material dependency readiness, load-group progress, event cursor observation, renderer scene/queue construction, editor-to-runtime database cook/bundle/mount/load flow, and model-generated mesh/material/skeleton/animation/physics_mesh subresource runtime loading.

## Focused Tests Added

`Asset/engine_asset/tests/runtime.rs` covers:

- Audio load success paths through minimal `NGA_AUDIO_V1` payload parsing plus RIFF/WAVE PCM8/PCM16/PCM24/PCM32, WAVE_FORMAT_EXTENSIBLE PCM/IEEE-float subformats, and IEEE-float32 payload parsing, CPU-only `Ready`, sample/duration fields, no GPU upload, and ready event observation.
- Invalid text audio and invalid WAV payload failures with visible `AssetError::Decode` and failed events.
- Skeleton load success path through minimal `NGA_SKELETON_V1` payload parsing, CPU-only `Ready`, bone hierarchy fields, no GPU upload, and ready event observation.
- Skeleton payload success with explicit bind/inverse-bind matrices, explicit inverse-bind validation against accumulated parent bind poses, plus invalid skeleton payload failure with visible `AssetError::Decode` and failed event.
- Animation load success path through minimal `NGA_ANIMATION_V1` payload parsing, CPU-only `Ready`, track/keyframe fields, no GPU upload, ready event observation, empty-track and duplicate-target validation, finite numeric validation, nonnegative/duration-bounded keyframe time validation, and per-channel keyframe ordering diagnostics.
- Invalid animation payload failure with visible `AssetError::Decode` and failed event, including empty-track and duplicate-target diagnostics.
- Font load success path through minimal `NGA_FONT_V1` bitmap payload parsing, CPU-only `Ready`, glyph fields, no GPU upload, and ready event observation.
- Invalid font payload failure with visible `AssetError::Decode` and failed event.
- Physics mesh load success path through minimal `NGA_PHYSICS_MESH_V1` text payload parsing, CPU-only `Ready`, trimesh fields, no GPU upload, and ready event observation.
- Invalid physics mesh payload failure with visible `AssetError::Decode` and failed event.
- Mesh load success path through text vertex/index parsing, GPU upload command drain, upload completion, `Ready`, and event observation.
- Invalid mesh payload failure with visible index-range `AssetError::Decode` and failed event.
- Shader load success path through standalone WGSL source, binary SPIR-V payload handling, GLSL text pass-through, `#compute` stage selection, bind-group reflection, GPU upload command drain, upload completion, `Ready`, and event observation.
- Vertex shader load success path through deterministic vertex input reflection from `@location` attributes, plus SPIR-V alignment and magic-word validation.
- Invalid shader payload, malformed resource binding, basic delimiter-structure, unsupported shader stage-label, and invalid SPIR-V failures with visible `AssetError::Decode`; GLSL runtime pass-through does not attempt WGSL-style reflection, and importer-side `language=spv` source docs now round-trip to raw SPIR-V bytes.
- Texture load success path through CPU decode, GPU upload command drain, upload completion, `Ready`, and event observation.
- Material load with shader and texture dependencies, dependency graph entries, waiting state, dependency readiness, and final material upload.
- Invalid material syntax, numeric property, and bool property failures with visible `AssetError::Decode` and failed events.
- Scene load with documented `NGA_SCENE_V1` payloads, dependency path registration, waiting state, dependency readiness, exposed weak untyped dependency handles, serialized entities/components, and no renderer upload.
- Invalid scene payload failure with visible `AssetError::Decode` and failed events.
- Prefab load with documented `NGA_PREFAB_V1` payloads, dependency path registration, waiting state, dependency readiness, exposed weak untyped dependency handles, serialized root/children/components, and no renderer upload.
- Invalid prefab payload failure with visible `AssetError::Decode` and failed events.
- Missing path failure with visible `Failed` state, `AssetError::Io`, and failed event.
- Explicit unload by id with storage removal and unload event.
- Event cursor incremental behavior.

`Asset/engine_asset/tests/database.rs` covers:

- Source scan over `AssetIo::list`.
- Incremental source scan classification for added, changed, unchanged, and removed source assets while preserving registry ids, including OBJ model source hash invalidation when readable `.mtl` context sources change.
- Built-in importer dispatch and stable `AssetId` preservation across repeated imports.
- Importer-produced dependencies and generated subresource metadata persisted through registry, sidecars, and dependency report exports.
- Importer settings persisted in stable sorted form through registry, sidecar reloads, and incremental scan workflows.
- Built-in material importer shader/texture dependencies persisted through registry and sidecar reloads, emitted in dependency text/DOT reports, preserved through cook and database bundle build, and verified without manual metadata patching.
- Built-in material import/cook/runtime load from cooked bytes preserving dependency metadata and parsed material fields.
- Missing material dependency paths reported as visible `MaterialImporter` import errors with the referenced key/path.
- Quoted OBJ `maplib` / `usemap` display-attribute parsing now has explicit acceptance coverage in `database.rs` for both top-level and nested include sources, quoted OBJ `call` source inclusion now also has explicit acceptance coverage, quoted OBJ `call` line-continuation handling now also has explicit acceptance coverage, and quoted OBJ/MTL line continuations now have explicit acceptance coverage for `mtllib`/`usemtl` names plus `map_Kd` quoted texture-path continuations in both top-level and recursive nested-call contexts alongside the existing unquoted continuation case, so quoted marker tokens, quoted include-path resolution, and logical-line merges are exercised alongside the existing unquoted display/render cases without changing generated mesh output.
- Built-in model importer `NGA_MODEL_V1` manifests generating mesh/material/skeleton/animation/physics_mesh metadata and `.obj` sources generating mesh/material metadata with stable labels, generated bytes, `f`/`fo` face parsing and positive/relative OBJ face-index parsing, OBJ directive case-insensitive parsing, OBJ inline comment parsing, model mesh optimization degenerate triangle culling, generated LOD degenerate triangle culling, manifest mesh-to-material and mesh-to-physics_mesh dependencies, physics_mesh target mesh dependencies, material target mesh dependencies, generated LOD mesh dependencies, declared skin joint/influence limit validation, duplicate active skin-joint validation, multi-root skin skeleton root-scope validation, skin root-bone subtree validation, generated path collision diagnostics, duplicate generated dependency metadata diagnostics, invalid generated dependency list, typed syntax, and inline payload syntax diagnostics, standalone generated material/skeleton/animation payload diagnostics, duplicate MTL material-name diagnostics, loaded-MTL `usemtl` validation, persistent OBJ `usemtl` binding across `o`/`g` labels, per-material OBJ mesh splitting, typed generated dependency metadata, explicit animation skeleton target metadata, MTL emissive color canonicalization, and `ModelImporter::version()` 84 validation coverage including generated physics_mesh payloads, typed `physics_mesh:` dependencies, mesh physics_mesh bindings, physics_mesh target mesh bindings, explicit mesh LOD bindings plus in-memory and file whole/scoped dependency report exports on AssetDatabase and AssetServer, OBJ `usemtl` state persistence across `o`/`g` labels, OBJ directive case-insensitive parsing, OBJ face-outline alias parsing, OBJ/MTL backslash line continuation parsing, OBJ/MTL alpha source-channel preservation, OBJ homogeneous vertex coordinate parsing, OBJ parameter-space vertex validation, OBJ point/line element validation, OBJ free-form curve/surface/connection attribute validation, OBJ display/render, texture-map library/use-map, render-object, approximation-technique, and merging-group attribute validation, OBJ texture-coordinate non-zero `w` diagnostics, MTL direct RGB and `xyz` color canonicalization, visible unsupported `spectral` color diagnostics, MTL bool value case-insensitive canonicalization, MTL source-channel case-insensitive canonicalization, MTL projection case-insensitive canonicalization, MTL texture-map directive case-insensitive canonicalization, MTL texture option-name case-insensitive parsing, MTL material property directive case-insensitive parsing, OBJ inline comment parsing, model mesh optimization degenerate triangle culling, generated LOD degenerate triangle culling, OBJ smoothing-group normal generation, OBJ smoothing off value case-insensitive flat-normal parsing, OBJ `d`/`Tr` alpha-to-`alpha_mode=blend` material state mapping, OBJ `map_d`/`map_Tr`/`map_opacity`/`map_alpha`/`map_transparency` alpha texture-to-`alpha_mode=blend` material state mapping, OBJ `d -halo` option case-insensitive dissolve-halo alpha/custom material state mapping, OBJ `sharpness` custom material property, `map_bump`/`map_Kn`/`map_normal` normal map alias mapping, `map_aat` texture antialias custom bool preservation with case-insensitive bool values, MTL `-colorspace` texture color-space metadata preservation, MTL `-imfchan` source-channel case-insensitive canonicalization including `a`/`alpha`, MTL `-type` projection case-insensitive canonicalization, and `map_Tf`/`map_Ni` transmission/IOR texture dependency mapping, plus `map_Ks -imfchan blue` specular source-channel preservation and `map_Ns -imfchan green` roughness source-channel preservation, OBJ Exocortex PBR scalar/texture material extension mapping, OBJ `Pr`/`roughness` scalar roughness plus `map_Ns` roughness texture mapping, legacy `disp`/`decal`/`refl` texture map dependencies, and MTL `Ka`/`Ks`/`Tf`/`Ni`/`illum` typed custom material property case-insensitive preservation, generated animation target/track-shape/keyframe diagnostics, and `import_physics_meshes` filtering.
- Built-in model importer ambient-occlusion texture aliases map case-insensitive MTL `map_AO`, `map_Occlusion`, and `map_ambient_occlusion` directives to `texture.occlusion`, preserving registered texture dependencies, source-channel options, bundle manifest dependencies, runtime dependency graphs, and decoded `MaterialTextureBinding` values.
- Built-in model importer packed PBR texture aliases map case-insensitive MTL `map_MR`, `map_metallicroughness`, `map_metallic_roughness`, `map_roughnessmetallic`, and `map_roughness_metallic` directives to `texture.metallic_roughness`, `map_occlusionroughnessmetallic` and `map_occlusion_roughness_metallic` to `texture.orm`, `map_MRA` to `texture.mra`, and `map_ARM` to `texture.arm`, preserving registered texture dependencies, sampler/color-space/source-channel options, bundle manifest dependencies, runtime dependency graphs, and decoded `MaterialTextureBinding` values.
- Built-in model importer settings parse documented `ModelImportSettings` keys from `ImporterSettings`, persist them through sorted metadata/settings hashes, filter generated mesh/material/skeleton/animation/physics_mesh kinds, apply finite positive scale to manifest/OBJ generated mesh positions and manifest generated physics mesh vertices, control OBJ tangent generation, optimize generated meshes by dropping unreferenced vertices and deduplicating/remapping exact attribute tuples, generate deterministic `<Label>.LOD1` mesh subresources with degenerate-triangle culling, all-degenerate diagnostics, and copied material/skin dependency metadata, and report invalid bool/scale/LOD input values as visible `ModelImporter` import errors with source/settings context.
- Built-in model importer `.obj`/`NGA_MODEL_OBJ_V1` sources generating triangulated mesh subresources with expanded position/UV/normal vertex tuples, generated tangents, and trailing `#` inline comments, validated/ignored OBJ `vp` parameter-space vertices, validated/ignored OBJ `p`/`l` point/line elements, validated/ignored OBJ `cstype`/`deg`/`bmat`/`step`/`curv`/`curv2`/`surf`/`parm`/`trim`/`hole`/`scrv`/`con`/`sp`/`end` free-form curve/surface/connection attributes, validated/ignored OBJ `bevel`/`c_interp`/`d_interp`/`lod` display attributes, `maplib` texture-map libraries, `usemap` bindings, `shadow_obj`/`trace_obj` render-object attributes, `ctech`/`stech` approximation techniques, and `mg` merging groups, material subresources from `usemtl`, per-material generated mesh splitting for multi-material objects, generated mesh-to-material dependencies, `mtllib` provenance comments, resolved sidecar case-insensitive MTL direct RGB, descriptive scalar/color aliases, and `xyz` colors for base/emissive/ambient/specular/transmission material fields, visible unsupported `spectral` color diagnostics, alpha/metallic/roughness properties including transparent `d`/`Tr` alpha-to-blend material state, `map_d`/`map_Tr`/`map_opacity`/`map_alpha`/`map_transparency` alpha texture-to-`alpha_mode=blend` material state, `d -halo` option case-insensitive dissolve-halo alpha/custom material state mapping, `sharpness` custom material property, `map_bump`/`map_Kn`/`map_normal` normal map alias mapping, `map_aat` texture antialias custom bool preservation with case-insensitive bool values, MTL `-colorspace` texture color-space metadata preservation, MTL `-imfchan` source-channel case-insensitive canonicalization including `a`/`alpha`, MTL `-type` projection case-insensitive canonicalization, and `map_Tf`/`map_Ni` transmission/IOR texture dependency mapping, plus `map_Ks -imfchan blue` specular source-channel preservation and `map_Ns -imfchan green` roughness source-channel preservation, Exocortex PBR scalar/texture material extension mapping, common PBR scalar/color alias mapping, `Pr`/`roughness`, plus `Ka`/`Ks`/`Tf`/`Ni`/`illum` typed custom material properties, MTL context hashes that make MTL-only edits visible to incremental scans and stable-id reimports, registry-backed case-insensitive common alpha/opacity texture aliases (`map_opacity`/`map_alpha`/`map_transparency`), descriptive PBR texture aliases (`map_diffuse`/`map_albedo`/`map_basecolor`/`map_base_color`/`map_specular`/`map_emissive`/`map_emission`/`map_roughness`/`map_metallic`/`map_metalness`/`map_normalgl`/`map_normaldx`), descriptive PBR scalar/color aliases (`diffuse`/`albedo`/`basecolor`/`base_color`/`ambient`/`ambient_color`/`specular`/`specular_color`/`emissive`/`emission`/`emissive_color`/`emission_color`/`opacity`/`alpha`/`transparency`/`transmission_filter`/`transmission_color`/`metalness`/`clear_coat`/`clearcoatroughness`/`clear_coat_roughness`/`anisotropyrotation`), plus common and legacy MTL texture map dependencies including `map_Ns` roughness maps and `disp`/`decal`/`refl` displacement/decal/reflection maps with deterministic case-insensitive map option parsing/path extraction plus `-clamp` sampler-address with case-insensitive bool values, `-o`/`-s`/`-t`/`-bm` transform/bump payload preservation, `-mm`/`-imfchan`/`-boost` color-remap/channel/boost payload preservation with case-insensitive source-channel values, and `-blendu`/`-blendv`/`-cc` case-insensitive bool payload preservation plus `-colorspace`/`-type`/`-texres` color-space/projection/resolution payload preservation with case-insensitive texture-map directive matching, texture option names, and projection values, runtime-loadable cooked bytes, and visible invalid vertex/UV/normal/material-library/material-texture diagnostics including invalid `Pr`, duplicate `newmtl` names within/across loaded MTL sources, unsupported `spectral` color values, and `usemtl` names missing from fully loaded MTL source sets.
- Mesh importer source conversion preserving optional normals/UVs/secondary UV sets/tangents/four-influence skin joints and weights in runtime mesh bytes; runtime mesh loading now supports documented `NGA_MESH_BINARY_V1` binary payloads with a secondary-UV mask and optional `u16` index blocks, builds deterministic interleaved binary vertex buffers and `GpuUploadMetadata::Mesh` layout records, `MeshCooker` emits deterministic `NGA_MESH_BINARY_V1` cooked bytes from imported text or binary mesh payloads with mobile/web exact vertex compaction and `u16` index blocks when possible, and invalid attribute-count or binary payload diagnostics are reported before bad source reaches runtime.
- Model mesh block `skin=`/`skeleton=` metadata writes generated mesh-to-skeleton dependencies, persists through sidecars and bundle manifests, runtime-loads from bundle output, and reports unknown skeleton labels, invalid skeleton inverse binds, missing skin joint/weight attributes, duplicate skeleton bone names, out-of-range skin joints, zero-total skin weights, unnormalized skin weights, duplicate active skin joints, multi-root skin skeletons without explicit root-scope metadata, missing/repeated/zero skin joint limit metadata, skeletons exceeding declared skin joint limits, missing/repeated/zero/over-four skin influence limit metadata, skin vertices exceeding declared influence limits, missing/repeated/without-skin root-bone metadata, missing root bones, or weighted skin joints outside the declared root-bone subtree as visible `ModelImporter` import errors.
- Model mesh block `material=`/`materials=` metadata writes generated mesh-to-material dependencies, persists through sidecars and bundle manifests, runtime-loads from bundle output, and reports unknown or non-material generated labels as visible `ModelImporter` import errors.
- Model mesh block `lod=`/`lods=` metadata writes generated mesh-to-mesh LOD dependencies, persists through sidecars, bundle manifests, and runtime preload dependency graphs, and reports unknown or non-mesh generated labels as visible `ModelImporter` import errors.
- Model mesh block `physics_mesh=`/`physics_meshes=` metadata writes generated mesh-to-physics_mesh dependencies, persists through sidecars and bundle manifests, and reports unknown or non-physics_mesh generated labels as visible `ModelImporter` import errors. Model physics_mesh block `mesh=`/`target_mesh=` metadata writes generated physics_mesh-to-mesh dependencies, persists through sidecars and bundle manifests, runtime-loads through mounted bundles, and reports unknown, non-mesh, or repeated target metadata as visible `ModelImporter` import errors.
- Model material block `mesh=`/`target_mesh=` metadata writes generated material-to-mesh dependencies, persists through sidecars and bundle manifests, and reports unknown, non-mesh, or repeated target metadata as visible `ModelImporter` import errors.
- Model typed `depends=mesh:<Label>`/`material:<Label>`/`skeleton:<Label>`/`animation:<Label>` metadata records generated dependencies with kind validation. Standalone generated material, skeleton, and animation payloads are parsed during import; model animation block `skeleton=`/`target_skeleton=` metadata records generated skeleton dependencies and validates target labels; skeleton dependencies from typed metadata or `depends=` validate `track=bone:<BoneName>`, `track=node:<NodeName>`, and `track=node_index:<u32>` targets against generated skeleton bones plus generated animation track shape and keyframe times, reporting missing bones/nodes, out-of-range node indices, empty tracks, duplicate targets, and invalid keyframe timing as visible `ModelImporter` import errors.
- Import roots linking to generated subresource ids after stable-id reconciliation, with custom importer and ModelImporter coverage proving generated dependency edges persist through registry, sidecars, dependency reports, and scoped HTML.
- Model-generated material and physics-mesh dependencies persisted through registry, sidecars, dependency reports, and bundle manifests while generated mesh/material/skeleton/animation/physics_mesh bytes cook and runtime preload from a database-built bundle.
- Missing material dependencies inside model manifests reported as visible `ModelImporter` import errors with the referenced key/path.
- Built-in font import/cook/runtime load from cooked bytes preserving bitmap font payloads.
- Built-in physics mesh import/cook/runtime load from cooked bytes preserving trimesh payloads.
- Texture importer conversion from `NGA_TEXTURE_SOURCE_V1` text source into runtime texture bytes, including imported byte persistence, cook output, sidecar/registry metadata, runtime `AssetServer` load, and invalid-source importer diagnostics.
- Material importer canonicalization from authoring text with comments/spacing into stable runtime material bytes, including imported byte persistence, cook output, dependency metadata, runtime `AssetServer` load, and missing dependency diagnostics.
- Registry save/load persistence.
- Dependency report text, DOT, JSON, and self-contained HTML save-to-file exports for editor audit output, including scoped root subgraph JSON/HTML export and escaped path/type labels.
- Missing importer error path and importer validation failures with importer/source/settings context.
- Cooker dispatch by `AssetTypeId` and cooked/version hash update.
- Missing/failing cooker diagnostics with asset id/path, cooker name, and target platform context.
- `.meta` sidecar file creation and loading.
- Missing metadata diagnostics for new sources.
- Rename fallback that preserves `AssetId` across a source path move.
- Stale metadata diagnostics when sidecar source is missing.
- Dependency metadata restoration from sidecars.
- Built-in cooker registration and runtime-loadable texture cooked output.
- Built-in mesh import/cook/runtime load from cooked bytes with payload preservation and GPU upload completion.
- Built-in shader import/cook/runtime load from cooked bytes with payload preservation and GPU upload completion.
- Built-in audio import/cook/runtime load from cooked bytes with payload preservation.
- Audio importer conversion from `NGA_AUDIO_SOURCE_V1` text source into canonical runtime audio bytes plus `.wav` payload pass-through through import, cook output, runtime `AssetServer` load, and invalid sample-count diagnostics.
- Cook errors for missing metadata and missing source bytes.
- Registry malformed header/field diagnostics with line context.
- Sidecar malformed header diagnostics with file context.
- 12-field V1-compatible metadata payloads loading with empty labels and empty importer settings.
- Unsupported registry/sidecar version diagnostics that tell the caller to run metadata migration report.
- Dry-run metadata migration reports classifying registry entries and sidecars as current, upgradeable, unsupported-version, or invalid without mutating `AssetRegistry`, including migration-only V0 registry and sidecar headers.
- Write-back metadata migration reports that rewrite upgradeable registry and sidecar files to the current 14-field format while leaving the in-memory `AssetRegistry` untouched.
- Write-back migration preserving 13-field labels, defaulting missing importer settings to empty, and marking rewritten files with `written=true`.
- Write-back migration from V0 preserving path/source/cooked generated metadata paths and dependency ids while defaulting missing version hashes, labels, and importer settings.
- Write-back migration leaving invalid and unsupported-version files unchanged while still reporting blocking diagnostics.
- Database-built bundle output from cooked texture/material files.
- Bundle manifest dependency preservation from registry metadata.
- Database-built bundle readback plus `AssetServer` mount/preload readiness.
- RLE-compressed multi-chunk database-built bundle output from cooked texture files with `BundleReader` range/cache report coverage and runtime mount/preload readiness.
- Zstd-compressed multi-chunk database-built bundle output from cooked texture files with `BundleReader` range/cache report coverage and runtime mount/preload readiness.
- Bundle-build errors for missing metadata, missing cooked path/file, and stale cooked hashes.

`Asset/engine_asset/tests/bundle.rs` covers:

- Uncompressed bundle writer/reader round trip.
- Manifest dependency lookup.
- V2 bundle manifests exposing deterministic chunk layout metadata and per-entry chunk offsets.
- Legacy V1 bundle manifests loading as a single uncompressed chunk.
- Compression codec reports, zstd feature-status reporting, and compressed-chunk disabled-codec diagnostics that include the chunk index.
- RLE and default-feature Zstd codec-backed bundle writer/reader round trips with compressed-length manifest metadata and uncompressed chunk hash validation.
- `BundleReader::read_entry_range`, `BundleReader::read_path_range_with_report`, and `BundleAssetIo::read_range` returning entry-local byte ranges from compressed chunks with `BundleChunkReadReport` observability.
- `BundleChunkPartitionPolicy::MaxUncompressedBytes` creating deterministic multi-chunk manifests and preserving per-entry chunk indexes.
- `BundleChunkLoadingPolicy::OnDemandCached` and `OnDemandCachedLimited` reporting cache miss/hit behavior, max decoded chunk limits, LRU evictions, prefetch counts, and decoded chunk stats through `BundleChunkCacheStats`.
- Explicit `BundleReader`/`BundleAssetIo` chunk/path prefetch APIs with `BundleChunkPrefetchReport` showing requested chunks, newly decoded chunks, cache hits/misses, and evicted chunks.
- Invalid max-size chunk policy diagnostics.
- Corrupted RLE and Zstd chunk diagnostics for malformed compressed payloads.
- Bundle IO read/range/metadata/list behavior.
- Missing bundle entry error mapping.
- Composite IO default override precedence.
- Named source/mod/patch/base bundle precedence diagnostics for `read`, `metadata`, and `resolve`.
- De-duplicated composite listing diagnostics that report the highest-priority layer serving each path.
- `AssetPackageRegistry` save/load text persistence for package order, enabled state, layer kind, bundle id, payload path, and embedded manifest metadata.
- Package-level conflict reports that identify the first-layer-wins package and every lower-priority package shadowed for a path before reads occur.
- Package V2 registry persistence for package version and minimum runtime version, with V1 registry loading that defaults both values to `1`.
- Package V3 registry persistence for package dependency constraints, with V1/V2 registry loading that defaults missing dependency data to empty.
- Package update reports for added/removed/updated/enabled/disabled records, conflict summaries, semantic asset override diagnostics for overridden ids/type/hash/dependency/provider changes, runtime-too-old diagnostics, package-version-downgrade diagnostics, missing dependency providers, dependency-too-old/dependency-too-new diagnostics, default type-mismatch rejection, strict asset-compatibility rejection, and explicit downgrade allowance.
- `AssetServer::preview_asset_package_update` and `AssetServer::activate_asset_package_registry` covering non-mutating previews and rollback-safe activation that leaves mounted package manifests and loaded assets unchanged when compatibility fails.
- Package-registry-driven `CompositeAssetIo` construction from caller-supplied bundle payload bytes, including manifest mismatch and missing payload diagnostics.
- `AssetPackageArtifactStore` installing bundle bytes into package artifact paths, replacing same-name/same-bundle records, reporting payload hash/size, verifying enabled package artifacts, building composite IO from installed artifacts, and removing package records with optional artifact deletion.
- `AssetServer::verify_asset_package_artifacts` and `AssetServer::activate_asset_package_registry_from_artifacts` covering missing-artifact and mismatched-manifest diagnostics before runtime activation, with loaded asset state preserved when activation fails.
- Invalid package registry metadata diagnostics for malformed headers, truncated manifests, duplicate package names, duplicate bundle ids, duplicate priorities, empty fields, and duplicate manifest paths.
- `AssetServer::restore_asset_package_registry` preserving already-ready asset state while replacing package-managed mounted bundle manifests and honoring disabled package records on reload.
- `AssetServer` loading a texture through `BundleAssetIo`.
- `AssetServer` mounting bundle bytes, preloading manifest entries into a load group, finishing GPU upload, unmounting the bundle, and reporting invalid mount/unmount errors.
- `BundleWriter::write_file` writing bundle bytes to disk and returning a manifest that preserves dependency metadata.
- `MountedBundleRegistry` save/load round trip plus remounting persisted bundle metadata into a fresh `AssetServer` to drive real preload/readiness.

`Asset/engine_asset/tests/streaming.rs` covers:

- Streaming region preload progress and ready state from real asset states.
- Region priority controlling load scheduler order.
- Runtime updates to a streaming region's priority, including immediate queued-request reprioritization.
- Resident regions protecting assets from region unload until residency is cleared.
- Shared assets staying resident until every resident region releases them.
- Removing resident regions decrementing shared residency counts.
- Memory-budget eviction skipping resident streaming assets and resuming after residency clears.
- Registering a streaming region from every asset in a mounted bundle manifest, preserving manifest metadata and supporting preload/unload through real asset states.
- Registering and preloading a streaming region from an RLE-compressed multi-chunk mounted bundle manifest through on-demand cached bundle IO.
- Registering a streaming region from an explicit mounted bundle subset while rejecting ids that are absent from the manifest.

`Asset/engine_asset/tests/hot_reload.rs` covers:

- Successful hot reload preserving the handle id and replacing the asset after GPU upload completion.
- Reload start and upload state preserving the old CPU asset for rollback.
- Decode failure rollback preserving the previous ready asset and exposing the reload error.
- GPU upload failure rollback without replacing the old asset.
- Rollback policy reports distinguishing built-in `CpuAndGpu` retention from custom CPU-only retention.
- Custom/non-built-in asset reload failure rollback preserving previous CPU state with explicit diagnostics.
- Editor/debug hot-reload policy snapshots combining dependency policy, rollback policies, watched paths, polling backend/status, queued changes, and last poll report without mutating runtime state.
- Per-type rollback retention overrides, including opt-out behavior that leaves a failed custom asset reload in `Failed`.
- A later successful reload clearing a stale failure diagnostic.
- Successful reload event ordering through reload start, upload queue, reloaded, GPU finished, and ready.
- Reloading a dependency and its dependent material through the reverse dependency graph.
- Direct and transitive dependent reload planning from id/path without mutating asset state or events.
- Opt-in transitive dependent execution through `AssetServerConfig::hot_reload_dependency_policy`, reloading nested material and scene dependents while default execution remains direct.
- Deterministic and cycle-safe transitive dependent traversal for reload impact reporting.
- Polling watched paths through `AssetIo::metadata` and queuing reloads when metadata/hash changes.
- Debouncing a watched change when the same path is already queued for reload.
- Reporting per-batch watch metadata errors without dropping the watch.
- Async notification watch registration, start lifecycle report, pending/received/delivered counters, and per-watch `AsyncNotification` policy/status visibility.
- Async notification watches queuing only explicitly notified paths instead of scanning unchanged async watches during metadata polling.
- Async notification metadata error surfacing through poll reports, async watch reports, and per-watch policy status.
- Async notification stop behavior dropping pending notifications and reporting dropped counts; stopped backends reject later notifications without queueing reload work.

`Asset/engine_asset/tests/gc.rs` covers:

- Strong handle protection from unused GC.
- Drop-based lifecycle counts for cloned strong handles, weak clones, typed-to-untyped conversion, and promoted strong handles.
- GC protection while a promoted strong preload handle is live, followed by unload after that strong handle drops.
- Public `memory_info` / `memory_report` per-asset and per-type CPU/GPU bytes, strong/weak/dependency counts, resident state, and `last_used_frame`.
- Per-`AssetTypeId` total memory budget eviction that evicts the oldest over-budget type asset without touching unrelated asset types.
- Per-type budget protection from strong handles, dependency refs, and resident flags.
- Weak-only asset unload after the unused-frame window.
- Dependency reference protection.
- Dependency reference release when a dependent asset unloads, allowing the dependency to be collected.
- Resident asset protection until residency is cleared.
- Budget-driven oldest-first eviction using CPU/GPU bytes and `last_used_frame`, with `memory_report`/`memory_stats` agreeing after eviction.

`Asset/engine_asset/tests/dependency.rs` covers:

- Dependency failure propagation to a waiting parent asset.
- Dependency report edges, stable text/DOT/JSON/HTML exports, escaped HTML labels, scoped root dependency/dependent subgraphs, scoped HTML filtering, missing-root diagnostics, and topological order.
- Cyclic dependency detection with user-visible `CyclicDependency` errors.

`Asset/engine_asset/tests/ecs.rs` covers:

- Documented system stage order and descriptor names.
- Mesh/material component readiness through `AssetServer` and material dependencies.
- Public serialized component asset-field schema/query APIs for known ECS-facing component asset fields.
- `materialize_serialized_component` typed construction for known `MeshRenderer`, `AudioSource`, and `PhysicsCollider` serialized components, unknown component passthrough, server-tracked handle creation, required-field, duplicate-field, and type-mismatch diagnostics.
- Scene instantiation readiness, deterministic planning, structured asset-reference export, direct sink application, stable command export, generic host entity mapping, typed host component materialization, loaded flag update after successful host instantiation, missing-parent failure without mutating loaded state, and typed materialization failure without host spawn/attach or loaded-state mutation.
- Prefab instantiation readiness, deterministic planning, structured asset-reference export, direct sink application, stable command export, generic host entity mapping, typed host component materialization, and loaded flag update after successful host instantiation.
- Audio source readiness while exposing only handles, not owned audio resources.
- Physics collider readiness while exposing only handles, not owned physics resources.

`examples/asset_smoke` covers:

- Renderer-facing `MeshRendererComponent`, audio-facing `AudioSourceComponent`, and physics-facing `PhysicsColliderComponent` observing ready assets from a shared `AssetServer`.
- Consuming-system physics bridge from a ready asset `PhysicsMesh` into `engine_physics::PhysicsWorld` via `TriMeshDesc`, `ColliderDesc::trimesh`, and a ray query, keeping asset ownership in `AssetServer`.
- Handle count exposure for each consumer surface without taking ownership of loaded resources.
- Event visibility from the same public runtime APIs used by engine integration code.
- Material dependency readiness through `AssetServer::is_ready_with_dependencies`.
- Load-group state/progress for mesh/material/audio/physics paths.
- Event cursor observation for ready, dependency-ready, and failed-event counts.
- Workspace-level typed host scene/prefab instantiation through `TypedHostInstantiationSink`, including host parent mapping, typed `MeshRenderer` materialization, unknown `Transform` passthrough, typed asset-handle counts, and loaded-state no-repeat behavior.
- Editor-to-runtime smoke through `AssetDatabase` scan/import/cook, bundle build, runtime bundle mount/preload, and material load from the produced runtime output.
- Model-generated mesh/material/skeleton/animation/physics_mesh subresources from `AssetDatabase` model import, cooked and bundled, then loaded through runtime bundle APIs.

`Asset/engine_asset/tests/feature_flags.rs` covers:

- Default feature status and successful fallible registration of built-in importers/cookers.
- Disabled `async_loading` and `parallel` config requests returning visible `Unsupported` diagnostics via setters and loading policy reports.
- Enabled `async_loading` reporting `WorkerAsync` mode without feature errors.
- Enabled `parallel` accepting worker thread configuration without making worker threads effective until `async_loading` is enabled.
- Enabled `async_loading,parallel` reporting the requested parallel worker count as the effective async worker count.
- Default bundle/hot-reload/streaming entry points reaching their normal errors or success path instead of `Unsupported`.
- `--no-default-features` importer/cooker registration returning visible `Unsupported` errors and leaving import dispatch disabled.
- `--no-default-features` bundle mount, mounted-registry and package-registry save/load/restore/preview/activate/artifact-verify/artifact-activate/unmount, database bundle build/build bytes, hot-reload queue-by-id/path, polling and async watch registration/lifecycle/notification/poll, dependency planning, empty watch/policy reports, and streaming region registration, bundle registration, resident/preload/unload/progress/state/remove operations returning `Unsupported` before inner parsing or lookup work.

`Asset/engine_asset/tests/scheduler.rs` covers:

- Cancelling a queued load by id before decode, with `Cancelled` state and event.
- Cancelling by path and then requeueing the same asset id successfully.
- Cancelling an entire queued load group.
- Priority selection and de-duplication after cancelling a duplicate queued request.
- Synchronous loading budget using the stricter of IO and CPU per-frame job limits.
- Async loading dispatching queued IO/decode work to a background worker and collecting the result on a later update.
- Reusing the same async worker pool across multiple loads, reporting desired/active workers, in-flight jobs, dispatch/complete counts, cumulative started worker threads, and idempotent shutdown.
- Cancelling an in-flight async request before its worker result is applied, preserving `Cancelled` state and suppressing GPU upload handoff.
- Async+parallel loading dispatching multiple in-flight workers in one update when `worker_threads > 1`.

`Asset/engine_asset/tests/load_group.rs` covers:

- Mixed queued/ready/failed/cancelled progress counters and failure-dominant group state.
- Loading and ready CPU+GPU byte accounting for GPU-backed textures.
- `release_group` removing tracking without cancelling queued loads.

`Asset/engine_asset/tests/gpu_upload.rs` covers:

- Initial GPU upload failure producing `Failed`, `AssetError::GpuUpload`, and a failed event with no ready storage entry.
- `max_gpu_uploads_per_frame` limiting each drain and retaining later pending commands.
- Waiting dependents failing when a dependency GPU upload fails.

`Asset/engine_asset/tests/io.rs` covers:

- `FileSystemAssetIo` read/range behavior and empty out-of-range slices.
- Recursive listing with normalized logical paths.
- Missing file, read-range, metadata, non-file read, and list error mapping with `AssetIoAction`, logical path, display text, and source-message assertions.
- Composite IO read/metadata/list diagnostics across filesystem layers.

`Asset/engine_asset/tests/storage.rs` covers:

- `Assets<T>` count/contains, ready access, and CPU-only access.
- Typed get/get_mut plus direct iteration/mutation.
- Remove, metadata/count/error fields, and last-used tracking.

`Asset/engine_asset/tests/loader.rs` covers:

- Custom loader registration and path/extension resolution.
- Asset-type loader fallback when a path extension does not match.
- Explicit `LoadContext` dependencies and subresource registry entries.
- Custom CPU-only assets exposing dependencies through `AssetDependencies` without calling `LoadContext::dependency`, including dependency graph/metadata insertion, recursive dependency loading, readiness, and visited dependency references.
- Serialized `AssetRef<T>` fields contributing `AssetDependencyReference` fallback paths through `visit_dependency`, registering metadata, recursively loading the fallback asset, and updating dependency ref counts.
- Dependency failure propagation from an `AssetDependencies`-provided handle to the waiting parent asset.
- Unknown `AssetDependencies`-provided dependency ids failing as `AssetNotFound` and propagating `DependencyFailed` instead of leaving the parent in `WaitingForDependencies`.
- Type mismatch failures and missing loader diagnostics.

`Asset/engine_asset/tests/error_model.rs` covers:

- `NotLoaded` from unloading registered but never loaded assets without false unload events.
- Zstd feature-status consistency plus bundle parse errors.
- `AssetServer::insert_loaded*` producing ready assets with metadata, events, and memory info.
- `AlreadyLoaded` from attempts to replace ready or queued live assets, plus unload-then-reinsert behavior and display/comparison stability.

`Asset/engine_asset/tests/serde.rs` covers:

- `serde` feature round trips for typed `AssetRef<T>` fallback paths, stable asset/type ids, type names, content/version hashes, normalized labelled paths, and full `AssetMetadata` records.
- The same serialization-facing API under `--no-default-features --features serde`.

## Current Gaps

The following are still not complete and prevent final goal closure:

- `AssetDatabase` is still partial: richer dependency graph integration with real model format outputs is missing.
- Bundle system is still partial: IO, database build, server mount/preload, write-to-file, mounted-registry persistence, package-registry persistence/conflict/update reporting, artifact install/remove lifecycle, package dependency compatibility, semantic asset override policy, transaction-safe package activation, chunk-layout metadata, deterministic multi-chunk partitioning, bounded on-demand cached chunk decode with prefetch/eviction observability, real RLE codec paths, default-feature Zstd codec paths, and zstd-empty bundle writer manifest smoke now exist, but broader production codec tuning and packaging acceptance coverage are still missing.
- DLC/patch/mod override behavior now has IO layer precedence plus persistent package registries, artifact install/remove lifecycle, conflict reports, semantic asset override reports, package version/min-runtime/dependency/asset compatibility, and rollback-safe transaction activation, but broader mod/package acceptance coverage is still missing.
- Streaming, hot reload, and ECS are partial with runtime behavior/helpers; ECS now includes direct component-to-sink instantiation helpers, structured component asset-reference schema export, a generic host instantiation adapter with loaded-state lifecycle updates, and typed known-component materialization with server-tracked handles, while concrete renderer backend/audio/ECS crate adapters, deeper physics cooking integration, and non-asset gameplay component construction remain.
- Reload keeps direct reverse-dependency execution by default, can opt into transitive dependent execution, reports built-in/custom rollback retention, exposes editor/debug policy snapshots, supports per-type rollback overrides, and has polling plus async-notification watch backends; richer editor UI integration is still missing.
- Built-in importers and cookers remain partially implemented beyond texture/audio force-mono/normalize/streaming/compression source conversion plus canonical texture/material/audio/WAV cooking and mesh/shader/font/physics-mesh text-source conversion, material canonicalization, and `MeshCooker` deterministic binary mesh cooked output with mobile/web exact vertex compaction/deduplication plus index-width selection; material dependency extraction, material state-field loading/validation, shader runtime behavior, SPIR-V binary runtime payload handling, minimal audio/skeleton/animation/font/physics runtime loading, skeleton bind-pose matrix decoding, duplicate-name validation, and explicit inverse-bind validation, animation track-shape/keyframe timing validation, model inline/block manifest subresource generation, model import settings filtering/scale/tangent/mesh optimization and LOD generation with degenerate-triangle culling/physics-mesh generation control, model generated-path collision diagnostics, model material-to-mesh target validation, model mesh-to-material dependency validation, model mesh-to-skeleton skin dependency validation including declared skin joint/influence limits, skin root-bone subtree constraints, normalized positive weights, unique active skin joints, and multi-root skin skeleton root-scope metadata, standalone generated material/skeleton/animation payload validation, model generated dependency list syntax and typed target validation, animation-to-skeleton explicit target metadata, and track target/track-shape/keyframe timing validation, OBJ subset parsing with homogeneous vertex coordinate normalization, OBJ/MTL backslash line continuation parsing, parameter-space vertex validation, point/line element validation, free-form curve/surface/connection attribute validation, display/render, texture-map library/use-map, render-object, approximation-technique, and merging-group attribute validation, `f`/`fo` face parsing, positive/relative face-index, non-zero texture-coordinate `w` diagnostics, and UV/normal validation, runtime tuple expansion, tangent generation, resolved sidecar MTL material/emissive properties and descriptive scalar/color aliases, duplicate MTL material-name diagnostics, loaded-MTL `usemtl` validation, persistent OBJ `usemtl` binding across `o`/`g` labels, per-material OBJ mesh splitting, registry-backed case-insensitive common alpha/opacity texture aliases (`map_opacity`/`map_alpha`/`map_transparency`), descriptive PBR texture aliases (`map_diffuse`/`map_albedo`/`map_basecolor`/`map_base_color`/`map_specular`/`map_emissive`/`map_emission`/`map_roughness`/`map_metallic`/`map_metalness`/`map_normalgl`/`map_normaldx`), descriptive PBR scalar/color aliases (`diffuse`/`albedo`/`basecolor`/`base_color`/`ambient`/`ambient_color`/`specular`/`specular_color`/`emissive`/`emission`/`emissive_color`/`emission_color`/`opacity`/`alpha`/`transparency`/`transmission_filter`/`transmission_color`/`metalness`/`clear_coat`/`clearcoatroughness`/`clear_coat_roughness`/`anisotropyrotation`), plus common and legacy MTL texture map dependencies including `map_Ns` roughness maps and `disp`/`decal`/`refl` displacement/decal/reflection maps with deterministic case-insensitive map option parsing/path extraction plus `-clamp` sampler-address with case-insensitive bool values, `-o`/`-s`/`-t`/`-bm` transform/bump payload preservation, `-mm`/`-imfchan`/`-boost` color-remap/channel/boost payload preservation with case-insensitive source-channel values, `-blendu`/`-blendv`/`-cc`/`-colorspace`/`-type`/`-texres` blend/color-correction/color-space/projection/resolution payload preservation with case-insensitive texture-map directive matching, texture option names, and projection values, mesh secondary UV/skinning attribute preservation, positive normalized mesh skin-weight validation, binary mesh runtime/source/cooked payload validation, binary mesh vertex-buffer and index-format upload metadata, local generated dependency metadata, and root-to-generated dependency edges remain; the model importer now also has explicit `refl -type sphere` reflection-projection coverage. Broader production shader source analytics, richer model/bindings semantics, and broader engine subsystem integration remain missing.
- Dependency graph export now has stable whole-graph and scoped text/DOT/JSON plus self-contained HTML file-ready output with escaped metadata labels; richer dependency metadata from real model importers is still incomplete beyond current generated material, generated physics_mesh, mesh physics_mesh, physics_mesh target mesh, explicit mesh LOD, strict local generated, skin skeleton/root-bone, explicit animation skeleton, and OBJ material edges.
- Scene and Prefab runtime loading now have baselines, renderer/audio/physics-facing smoke coverage, a real `engine_render` consuming smoke bridge for loaded `Mesh`/`Material`/`Texture` assets, a headless `engine_renderer` resource-lifecycle smoke bridge for the same assets, a real `engine_physics` consuming smoke bridge for loaded `PhysicsMesh`, scene/prefab instantiation planning, structured component asset-reference export, generic host entity instantiation mapping, typed host component materialization, and workspace-level typed host smoke coverage, while the smoke example now also drives cooked audio assets through the same editor-to-runtime bundle path and an `AudioSourceComponent` bridge; real audio crate integration is still not wired.
- Feature flags now gate filesystem-backed IO behavior, zstd codec support, concrete built-in importers/cookers, database bundle-build internals, and selected `AssetServer` bundle/hot-reload/streaming runtime state; bundle entry-point smoke now also proves the enabled path yields an empty manifest with zero assets and a stable empty-name bundle, zstd gate smoke now also reads back the empty bundle manifest instead of only checking success, cooker gate smoke now also verifies cooked metadata plus imported/cooked bytes for texture and scene/prefab/animation/skeleton outputs, streaming gate smoke now proves an enabled empty streaming registration yields a stable, queryable region record with ready/zero-progress baselines, hot-reload gate smoke now also proves a queryable watch/report baseline, importers gate smoke now verifies texture importer metadata plus imported/cooked bytes and downstream scene/prefab dependency wiring rather than only checking registration success, enabled parallel gate smoke now also asserts `AssetFeatureStatus` consistency plus empty diagnostics, explicit unsupported diagnostics for disabled paths, and `require_supported()` on worker-count configuration, direct `Filesystem`, `AsyncLoading`, `Parallel`, `HotReload`, `Streaming`, `Editor`, and `Bundle` gate smoke now also exercise the composed feature status and no-default disabled diagnostics, and async-loading gate smoke now asserts the worker-async report fields rather than only checking success. `asset_smoke` now also carries runtime audio readiness through dependency-aware assertions in addition to distinct audio-source bridges, and the smoke example's editor path continues to cover physics, scene, and prefab ready/handle visibility. `async_loading`/`parallel` now have policy reports, disabled-feature diagnostics, reusable worker-backed async loading, effective parallel worker-count reporting, in-flight cancellation coverage, worker-pool lifecycle reporting/shutdown coverage, and async+parallel dispatch tests.

## Next Audit Target

After the next implementation slice, verify continued production-depth model importer behavior against:

- The selected importer performs deterministic validation/canonicalization instead of only passing source bytes through.
- Import metadata records source/cooked paths, hashes, importer version/settings, and remains stable across sidecars/registry reload.
- Cooked output and runtime `AssetServer` load reach `Ready` with expected decoded asset data where applicable.
- Invalid source payloads surface importer diagnostics with path/settings context.
- Existing default-feature runtime/editor/bundle/hot-reload behavior remains green.


Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test ecs
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime scene
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime prefab
C:\Users\JM\.cargo\bin\cargo.exe fmt -p asset_smoke
C:\Users\JM\.cargo\bin\cargo.exe test -p asset_smoke smoke_consumers_observe_ready_assets_without_owning_resources
C:\Users\JM\.cargo\bin\cargo.exe test -p asset_smoke
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_physics --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe check -p asset_smoke
```

Result: formatting passed; focused ECS schema/instantiation/host-adapter/materialization coverage passed (17 ECS tests), scene/prefab runtime dependency/error coverage stayed green (3 scene-filtered + 3 prefab-filtered runtime tests), focused asset smoke typed-host and physics bridge coverage passed, the asset smoke example passed 3 tests, engine_physics no-default tests passed 13 tests, and asset_smoke check passed.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p asset_smoke
C:\Users\JM\.cargo\bin\cargo.exe test -p asset_smoke smoke_consumers_observe_ready_assets_without_owning_resources
C:\Users\JM\.cargo\bin\cargo.exe test -p asset_smoke
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_render
C:\Users\JM\.cargo\bin\cargo.exe check -p asset_smoke
git diff --check
```

Result: formatting passed; focused asset smoke renderer bridge coverage passed with `engine_render::RenderScene` and `RenderQueue` scene/queue statistics; the asset smoke example passed 3 tests; engine_render passed 159 tests; asset_smoke check passed; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p asset_smoke -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p asset_smoke --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p asset_smoke --quiet
git diff --check
```

Result: asset_smoke formatting check passed; the asset smoke example passed 3 tests including the headless `engine_renderer` mesh/texture/material resource bridge; asset_smoke check passed; whitespace check passed with only CRLF conversion warnings.

Failed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_renderer --quiet
```

Result: engine_renderer full suite passed 435 of 450 tests and failed 15 existing backend-focused tests around wgpu material mip binding, backend hook/capture reporting, nonblocking backend completion polling, frame-capture resource accounting, and reflected facade pipeline-cache assertions. This audit change does not modify `Render/engine_renderer`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_converts_obj_xyz_material_colors
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_material_library_property
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; focused OBJ MTL `xyz` material color conversion passed through generated material bytes, cook output, bundle manifest, bundle runtime preload, and decoded runtime `Material` values; invalid `spectral` color syntax now surfaces a visible `ModelImporter` unsupported diagnostic with source path context; model_importer-only no-default check passed with one existing unused shader helper warning; model_importer feature-gate tests passed 9 tests; full database passed 164 tests; full engine_asset passed 349 integration tests; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_parameter_vertices
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_parameter_vertex
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
git diff --check
```

Result: formatting check passed; focused OBJ `vp` parameter-space vertex tests passed through generated mesh bytes, cooking, bundle manifest reads, bundle runtime preload, and decoded runtime `Mesh` values; invalid `vp` forms now surface visible missing, non-finite, and too-many-values `ModelImporter` diagnostics with source path context; full database passed 166 tests; full engine_asset passed 351 integration tests; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_point_and_line_elements
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_point_and_line_elements
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
git diff --check
```

Result: formatting passed; focused OBJ `p` point and `l` line element validation passed through generated mesh bytes, cooking, bundle manifest reads, bundle runtime preload, and decoded runtime `Mesh` values while omitting point/line data from triangle-only mesh payloads; invalid point/line forms now surface visible missing point vertex, missing referenced vertex, too-short line, missing line texture-coordinate, and invalid line-tuple `ModelImporter` diagnostics with source path context; full database passed 168 tests; full engine_asset passed 353 integration tests; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_display_attributes
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_display_attributes
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
git diff --check
```

Result: formatting passed; focused OBJ `bevel`/`c_interp`/`d_interp`/`lod` display/render attribute validation passed through generated mesh bytes, cooking, bundle manifest reads, bundle runtime preload, and decoded runtime `Mesh` values while omitting display-only state from triangle mesh payloads; invalid display attributes now surface visible invalid on/off, missing value, extra value, invalid `lod`, and negative `lod` `ModelImporter` diagnostics with source path context; full database passed 170 tests; full engine_asset passed 357 integration tests; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_display_attributes
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_display_attributes
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; focused OBJ `mg` merging-group validation passed through generated mesh bytes, cooking, bundle manifest reads, bundle runtime preload, and decoded runtime `Mesh` values while omitting merge/display-only state from triangle mesh payloads. Invalid `mg` forms now surface visible missing value, missing resolution, non-positive group, invalid group number, invalid resolution, non-finite resolution, and extra-value `ModelImporter` diagnostics with source path context. Full database passed 170 tests; full engine_asset default suite passed 355 tests across the emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_pbr_packed_texture_maps
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; focused OBJ MTL `map_RMA`/`map_ORM` packed PBR texture map preservation passed through generated material bytes, registry dependencies, cook output, bundle manifest reads, bundle runtime preload, runtime dependency graph, and decoded runtime `MaterialTextureBinding` names/options. Full database passed 171 tests; full engine_asset default suite passed 356 tests across the emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_ambient_occlusion_texture_aliases
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; focused OBJ MTL `map_AO`/`map_Occlusion`/`map_ambient_occlusion` ambient-occlusion texture alias preservation passed through generated material bytes, registry dependencies, cook output, bundle manifest reads, bundle runtime preload, runtime dependency graph, and decoded runtime `MaterialTextureBinding` names/options. Full database passed 172 tests; full engine_asset default suite passed 357 tests across the emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_pbr_packed_texture_aliases
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; focused OBJ MTL `map_MR`/`map_metallicroughness`/`map_MRA`/`map_ARM` packed PBR texture alias preservation passed through generated material bytes, registry dependencies, cook output, bundle manifest reads, bundle runtime preload, runtime dependency graph, and decoded runtime `MaterialTextureBinding` names/options. Full database passed 173 tests; full engine_asset default suite passed 358 tests across the emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_display_attributes
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_display_attributes
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; focused OBJ `maplib` texture-map library marker and `usemap` binding validation passed through generated mesh bytes, cooking, bundle manifest reads, bundle runtime preload, and decoded runtime `Mesh` values while omitting texture-map-only state from triangle mesh payloads. Invalid texture-map display forms now surface visible empty `maplib`, missing `usemap`, and extra-value `usemap` `ModelImporter` diagnostics with source path context. `ModelImporter::version()` is now 77. Full database passed 173 tests; full engine_asset default suite passed 358 tests across the emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_display_attributes
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_display_attributes
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; focused OBJ `shadow_obj` and `trace_obj` render-object attribute validation passed through generated mesh bytes, cooking, bundle manifest reads, bundle runtime preload, and decoded runtime `Mesh` values while omitting render-object-only state from triangle mesh payloads. Invalid render-object forms now surface visible missing-value and extra-value `ModelImporter` diagnostics for both directives with source path context. `ModelImporter::version()` remains 77 for the combined `maplib`/`usemap` plus render-object directive slice. Full database passed 173 tests; full engine_asset default suite passed 358 tests across the emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_display_attributes
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_display_attributes
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; focused OBJ `ctech` and `stech` curve/surface approximation technique validation passed through generated mesh bytes, cooking, bundle manifest reads, bundle runtime preload, and decoded runtime `Mesh` values while omitting approximation-only state from triangle mesh payloads. Invalid approximation forms now surface visible missing technique, unknown technique, missing value, non-finite/negative value, and extra-value `ModelImporter` diagnostics with source path context. `ModelImporter::version()` is now 77. Full database passed 173 tests; full engine_asset default suite passed 358 tests across the emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_free_form_attributes
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_free_form_attributes
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; focused OBJ free-form curve/surface/connection attribute validation accepted `cstype`/`deg`/`bmat`/`step` plus `curv`/`curv2`/`surf`/`parm`/`trim`/`hole`/`scrv`/`sp`/`end` through generated mesh bytes, cooking, bundle manifest reads, bundle runtime preload, and decoded runtime `Mesh` values while omitting free-form-only state from triangle mesh payloads. Invalid free-form forms now surface visible missing, unknown, non-positive, incomplete group, and missing-reference `ModelImporter` diagnostics with source path context. `ModelImporter::version()` is now 79. Full database passed 175 tests; full engine_asset default suite passed 360 tests across the emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_free_form_attributes
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_free_form_attributes
```

Result: formatting passed; focused OBJ `con` surface-connection validation now accepts references to previously declared `surf` and `curv2` statements while keeping connection-only data out of runtime triangle mesh payloads. Invalid `con` forms now surface visible missing surface, missing surface reference, missing curve2 reference, non-finite range, and extra-value `ModelImporter` diagnostics with source path context. `ModelImporter::version()` is now 79 for the combined free-form curve/surface/connection slice.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting check passed; full database passed 175 tests including OBJ free-form connection coverage; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; full engine_asset default suite passed 360 tests across the emitted test binaries; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_face_outline_alias
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_face_index
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; focused OBJ `fo` face-outline alias parsing passed through generated mesh bytes, cooking, bundle manifest reads, bundle runtime preload, and decoded runtime `Mesh` values using the same fan triangulation path as `f`. Invalid short `fo` faces now surface visible `ModelImporter` face diagnostics with source path context. `ModelImporter::version()` is now 80. Full database passed 176 tests; full engine_asset default suite passed 361 tests across the emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_parses_obj_line_continuations
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_line_continuation
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; focused OBJ/MTL backslash line continuation parsing passed through generated mesh/material bytes, cooking, bundle manifest reads, bundle runtime preload, decoded runtime `Mesh` indices, and decoded runtime `Material` base color. Dangling continuation markers in either the OBJ source or loaded MTL source now surface visible `ModelImporter` diagnostics with source path and MTL context. `ModelImporter::version()` is now 81. Full database passed 178 tests; full engine_asset default suite passed 363 tests across the emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime material_load_accepts_alpha_texture_source_channel
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime invalid_material_texture_source_channel_fails_with_decode_error_and_event
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_material_importer_canonicalizes_source_and_runtime_loads_it
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_alpha_source_channel
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime material
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features material_importer
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; runtime material parsing now accepts `alpha` texture source-channel metadata while preserving invalid-channel diagnostics; `MaterialImporter::version()` is now 5 and canonicalizes alpha source-channel metadata through import, cook, and runtime load. OBJ MTL `-imfchan A` now canonicalizes to `texture.alpha.source_channel=alpha`, preserving bundle manifest dependencies and runtime `MaterialTextureChannel::Alpha`; `ModelImporter::version()` is now 84. Runtime material focused tests passed 15 tests; full database passed 180 tests; full engine_asset default suite passed 366 tests across the emitted test binaries; material_importer/model_importer no-default checks and model_importer feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_maps_obj_common_alpha_texture_aliases_to_alpha_mode
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL `map_opacity`, `map_alpha`, and `map_transparency` now canonicalize to `texture.alpha` and set `alpha_mode=blend`, preserving registry dependencies, bundle manifest dependencies/readback, runtime preload dependency graphs, and decoded runtime `Material` alpha bindings. `ModelImporter::version()` is now 84. Full database passed 180 tests; full engine_asset default suite passed 366 tests across the emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_maps_obj_common_pbr_texture_aliases
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL descriptive PBR texture aliases now canonicalize to runtime channels: `map_diffuse`/`map_albedo`/`map_basecolor`/`map_base_color -> texture.albedo`, `map_specular -> texture.specular`, `map_emissive`/`map_emission -> texture.emissive`, `map_roughness -> texture.roughness`, `map_metallic`/`map_metalness -> texture.metallic`, and `map_normalgl`/`map_normaldx -> texture.normal`. The focused test preserves registry dependencies, generated material bytes, bundle manifest dependencies/readback, runtime preload dependency graphs, and decoded runtime `MaterialTextureBinding` names/ids. `ModelImporter::version()` is now 84. Full database passed 180 tests; full engine_asset default suite passed 366 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_maps_obj_packed_pbr_long_name_texture_aliases
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL packed PBR long-name texture aliases now canonicalize to runtime channels: `map_occlusionroughnessmetallic`/`map_occlusion_roughness_metallic -> texture.orm`, and `map_metallic_roughness`/`map_roughnessmetallic`/`map_roughness_metallic -> texture.metallic_roughness`. The focused test preserves registry dependencies, generated material bytes, bundle manifest dependencies/readback, runtime preload dependency graphs, and decoded runtime `MaterialTextureBinding` names/ids. `ModelImporter::version()` is now 85. Full database passed 181 tests; full engine_asset default suite passed 367 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_maps_obj_common_pbr_scalar_aliases
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL descriptive scalar/color aliases now canonicalize through generated material bytes and runtime `Material` fields: `diffuse`/`albedo`/`basecolor`/`base_color -> base_color`, `ambient`/`ambient_color -> custom.ambient_color`, `specular`/`specular_color -> custom.specular_color`, `emissive`/`emission`/`emissive_color`/`emission_color -> emissive`, `opacity`/`alpha` and `transparency -> alpha_mode=blend`, `transmission_filter`/`transmission_color -> custom.transmission_filter`, `metalness -> metallic`, `clear_coat -> custom.clearcoat`, `clearcoatroughness`/`clear_coat_roughness -> custom.clearcoat_roughness`, and `anisotropyrotation -> custom.anisotropy_rotation`. The focused test preserves generated material bytes, bundle manifest dependencies/readback, runtime preload dependency graphs, and decoded runtime `Material` properties/render state. `ModelImporter::version()` is now 86. Full database passed 182 tests; full engine_asset default suite passed 368 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_parses_obj_quoted_material_texture_paths
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_unterminated_obj_material_texture_quote
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL single/double quoted texture paths now parse as one texture path token, allowing spaces in registered texture asset paths while preserving map options before and after the path. Unterminated quoted texture paths now surface visible ModelImporter diagnostics with OBJ source path and MTL library/path/line context. The focused success test preserves generated material bytes, registry dependencies, bundle manifest dependencies/readback, runtime preload dependency graphs, and decoded runtime MaterialTextureBinding names/options. `ModelImporter::version()` is now 87. Full database passed 184 tests; full engine_asset default suite passed 370 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_maps_obj_common_pbr_texture_scalar_aliases
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL descriptive texture scalar/color aliases now canonicalize to runtime texture channels through generated material bytes and runtime `MaterialTextureBinding` values: `map_specular_color -> texture.specular`, `map_ambient`/`map_ambient_color -> texture.occlusion`, `map_emissive_color`/`map_emission_color -> texture.emissive`, `map_transmission_filter`/`map_transmission_color -> texture.transmission_filter`, `map_ior -> texture.index_of_refraction`, `map_clear_coat -> texture.clearcoat`, `map_clearcoatroughness`/`map_clear_coat_roughness -> texture.clearcoat_roughness`, and `map_anisotropyrotation -> texture.anisotropy_rotation`. The focused test preserves generated material bytes, sidecar dependency restoration, bundle manifest dependencies/readback, runtime preload dependency graphs, and decoded texture binding names/ids/options. `ModelImporter::version()` is now 88. Full database passed 185 tests; full engine_asset default suite passed 371 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_parses_obj_quoted_material_names_and_libraries
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_unterminated_obj_quote
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ source parsing now supports single/double quoted `mtllib` path tokens and quoted `o`/`g` labels, `usemtl` names, and MTL `newmtl` names while preserving the previous raw remainder behavior for unquoted names. The focused success test imports a quoted material library path containing spaces, matches quoted `usemtl` to quoted `newmtl`, preserves generated mesh/material labels and bytes, cooks and bundles the outputs, and runtime-loads the bundle with the expected mesh-to-material dependency graph and decoded material base color. Unterminated OBJ source quotes now surface a visible `ModelImporter` diagnostic with source path and line context. `ModelImporter::version()` is now 89. Full database passed 187 tests; full engine_asset default suite passed 373 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_hash_inside_quoted_obj_names
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ/MTL inline comment stripping is now quote-aware, so unquoted `#` still starts comments while `#` inside single/double quoted OBJ labels/material names and MTL `newmtl` names is preserved. The focused test preserves quoted `#` in generated mesh/material labels and material bytes, cooks and bundles the outputs, runtime-loads the bundle, preserves the mesh-to-material dependency graph, and decodes the expected material base color. `ModelImporter::version()` is now 90. Full database passed 188 tests; full engine_asset default suite passed 374 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_rejects_generated_dependency_cycles
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; `ModelImporter` now rejects generated subresource dependency cycles before emitting generated assets, while preserving the existing more specific diagnostics for self-dependencies, unknown generated labels, and typed dependency kind mismatches. The focused test covers a manifest mesh-to-material plus material-target-mesh cycle and verifies a visible source-context `ModelImporter` error. `ModelImporter::version()` is now 91. Full database passed 189 tests; full engine_asset default suite passed 375 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_transmittance_aliases
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL `Kt`/`transmittance`/`transmittance_color` now canonicalize to the existing `custom.transmission_filter.vec3` material field, and `map_Kt`/`map_transmittance`/`map_transmittance_color` now canonicalize to `texture.transmission_filter`. The focused test preserves generated material bytes, registry dependencies, bundle manifest dependencies/readback, runtime preload dependency graphs, decoded `MaterialTextureBinding` source-channel metadata, and decoded runtime custom transmission-filter values. `ModelImporter::version()` is now 92. Full database passed 190 tests; full engine_asset default suite passed 376 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_material_library_property
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL `illum` now remains a case-insensitive typed custom material property only when it is a standard illumination model value in the `0..=10` range. Out-of-range values such as `illum 11` now return a visible `ModelImporter` import error with OBJ source path, MTL library/path, line number, and accepted range while preserving the existing invalid-integer diagnostic. `ModelImporter::version()` is now 93. The focused invalid-property database test covers the new range diagnostic; full database passed 190 tests; full engine_asset default suite passed 376 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_material_library_property
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL `Ni` and `ior` now keep valid positive finite index-of-refraction values as `custom.index_of_refraction.float` while rejecting non-positive values such as `Ni 0` with a visible `ModelImporter` import error containing OBJ source path, MTL library/path, line number, and the positive-value requirement. `ModelImporter::version()` is now 94. The focused invalid-property database test covers the new range diagnostic while preserving the existing invalid-integer and `illum` range diagnostics; full database passed 190 tests; full engine_asset default suite passed 376 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_material_library_property
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL `d`/`opacity`/`alpha` and `Tr`/`transparency` now reject non-finite or out-of-range alpha/transparency values outside `0..=1` instead of silently clamping them into a material state. The focused invalid-property database test covers both `d -halo 1.25` and `Tr -0.1` visible `ModelImporter` diagnostics with OBJ source path, MTL library/path, line number, and accepted range while preserving the existing valid alpha-to-`alpha_mode=blend` behavior. `ModelImporter::version()` is now 95. Full database passed 190 tests; full engine_asset default suite passed 376 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_material_library_property
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL `Ns` shininess now accepts only finite values in the documented `0..=1000` range and maps valid values to roughness with `1 - sqrt(Ns / 1000)`. Out-of-range values such as `Ns 1001` now return a visible `ModelImporter` import error with OBJ source path, MTL library/path, line number, and accepted range instead of being silently clamped. `ModelImporter::version()` is now 96. The focused invalid-property database test covers the new `Ns` range diagnostic; full database passed 190 tests; full engine_asset default suite passed 376 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_material_library_property
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL `Pr`/`roughness` and `Pm`/`metallic`/`metalness` now accept only finite values in the `0..=1` unit range and preserve valid values as roughness/metallic material fields. Out-of-range values such as `Pr 1.2` and `Pm -0.1` now return visible `ModelImporter` import errors with OBJ source path, MTL library/path, line number, and accepted range instead of being silently clamped. `ModelImporter::version()` is now 97. The focused invalid-property database test covers both new unit-range diagnostics; full database passed 190 tests; full engine_asset default suite passed 376 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_material_library_property
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL `Ps`/`sheen`, `Pc`/`clearcoat`/`clear_coat`, `Pcr`/`clearcoat_roughness`/`clearcoatroughness`/`clear_coat_roughness`, and `aniso`/`anisotropy` now accept only finite values in the `0..=1` unit range while preserving valid values as typed custom material fields. Out-of-range values such as `Ps 1.1`, `Pc -0.1`, `Pcr 1.1`, and `aniso -0.1` now return visible `ModelImporter` import errors with OBJ source path, MTL library/path, line number, and accepted range. `ModelImporter::version()` is now 98. The focused invalid-property database test covers the new unit-range diagnostics; full database passed 190 tests; full engine_asset default suite passed 376 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_material_library_property
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL `sharpness` now accepts only finite values in the `0..=1000` range and preserves valid values as `custom.sharpness.float`. Out-of-range values such as `sharpness 1001` now return a visible `ModelImporter` import error with OBJ source path, MTL library/path, line number, and accepted range instead of writing invalid custom material state. `ModelImporter::version()` is now 99. The focused invalid-property database test covers the new range diagnostic while preserving the existing invalid-scalar diagnostic; full database passed 190 tests; full engine_asset default suite passed 376 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_material_texture_option
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ MTL texture-map `-texres` non-zero diagnostics are now covered and documented on the `ModelImporter` path. `-texres 0` returns a visible `ModelImporter` import error with OBJ source path, MTL library/path, directive, option name, rejected value, line number, and the greater-than-zero requirement, matching the runtime material loader's non-zero texture-resolution contract instead of surfacing a later material decode failure. `ModelImporter::version()` is now 100. The focused invalid texture-option database test covers the diagnostic while preserving the existing unknown-option diagnostic; full database passed 190 tests; full engine_asset default suite passed 376 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_combines_obj_multi_group_labels
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ `g` statements now accept multiple group names and canonicalize them into a stable generated mesh label by joining names with `.`, so `g Body Shell` imports as label `Body.Shell` and generated path `Body_Shell.mesh` instead of rejecting or preserving an ambiguous raw label. `ModelImporter::version()` is now 101. The focused database test covers generated metadata labels, sanitized generated path lookup, root-to-generated dependency metadata, runtime mesh payload bytes, and the updated importer version; full database passed 191 tests; full engine_asset default suite passed 377 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_includes_obj_call_sources
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_call_sources
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --no-default-features --features model_importer --test feature_flags
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; OBJ `call <relative.obj>` now recursively includes readable `.obj` context sources into the same parser state, so included geometry can generate normal model subresources and include-local `mtllib` declarations resolve relative to the included OBJ path. Nested include children now also resolve their own `mtllib` declarations relative to the child OBJ directory, including child-library texture paths under that directory, and changing a child-library `.mtl` source still flips the root model to `changed`. Invalid `call` statements now fail visibly for empty paths, unsupported macro arguments, invalid/parent paths, missing sources, non-OBJ sources, non-UTF-8 sources, recursive include cycles, and nested include parse failures. OBJ context hashing now folds in readable same-directory/subdirectory `.obj` files alongside `.mtl`, so include-source changes are detected by `scan_with_metadata` and stable-id reimport. The next regression adds explicit nested child quoted texture-path coverage, nested child quoted marker-path coverage, and a nested-child texture-path escape diagnostic. `ModelImporter::version()` is now 102. The focused database tests cover successful include import, include-relative MTL material payloads, generated mesh-to-material metadata, source-hash invalidation from included OBJ edits, stable-id reimport, nested child `mtllib` and child texture-path resolution, nested quoted texture-path resolution, nested quoted marker-path resolution, nested texture-path escape diagnostics, and invalid-call diagnostics; full database passed 235 tests; full engine_asset default suite passed 470 tests across emitted test binaries; model_importer-only no-default check and feature-gate tests passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime non_finite_material_numeric_property_fails_with_decode_error_and_event
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime material
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; runtime `MaterialLoader` now rejects non-finite material floats (`NaN`, `inf`, `-inf`) during decode instead of letting invalid material state reach renderer-facing GPU upload paths. The shared `parse_f32` path covers scalar material fields, vector fields such as `base_color`, texture transform/color-remap metadata, legacy unknown custom floats, and typed custom float/vector properties. The focused runtime test covers scalar, vec4, and typed custom vec3 non-finite inputs, verifies `Failed` state, preserved decode error text, and emitted failed event; material runtime filter passed 16 tests; full engine_asset default suite passed 380 tests across emitted test binaries; no-default check passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime non_finite_text_audio_f32_sample_fails_with_decode_error_and_event
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime audio_runtime_encoder_rejects_non_finite_f32_samples
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime audio
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; runtime text `NGA_AUDIO_V1` `format=f32` samples and the public `encode_audio_clip_runtime_bytes` helper now reject non-finite `f32` values (`NaN`, `inf`, `-inf`) with the same visible decode diagnostic instead of creating runtime bytes that cannot satisfy the loader's finite-sample policy. The focused runtime tests cover non-finite text sample decode failure state/events and encoder-side rejection of manually constructed non-finite `AudioClip` samples; audio runtime filter passed 11 tests; full engine_asset default suite passed 382 tests across emitted test binaries; no-default check passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime wav_integer_pcm_audio_bit_depths_load_as_i16_samples
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_builtin_wav_pcm24_import_cook_and_runtime_load_canonicalizes_to_i16
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime audio
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; `AudioLoader` now accepts RIFF/WAVE PCM `format=1` 8-bit unsigned, 16-bit signed, 24-bit signed, and 32-bit signed integer payloads, canonicalizing all integer PCM paths into `AudioSamples::I16` while retaining IEEE-float32 WAV as `AudioSamples::F32`. PCM8 uses 128 as the zero point, and PCM24/PCM32 preserve the high bits when reducing to i16. `AudioCooker::version()` is now 3 so newly supported WAV integer bit depths produce distinct cooked metadata. The focused runtime test covers PCM8/PCM24/PCM32 sample mapping, CPU-only readiness, duration, and no GPU upload; the database test covers PCM24 `.wav` import pass-through, cook output as canonical `NGA_AUDIO_V1` i16 bytes, `VersionHash(3)`, and runtime load from cooked bytes. The audio runtime filter passed 12 tests; full database passed 194 tests; full engine_asset default suite passed 384 tests across emitted test binaries; no-default check passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime wav_extensible_audio_subformats_load_through_runtime_loader
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_builtin_wav_extensible_float_import_cook_and_runtime_loads
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime audio
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; `AudioLoader` now recognizes WAVE_FORMAT_EXTENSIBLE (`format=0xfffe`) `fmt ` chunks, validates the extension size, valid-bits field, and subformat GUID, and maps PCM subformats into the existing PCM8/16/24/32 integer paths and IEEE-float subformats into the existing finite float32 path. Unsupported or malformed extensible GUID data now returns visible `AssetError::Decode` instead of falling through as a generic unsupported WAV format. `AudioCooker::version()` is now 4 so extensible WAV canonicalization is represented in cooked metadata. The focused runtime test covers extensible PCM24 and extensible float32 runtime loading, CPU-only readiness, duration, and no GPU upload; the database test covers extensible float32 `.wav` import pass-through, canonical cooked `NGA_AUDIO_V1` f32 bytes, `VersionHash(4)`, and runtime load from cooked bytes. The audio runtime filter passed 13 tests; full database passed 195 tests; full engine_asset default suite passed 386 tests across emitted test binaries; no-default check passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime invalid_wav_extensible_audio_subformat_fails_with_decode_error_and_event
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_builtin_wav_extensible_import_cook_reports_invalid_subformat
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime audio
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; WAVE_FORMAT_EXTENSIBLE error behavior is now covered in both runtime and editor/cook paths. Runtime loading of an extensible WAV with unsupported subformat tag `6` now reaches visible `Failed` state, preserves the `unsupported WAV extensible subformat 6` decode message, and emits a failed event. Database import still preserves the source `.wav` payload, while `AudioCooker` canonicalization fails visibly with `AudioCooker failed to canonicalize audio source` plus the same unsupported-subformat diagnostic. The audio runtime filter passed 14 tests; full database passed 196 tests; full engine_asset default suite passed 388 tests across emitted test binaries; no-default check passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime invalid_wav_extensible_audio_metadata_fails_with_decode_error_and_event
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime audio
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; WAVE_FORMAT_EXTENSIBLE metadata diagnostics are now covered on the runtime path for extension size, valid-bits, and GUID tail failures. Extensible WAV payloads with `cbSize < 22`, `valid_bits_per_sample > bits_per_sample`, or a non-standard GUID tail now reach visible `Failed` state, preserve the corresponding `AssetError::Decode` message, and emit failed events. The audio runtime filter passed 15 tests; full database passed 196 tests; full engine_asset default suite passed 389 tests across emitted test binaries; no-default check passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime wav_g711_audio_formats_load_as_i16_samples
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_builtin_wav_g711_import_cook_and_runtime_loads
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime audio
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; `AudioLoader` now decodes RIFF/WAVE G.711 A-law (`format=6`) and mu-law (`format=7`) 8-bit code payloads into `AudioSamples::I16`, with block-align/sample-count validation and CPU-only readiness matching the existing WAV paths. `AudioCooker::version()` is now 5 so G.711 WAV canonicalization is represented in cooked metadata while `MeshCooker` remains at `VersionHash(4)`. The focused runtime test covers A-law and mu-law sample mapping, duration, no GPU upload, and ready state; the database test covers `.wav` import pass-through, canonical cooked `NGA_AUDIO_V1` i16 bytes for both G.711 formats, `VersionHash(5)`, and runtime load from cooked bytes. The audio runtime filter passed 16 tests; full database passed 197 tests; full engine_asset default suite passed 391 tests across emitted test binaries; no-default check passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime wav_ima_adpcm_audio_loads_as_i16_samples
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime invalid_wav_ima_adpcm_audio_metadata_fails_with_decode_error_and_event
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_builtin_wav_ima_adpcm_import_cook_and_runtime_loads
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime audio
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; `AudioLoader` now decodes RIFF/WAVE IMA ADPCM (`format=17`) 4-bit block payloads with `samples_per_block` fmt-extension metadata into `AudioSamples::I16`, including mono blocks, stereo channel-chunk interleaving, block-align validation, declared sample truncation, step-index validation, and reserved-byte diagnostics. `AudioCooker::version()` is now 6 so IMA ADPCM WAV canonicalization is represented in cooked metadata while `MeshCooker` remains at `VersionHash(4)`. The focused runtime tests cover mono nibble decoding, stereo predictor/interleaving, duration, no GPU upload, ready state, and invalid step-index failure events; the database test covers `.wav` import pass-through, canonical cooked `NGA_AUDIO_V1` i16 bytes, `VersionHash(6)`, and runtime load from cooked bytes. The audio runtime filter passed 18 tests; full database passed 198 tests; full engine_asset default suite passed 394 tests across emitted test binaries; no-default check passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime wav_extensible_audio_subformats_load_through_runtime_loader
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime invalid_wav_extensible_audio_subformat_fails_with_decode_error_and_event
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_builtin_wav_extensible_encoded_import_cook_and_runtime_loads
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime audio
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; WAVE_FORMAT_EXTENSIBLE subformat handling now maps the already-supported G.711 A-law/mu-law and IMA ADPCM GUID tags into the same decoded `AudioSamples::I16` paths as direct `format=6`/`format=7`/`format=17` WAV payloads. Extensible PCM/IEEE-float still validate the valid-bits field, while extensible IMA ADPCM treats the same union field as `samples_per_block`. `AudioCooker::version()` is now 7 so these newly canonicalized extensible encoded WAV payloads have distinct cooked metadata. The runtime extensible test now covers PCM24, float32, A-law, mu-law, and IMA ADPCM success paths, while the unsupported-subformat test uses tag `99`; the database test covers extensible A-law and IMA ADPCM import pass-through, canonical cooked `NGA_AUDIO_V1` i16 bytes, `VersionHash(7)`, and runtime load from cooked bytes. The audio runtime filter passed 18 tests; full database passed 199 tests; full engine_asset default suite passed 395 tests across emitted test binaries; no-default check passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime wav_ms_adpcm
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_builtin_wav_ms_adpcm_import_cook_and_runtime_loads
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime audio
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; `AudioLoader` now decodes RIFF/WAVE Microsoft ADPCM (`format=2`) 4-bit block payloads with `samples_per_block` and coefficient-table fmt-extension metadata into `AudioSamples::I16`, including mono and stereo block layout, predictor/coefficient validation, delta validation, block capacity validation, and visible failed-state diagnostics. `AudioCooker::version()` is now 8 so MS ADPCM WAV canonicalization is represented in cooked metadata while existing PCM/G.711/IMA/extensible cooked outputs also use `VersionHash(8)`. The focused runtime filter passed 2 MS ADPCM tests; the full audio runtime filter passed 20 tests; full database passed 200 tests; full engine_asset default suite passed 398 tests across emitted test binaries; no-default check passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime font
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database font
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
git diff --check
```

Result: formatting passed; `FontLoader` now registers `font`, `ttf`, and `otf`, keeps bitmap `NGA_FONT_V1` loading, validates TrueType/OpenType sfnt signatures for binary `.ttf`/`.otf` payloads, derives binary font family names from asset path stems, preserves binary bytes, and reports visible decode diagnostics for invalid signatures or truncated headers. `FontImporter::version()` is now 3 and validates runtime/pass-through bitmap and binary font bytes before import output. `FontCooker::version()` is now 2 and validates bitmap/runtime plus TrueType/OpenType source bytes before writing cooked pass-through output. The focused runtime font filter passed 4 tests; the focused database font filter passed 5 tests; full database passed 202 tests; full engine_asset default suite passed 402 tests across emitted test binaries; no-default check passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_display_attributes
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_display_attributes
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; OBJ `maplib` texture-map library markers now validate every library token as a relative source path without labels or `..` segments, and `shadow_obj` / `trace_obj` now accept only `off` or the same relative source-path form instead of silently accepting invalid external references. `ModelImporter::version()` is now 103. The focused display-attribute success path still imports, cooks, bundles, preloads, and runtime-loads the generated mesh; the focused invalid display-attribute test now covers parent-path, quoted label-path, and absolute-path diagnostics. Full database passed 202 tests; full engine_asset default suite passed 402 tests across emitted test binaries; model_importer-only and bare no-default checks passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_manifest_structural_comments
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; `NGA_MODEL_V1` manifest block headers and pre-payload structural lines now use quote-aware trailing `#` comment stripping, so labels, generated dependency metadata, `---`, and `end` markers are not polluted or hidden by authoring comments while payload lines remain delegated unchanged to the asset-specific parser. `ModelImporter::version()` is now 104. The focused database test covers `mesh=Body # comment`, `depends=mesh:Body # comment`, `--- # payload`, and `end # block` while verifying stable generated paths, generated material bytes, generated dependency metadata, root dependency metadata, and importer versions. Full database passed 203 tests; full engine_asset default suite passed 403 tests across emitted test binaries; model_importer-only and bare no-default checks passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_unquotes_manifest_generated_labels
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_manifest_quoted_labels
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; `NGA_MODEL_V1` manifest generated labels now support single/double quoted tokens in block labels, inline labels, comma-separated local dependency lists, and typed `kind:<label>` dependency metadata. Quoted labels drop only the wrapper quotes while preserving internal `#` and comma characters, then continue to use the existing sanitized generated path and raw metadata label rules. Unterminated quoted labels and quoted dependency lists now fail visibly with `ModelImporter` source-path and line-number context. `ModelImporter::version()` is now 105. The focused success test covers quoted mesh/material/skeleton/animation labels, quoted material-list splitting, typed `depends=skeleton:"Rig #Main"`, stable generated paths, generated bytes, root metadata, and generated-to-generated dependencies; the focused failure test covers unterminated block label and dependency-list quotes. Full database passed 205 tests; full engine_asset default suite passed 405 tests across emitted test binaries; model_importer-only and bare no-default checks passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_manifest_binding_aliases
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; `NGA_MODEL_V1` manifest binding metadata now accepts production-style aliases while preserving the same generated dependency remapping and validation path: mesh `material_slot`/`material_slots`/`material_binding`/`material_bindings`, mesh `collision`/`collisions`/`collision_mesh`/`collision_meshes`, mesh `lod_mesh`/`lod_meshes`/`lod_level`/`lod_levels`, material `render_mesh`/`target_render_mesh`, and physics_mesh `source_mesh`/`render_mesh`/`target_render_mesh`. `ModelImporter::version()` is now 106. The focused database test covers alias-driven mesh-to-material, mesh-to-physics_mesh, mesh-to-LOD, material-to-mesh, and physics_mesh-to-mesh metadata dependencies plus stable generated bytes. Full database passed 206 tests; full engine_asset default suite passed 406 tests across emitted test binaries; model_importer-only and bare no-default checks passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_case_insensitive_manifest_structure_keys
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; `NGA_MODEL_V1` manifest structural keys now parse with ASCII case-insensitive rules while preserving generated labels and payload bytes: top-level generated kinds such as `Mesh=`/`MATERIAL=`/`PHYSICS_MESH=`, block metadata such as `Material_Slots=`/`Skin_Root=`/`Target_Render_Mesh=`/`Source_Mesh=`, and typed dependency kinds such as `Depends=Skeleton:Rig` all canonicalize to the existing generated dependency semantics. `ModelImporter::version()` is now 107. The focused database test covers mesh material/physics/skeleton dependencies, material target mesh aliases, animation typed skeleton dependencies, physics_mesh target mesh aliases, generated bytes, and root dependency metadata. Full database passed 207 tests; full engine_asset default suite passed 407 tests across emitted test binaries; model_importer-only and bare no-default checks passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_manifest_generated_kind_aliases
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; `NGA_MODEL_V1` manifest generated kind aliases now canonicalize common production/exporter names into existing generated asset types: `geometry`/`render_mesh -> mesh`, `mat -> material`, `skel`/`rig -> skeleton`, `anim`/`animation_clip`/`clip -> animation`, and `physics`/`collision`/`collision_mesh -> physics_mesh`. The same aliases are accepted in typed dependency prefixes such as `depends=skel:Rig` and `depends=geometry:Body`, while generated labels and payload bytes remain unchanged. `ModelImporter::version()` is now 108. The focused database test covers top-level kind aliases, typed dependency aliases, canonical asset types, generated bytes, and generated/root dependency metadata. Full database passed 208 tests; full engine_asset default suite passed 408 tests across emitted test binaries; model_importer-only and bare no-default checks passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_manifest_skeleton_metadata_aliases
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; `NGA_MODEL_V1` mesh/animation skeleton metadata now accepts production-style aliases while preserving existing generated dependency remapping and validation: mesh skin skeleton aliases `rig`, `skin_skeleton`, and `target_skeleton`; skin root aliases `root_joint` and `skin_root_joint`; skin joint limit aliases `max_joints` and `joint_limit`; skin influence limit aliases `max_influences` and `influence_limit`; and animation target aliases `rig`, `target_rig`, and `skeleton_target`. `ModelImporter::version()` is now 109. The focused database test covers alias-driven mesh-to-skeleton and animation-to-skeleton dependencies, root/joint/influence limit validation through successful import, generated bytes, and root dependency metadata. Full database passed 209 tests; full engine_asset default suite passed 409 tests across emitted test binaries; model_importer-only and bare no-default checks passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_manifest_dependency_metadata_aliases
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; `NGA_MODEL_V1` block dependency metadata now accepts production-style aliases while preserving the existing generated dependency remapping, typed kind aliasing, duplicate detection, cycle validation, and visible diagnostics: `dependency`, `dependencies`, `requires`, `require`, `refs`, and `references` all behave like `depends`. `ModelImporter::version()` is now 110. The focused database test covers alias-driven mesh-to-material, animation-to-skeleton, physics_mesh-to-mesh, material-to-physics_mesh, and mesh-to-mesh local generated dependencies plus stable generated bytes and root dependency metadata. Full database passed 210 tests; full engine_asset default suite passed 410 tests across emitted test binaries; model_importer-only and bare no-default checks passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_separator_insensitive_manifest_structure_keys
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; `NGA_MODEL_V1` manifest structural keys now accept ASCII case-insensitive spelling plus `-` and ASCII whitespace separator variants while preserving generated labels and payload bytes: top-level generated kinds such as `Render-Mesh=` and `Animation Clip=`, block metadata such as `material slot=`, `lod-level=`, `target rig=`, `source mesh=`, and `target render mesh=`, and typed dependency prefixes such as `physics-mesh:Collision` all canonicalize into the existing generated dependency semantics. `ModelImporter::version()` is now 111. The focused database test covers separator-normalized mesh/material/LOD/skeleton/animation/physics_mesh dependencies, canonical asset types, generated bytes, and root dependency metadata. Full database passed 211 tests; full engine_asset default suite passed 411 tests across emitted test binaries; model_importer-only and bare no-default checks passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_free_form_relative_references
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_free_form_attributes
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features --features model_importer
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; OBJ free-form reference validation now has focused success and failure evidence for relative references across `curv`, `curv2`, `surf`, `trim`, `hole`, `scrv`, `con`, and `sp`. The new success test imports, cooks, bundles, mounts, preloads, and runtime-loads an OBJ whose ignored free-form attributes use negative relative vertex/UV/normal/parameter-vertex/surface/curve references while preserving generated mesh bytes. The invalid free-form test now covers a missing negative `curv` vertex reference with visible `ModelImporter` source-path diagnostics. `ModelImporter::version()` remains 111 because this slice tightens documented/tested coverage for an existing parsing semantic. Full database passed 212 tests; full engine_asset default suite passed 412 tests across emitted test binaries; model_importer-only and bare no-default checks passed with one existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_free_form_relative_references
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_material_texture_option
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; `map_Kd` texture option handling now has focused evidence for `-blendu`/`-blendv`/`-cc` boolean option parsing alongside the preexisting texture option success coverage. The new failure case proves invalid boolean values surface a visible `ModelImporter` import error with the `.obj` source path, `.mtl` source path, option name, value, and line number in the message. Full database passed 212 tests; full engine_asset default suite passed 412 tests across emitted test binaries; bare no-default checks passed with the existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_includes_nested_obj_call_sources
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; nested OBJ `call` include sources now have explicit evidence that parent include state is shared across recursive depth: a parent include can declare `mtllib` and `usemtl`, a nested include can emit geometry without restating either, and the generated mesh still inherits the material dependency and runtime bytes. The focused nested-include test covers the shared-state path with visible `ModelImporter` output for generated mesh/material subresources. Full database passed 214 tests; full engine_asset default suite passed 414 tests across emitted test binaries; bare no-default checks passed with the existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_nested_obj_display_attributes
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; nested OBJ `call` include sources now also carry validated display/map/render/approximation attributes (`maplib`, `usemap`, `shadow_obj`, `trace_obj`, `ctech`, `stech`, `cstype`, `deg`, `bmat`, `step`, `parm`) without affecting generated runtime output. The new nested display-attributes test proves those directives remain recursive-include-safe and still produce the expected generated mesh bytes. Full database passed 214 tests; full engine_asset default suite passed 414 tests across emitted test binaries; bare no-default checks passed with the existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_includes_nested_obj_call_sources
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; nested OBJ `call` include source changes now invalidate the root model through `scan_with_metadata()` when the nested `.obj` body changes, proving the context-hash scan path sees recursive include depth rather than only the top-level source. The focused scan test preserves the original generated mesh/material output on first import, then detects the root model as `changed` after the nested include vertex data changes while keeping the shared include path and material library in the scan set. Full database passed 214 tests; full engine_asset default suite passed 414 tests across emitted test binaries; bare no-default checks passed with the existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_includes_nested_obj_call_sources
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset -- --check
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; nested OBJ `call` include source changes now invalidate the root model through `scan_with_metadata()` when the nested `.mtl` body changes as well, proving the context-hash scan path hashes recursive include directory context rather than only the top-level source. The focused scan test preserves the original generated mesh/material output on first import, then detects the root model as `changed` after the nested include material library data changes while keeping the shared include path and material library in the scan set. Full database passed 215 tests; full engine_asset default suite passed 415 tests across emitted test binaries; bare no-default checks passed with the existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_call_sources
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
```

Result: formatting passed; `OBJ call` now has an explicit negative-path regression for non-`.obj` sources in addition to macro-argument, parent-path, missing-source, and cycle coverage. The new case verifies `call part.model` fails with the expected `must reference a .obj source` diagnostic while preserving the existing recursive include behavior. Full database passed 214 tests; full engine_asset default suite passed 414 tests across emitted test binaries.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_call_sources
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; `OBJ call` now rejects an `.obj`-suffixed source that is actually an `NGA_MODEL_V1` manifest, proving the importer refuses manifest content even when the path extension looks correct. The regression keeps the recursive include path intact while checking the distinct `must be an OBJ source, not an NGA_MODEL_V1 manifest` diagnostic. Full database passed 214 tests; full engine_asset default suite passed 414 tests across emitted test binaries; bare no-default checks passed with the existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe fmt -p engine_asset
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_call_sources
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: formatting passed; `OBJ call` now also rejects non-UTF-8 nested include payloads, and the non-.obj, manifest-source, macro-argument, parent-path, missing-source, and recursive-cycle cases remain covered. Full database passed 214 tests; full engine_asset default suite passed 414 tests across emitted test binaries; bare no-default checks passed with the existing unused shader helper warning; whitespace check passed with only CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_includes_nested_obj_call_sources
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
```

Result: formatting passed; nested OBJ `call` include source changes still invalidate the root model through `scan_with_metadata()` even when the nested `.obj` payload becomes non-UTF-8 raw bytes, proving the scan path hashes raw include content rather than relying on UTF-8 decoding. Full database passed 214 tests; full engine_asset default suite passed 414 tests across emitted test binaries.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_uv_and_normal_data
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_non_utf8_obj_material_library
```

Result: formatting passed; `OBJ mtllib` now has an explicit relative-path regression in addition to the existing UTF-8, missing file, and duplicate-material coverage, so the material-library source edge cases are observable in tests. Full database passed 215 tests; full engine_asset default suite passed 415 tests across emitted test binaries.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_mesh_cooker_uses_u16_indices_for_mobile_and_web_targets
```

Result: formatting passed; `MeshCooker` now has explicit Android/iOS/Web coverage for `u16` index cooking, not just Web. The regression proves the same compacted mesh payload and `Uint16`-capable cooked bytes are produced for the three mobile/web targets when the indices fit in `u16`, while Windows remains on `u32` indices. Full database and default-suite counts remain unchanged at 215 and 415.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_server_rejects_packages_for_runtime_version_too_old
```

Result: the package activation path now has an explicit runtime-version gate regression. A package with `minimum_runtime_version=2` is rejected under the default runtime version `1`, `preview_asset_package_update()` reports `RuntimeTooOld`, and `activate_asset_package_registry()` fails with the expected `requires runtime package version 2, current runtime is 1` message. The existing bundle/package activation coverage remains unchanged otherwise.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: the bundle suite stays green with 28 tests, including the new runtime-version gate regression, and the default engine_asset suite stays green across 415 tests. The no-default check still passes with the existing unused shader helper warning, and whitespace checks still only report CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_server_rejects_packages_for_runtime_version_too_old
```

Result: the runtime-version gate regression now exercises both sides of the policy: runtime version `1` rejects the gated package, while `AssetPackageUpdatePolicy::new(2)` accepts and mounts the same package. This proves the compatibility check is policy-driven rather than hardcoded to the default runtime version.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_registry_reports_invalid_metadata_and_payload_mismatch
```

Result: bundle-registry validation now has explicit regression coverage for zero `package_version` and zero `minimum_runtime_version` inputs, in addition to the existing duplicate name/id/priority, empty path, duplicate manifest-path, malformed header, and payload-manifest mismatch cases.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test hot_reload hot_reload_async_watch_backend_start_and_stop_are_idempotent
```

Result: the async hot-reload watch backend now has an explicit state-machine regression for repeated `start` and `stop` calls. Starting twice keeps the backend running without disturbing queued notifications, stopping twice keeps it stopped without double-counting dropped notifications, and notifications sent while stopped still increment the dropped counter.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: the latest database pass is clean after the `OBJ mtllib` quoted-label fix and the non-UTF-8 material-library regression addition. `database_model_importer_reports_invalid_obj_face_index` now passes with the quoted `bad.mtl#Variant` label case, the full database suite passes 215 tests, the default engine_asset suite passes 415 tests, bare no-default checks still pass with the existing unused shader helper warning, and whitespace checks still only report CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_artifact_store_installs_builds_and_removes_package_files
```

Result: bundle artifact removal now has explicit coverage for both artifact-deleting and artifact-preserving paths. `delete_artifact=true` still removes the file and updates the registry, while `delete_artifact=false` keeps the artifact on disk, returns `artifact_removed=false`, and still allows `load_package_bytes()` to read the preserved payload after the logical registry removal.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_artifact_store_reports_missing_package_removal
```

Result: `AssetPackageArtifactStore::remove_package()` now has an explicit missing-package regression. Attempting to remove a package name that is not registered returns the expected bundle error instead of mutating registry state or touching artifacts.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_artifact_store_replaces_packages_with_matching_names
```

Result: `AssetPackageArtifactStore::install_package_bytes()` now has explicit replacement coverage. Installing a second package with the same name returns the replaced old record, updates the registry to the newer bundle id, and keeps the new artifact bytes readable through `load_package_bytes()`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_artifact_store_replaces_packages_with_matching_bundle_ids
```

Result: the artifact-store replacement branch now also covers bundle-id matching, not just name matching. A new package with the same `BundleId` replaces the old registry entry, returns the old record from `replaced`, and leaves the new artifact as the readable payload for the surviving record.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test streaming streaming_region_add_asset_reports_missing_region
```

Result: the streaming region API now has an explicit missing-region regression for `add_asset_to_streaming_region()`. Calling it with an unknown `StreamingRegionId` returns the expected `AddressNotFound` error instead of silently creating state or mutating a different region.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test streaming streaming_region_remove_asset_reports_missing_region
```

Result: `remove_streaming_region_asset()` now has the same missing-region regression coverage. An unknown `StreamingRegionId` returns the expected `AddressNotFound` error before any asset-removal bookkeeping runs.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test streaming streaming_region_priority_reports_missing_region
```

Result: `set_streaming_region_priority()` now has explicit missing-region coverage as well. An unknown `StreamingRegionId` returns `AddressNotFound` instead of mutating scheduler state or returning a bogus previous priority.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test streaming streaming_region_state_apis_report_missing_region
```

Result: the remaining streaming region state APIs now have explicit missing-region coverage. `set_streaming_region_resident()`, `preload_streaming_region()`, `unload_streaming_region()`, `streaming_region_progress()`, and `streaming_region_state()` all return `AddressNotFound` for an unknown region id rather than touching unrelated state or synthesizing defaults.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test streaming remove_streaming_region_reports_missing_region
```

Result: `remove_streaming_region()` now has the same missing-region regression coverage. Removing an unknown streaming region returns `AddressNotFound` before any residency release or state mutation happens.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_artifact_store_installs_builds_and_removes_package_files
```

Result: package artifact removal now also covers the post-delete read failure path. After `delete_artifact=true`, `load_package_bytes()` returns the expected IO error for the removed artifact path instead of silently reusing stale bytes.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_artifact_store_rejects_bundle_path_escape
```

Result: `AssetPackageArtifactStore` now has explicit artifact-path escape coverage. Both the helper path resolver and `install_package_bytes()` reject `"../escape.bundle"` before any artifact write can happen, so package payloads cannot escape the artifact root.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_artifact_store_rejects_absolute_bundle_path
```

Result: the artifact-path validator also rejects absolute bundle paths. A Windows-style absolute path such as `"C:/escape.bundle"` is rejected both by `artifact_path()` and by `install_package_bytes()` before any artifact write can happen.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test streaming streaming_region_resident_toggle_is_idempotent
```

Result: the streaming residency toggle now has an explicit idempotence regression. Repeating `set_streaming_region_resident(..., true)` or `(..., false)` on the same region does not double-count or double-release residency, and unload behavior remains stable across repeated calls.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_dependency_compatibility_reports_separator_diagnostics
```

Result: `AssetPackageRegistry::new()` now has explicit dependency-name separator coverage. Package dependency names containing `:` or `,` are rejected with the expected `dependency separators` bundle diagnostic before registry acceptance.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_dependency_compatibility_reports_missing_and_version_bounds
```

Result: package dependency validation now also has an explicit duplicate-entry regression. A package that declares the same dependency name twice is rejected with the expected `duplicate dependency` bundle diagnostic, in addition to the existing self-dependency and max-version bounds coverage.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_dependency_compatibility_reports_missing_and_version_bounds
```

Result: package dependency validation now also rejects zero minimum versions explicitly. A dependency declared with `min_version = 0` is rejected with the expected `min version must be greater than zero` bundle diagnostic before registry acceptance.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_registry_reports_invalid_metadata_and_payload_mismatch
```

Result: registry text parsing now also rejects malformed dependency fields explicitly. Corrupting the serialized dependency field so it no longer contains the expected `package:min:max` shape returns the expected `invalid asset package dependency field` bundle diagnostic before registry construction.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_registry_reports_invalid_metadata_and_payload_mismatch
```

Result: registry text parsing now also rejects malformed package counts explicitly. A registry header with `packages=abc` returns the expected `invalid asset package count` bundle diagnostic before any package lines are read.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_registry_reports_invalid_metadata_and_payload_mismatch
```

Result: registry text parsing now also rejects missing and malformed package lines explicitly. A registry that claims more packages than it contains reports `missing asset package line 1`, and a package line whose prefix is not `package` reports `invalid asset package line 0`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_registry_reports_invalid_metadata_and_payload_mismatch
```

Result: registry text parsing now also rejects manifest line count overflow explicitly. A package record whose manifest line count would overflow `usize` reports `asset package manifest line count overflow` before attempting to slice the manifest payload.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_registry_reports_invalid_metadata_and_payload_mismatch
```

Result: registry text parsing now also rejects malformed bundle IDs explicitly. Replacing the serialized package bundle id with `abc` returns the expected `invalid asset package bundle id` bundle diagnostic before registry construction.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_registry_reports_invalid_metadata_and_payload_mismatch
```

Result: registry text parsing now also rejects malformed priorities explicitly. Replacing the serialized package priority with `abc` returns the expected `invalid asset package priority` bundle diagnostic before registry construction.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_registry_reports_invalid_metadata_and_payload_mismatch
```

Result: registry text parsing now also rejects malformed package versions explicitly. Replacing the serialized package version with `abc` returns the expected `invalid asset package version` bundle diagnostic before registry construction.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_registry_reports_invalid_metadata_and_payload_mismatch
```

Result: registry text parsing now also rejects malformed minimum runtime versions explicitly. Replacing the serialized package minimum runtime version with `abc` returns the expected `invalid asset package minimum runtime version` bundle diagnostic before registry construction.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_registry_reports_invalid_metadata_and_payload_mismatch
```

Result: registry text parsing now also rejects malformed enabled flags explicitly. Replacing the serialized package enabled flag with `maybe` returns the expected `invalid asset package enabled` bundle diagnostic before registry construction.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_registry_reports_invalid_metadata_and_payload_mismatch
```

Result: registry text parsing now also rejects missing and malformed `packages=` lines explicitly. A registry with no `packages=` line returns `missing \`packages=\` line`, and a wrong prefix returns `expected \`packages=\` line` before any package parsing starts.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle mounted_bundle_registry_reports_manifest_line_count_overflow
```

Result: mounted bundle registry parsing now also rejects manifest line count overflow explicitly. Replacing the serialized mounted bundle manifest line count with `usize::MAX` returns `mounted bundle manifest line count overflow` before any slice of the manifest payload is attempted.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle mounted_bundle_registry_reports_missing_and_invalid_bundle_count_lines
```

Result: mounted bundle registry parsing now also rejects missing and malformed `bundles=` lines explicitly. A registry with no `bundles=` line returns `missing \`bundles=\` line`, a wrong prefix returns `expected \`bundles=\` line`, and a non-numeric bundle count returns `invalid mounted bundle count` before any bundle records are parsed.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle mounted_bundle_registry_reports_missing_and_invalid_bundle_lines
```

Result: mounted bundle registry parsing now also rejects missing and malformed bundle records explicitly. A registry that claims one bundle but provides none returns `missing mounted bundle line 0`, and a line whose prefix is not `bundle` returns `invalid mounted bundle line 0`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle mounted_bundle_registry_reports_missing_and_invalid_bundle_lines
```

Result: mounted bundle registry parsing now also rejects malformed bundle ids explicitly. Replacing the bundle id with `abc` returns `invalid mounted bundle id` before the mounted bundle manifest is parsed.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle mounted_bundle_registry_reports_missing_and_invalid_bundle_lines
```

Result: mounted bundle registry parsing now also rejects truncated manifests explicitly. Declaring two manifest lines for a mounted bundle but providing only one returns `mounted bundle 0 manifest is truncated`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_registry_reports_invalid_metadata_and_payload_mismatch
```

Result: asset package registry parsing now rejects unexpected trailing registry data explicitly. Appending an extra line after the declared package manifest returns `unexpected trailing asset package registry data` instead of silently ignoring the tail.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_registry_reports_invalid_metadata_and_payload_mismatch
```

Result: bundle manifest parsing now also rejects unexpected trailing manifest data explicitly. A manifest that declares zero chunks and entries but still includes an extra line returns `unexpected trailing bundle manifest data`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_server_save_and_load_package_registry_round_trip_preserves_mounted_bundles
```

Result: `AssetServer::save_asset_package_registry()` and `AssetServer::load_asset_package_registry()` now have a direct wrapper round-trip test. Saving a mounted registry to text and loading it into a fresh server preserves the mounted bundle list, while loading a missing registry file returns a visible IO failure without mutating the server's mounted bundle state.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle mounted_bundle_registry_round_trip_preserves_metadata_and_can_remount
```

Result: `AssetServer::save_mounted_bundle_registry()` and `AssetServer::load_mounted_bundle_registry()` now have a direct wrapper round-trip test. Saving a mounted bundle registry to text and loading it into a fresh server preserves the mounted bundle metadata, and loading a missing registry file returns a visible IO failure without mutating the mounted bundle state.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test streaming streaming_region_bundle_subset_deduplicates_repeated_ids
```

Result: streaming bundle subset registration now deduplicates repeated asset ids before region registration, so a subset request containing the same asset twice still registers a single region asset and avoids duplicate residency entries.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test hot_reload hot_reload_watcher_replaces_existing_watch_with_latest_backend_and_metadata
```

Result: repeating `watch_hot_reload_path*` on the same path replaces the prior watch entry instead of duplicating it, and the latest backend selection and metadata are reflected in the watch table.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test streaming streaming_region_paths_deduplicate_repeated_assets
```

Result: streaming region registration by path now deduplicates repeated paths before registration, so a region created from the same asset path twice still contains one asset handle.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test hot_reload hot_reload_async_watch_notification_is_dropped_after_unwatch
```

Result: async hot reload notifications that were already queued are explicitly dropped when the watch is removed before polling, and the drop is surfaced in the poll report.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test runtime scene_component_asset_fields_deduplicate_repeated_dependency_targets
```

Result: scene runtime dependency registration deduplicates repeated asset references in the same component payload, so a duplicated mesh/material reference only contributes one dependency edge per unique target.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_reflection_sphere_projection
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_material_texture_projection
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --quiet
C:\Users\JM\.cargo\bin\cargo.exe check -p engine_asset --no-default-features
git diff --check
```

Result: OBJ reflection texture projection coverage now includes the positive `refl -type sphere` path and a negative projection-regression path for invalid `-type` values. The focused database tests confirm the imported material preserves `texture.reflection.projection=sphere`, while the invalid case reports a visible `refl option -type value \`cylinder\`` import diagnostic with source-path context. The full database suite now passes 228 tests, the default engine_asset suite passes 462 tests, bare no-default checks still pass with the existing unused shader helper warning, and whitespace checks still only report CRLF conversion warnings.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_reports_invalid_obj_material_texture_remap
```

Result: OBJ material texture remap parsing now has a stable regression for malformed `-mm` tails. Supplying a fourth texture path on `map_Kd -mm ...` surfaces the expected `map_Kd texture paths` import diagnostic with model and material file context instead of silently accepting the extra path.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_material_texture_remap
```

Result: OBJ material texture remap parsing now also has a stable positive regression for `-mm` color-remap preservation. A valid `map_Kd -mm 0.2 0.7 textures/remap_albedo.texture` import keeps the remap values, preserves the resolved albedo dependency path, and loads back as a runtime `Material` whose `albedo` texture carries `color_remap=0.2,0.7`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_material_texture_resolution
```

Result: OBJ material texture resolution preservation now has a stable positive regression for `-texres`. A valid `map_Kd -texres 1024 textures/texres_albedo.texture` import keeps `texture_resolution=1024`, preserves the resolved albedo dependency path, and loads back as a runtime `Material` whose `albedo` texture carries `texture_resolution=1024`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_texture_antialiasing_enabled
```

Result: OBJ material texture antialiasing now has a stable positive regression for `map_aat ON`. A valid import keeps `custom.texture_antialias.bool=true`, preserves the resolved dependency path, and loads back as a runtime `Material` whose custom property is visible after bundle mount and runtime load.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_texture_raw_colorspace
```

Result: OBJ material texture color-space preservation now has a stable positive regression for `map_Kd -colorspace raw`. A valid import keeps `texture.albedo.color_space=raw`, preserves the resolved dependency path, and loads back as a runtime `Material` whose albedo texture carries the raw colorspace setting.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_texture_srgb_colorspace
```

Result: OBJ material texture color-space preservation now also has a stable positive regression for `map_Kd -colorspace srgb`. A valid import keeps `texture.albedo.color_space=srgb`, preserves the resolved dependency path, and loads back as a runtime `Material` whose albedo texture carries the sRGB colorspace setting.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_texture_linear_colorspace
```

Result: OBJ material texture color-space preservation now also has a stable positive regression for `map_Kd -colorspace linear`. A valid import keeps `texture.albedo.color_space=linear`, preserves the resolved dependency path, and loads back as a runtime `Material` whose albedo texture carries the linear colorspace setting.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_texture_color_correction
```

Result: OBJ material texture color-correction preservation now has a stable positive regression for `map_Kd -cc ON`. A valid import keeps `texture.albedo.color_correction=true`, preserves the resolved dependency path, and loads back as a runtime `Material` whose albedo texture carries the color-correction flag.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_texture_color_correction_disabled
```

Result: OBJ material texture color-correction preservation now also has a stable negative polarity regression for `map_Kd -cc off`. A valid import keeps `texture.albedo.color_correction=false`, preserves the resolved dependency path, and loads back as a runtime `Material` whose albedo texture carries the disabled color-correction flag.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_texture_blend_modes
```

Result: OBJ material texture blend-mode preservation now has a stable positive regression for `map_Kd -blendu ON -blendv OFF`. A valid import keeps `texture.albedo.blend_u=true` and `texture.albedo.blend_v=false`, preserves the resolved dependency path, and loads back as a runtime `Material` whose albedo texture carries the blend-mode flags.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_material_texture_boost
```

Result: OBJ material texture boost preservation now has a stable positive regression for `map_Kd -boost 1.5`. A valid import keeps `texture.albedo.boost=1.5`, preserves the resolved dependency path, and loads back as a runtime `Material` whose albedo texture carries the boost value.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_preserves_obj_material_texture_transform
```

Result: OBJ material texture transform preservation now has a stable positive regression for `map_Kd -o 0.25 0.5 0 -s 2 3 1 -t 0.01 0.02 0.03`. A valid import keeps the offset, scale, and turbulence payloads intact, preserves the resolved dependency path, and loads back as a runtime `Material` whose albedo texture carries the full transform state.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p asset_smoke
```

Result: `examples/asset_smoke` now exercises the same material texture-transform and boost metadata end to end. The editor smoke path loads a material with `texture.albedo.boost=1.25` and `texture.albedo.transform.offset/scale/turbulence`, then asserts those values survive to the runtime-loaded `Material` while the existing mesh/audio/physics/scene/prefab smoke checks continue to pass.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p asset_smoke
```

Result: the runtime smoke path now also loads a separate normal-map material that preserves `texture.normal.bump_scale=0.35` in the runtime `Material`, and now carries two independent audio clips through separate `AudioSourceComponent` bridges plus standalone `Skeleton`/`AnimationClip` readiness and `SkinnedMeshRendererComponent` scene/prefab coverage, giving the example suite direct coverage of another material texture option, a multi-audio consumer path, standalone animation/skeleton readiness, and skinned ECS consumption without going through the editor importer.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p asset_smoke
```

Result: the editor smoke path keeps the earlier albedo texture-transform and boost evidence, while the runtime smoke path now also covers a separate normal-map material, preserves `texture.normal.bump_scale=0.35` in the runtime `Material`, carries two audio clips through distinct `AudioSourceComponent` bridges, and includes standalone `Skeleton`/`AnimationClip` readiness plus `SkinnedMeshRendererComponent` scene/prefab coverage.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database roughness_extensions
```

Result: the roughness texture regression now also preserves `texture.roughness.color_space=non_color` for `map_Ns -colorspace Non-Color` in addition to the existing roughness source-channel evidence, and the runtime-loaded `Material` carries the same color-space setting through the bundle path.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_scene_and_prefab_importers_preserve_runtime_documents_and_dependencies
```

Result: `SceneImporter` and `PrefabImporter` now have direct importer evidence: scene and prefab source documents preserve their runtime bytes, register dependency ids through `ImportContext`, and report `VersionHash(1)` with importer/version/source metadata intact.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_audio_font_and_physics_mesh_importers_preserve_runtime_documents_and_metadata
```

Result: `AudioImporter`, `FontImporter`, and `PhysicsMeshImporter` now have direct importer evidence: audio/font/physics source documents preserve or canonicalize runtime bytes, report the expected importer/version/source metadata, and keep their generated bytes and runtime document outputs stable through `ImportContext`.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_material_importer_preserves_texture_metadata_round_trip
```

Result: `MaterialImporter` now has direct importer evidence for texture metadata fields beyond the existing source-channel coverage: a direct material source document preserves albedo/normal texture metadata such as source-channel, boost, color-correction, colorspace, and bump-scale through `ImportContext`, and the runtime-loaded `Material` reads the same texture options back.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_font_importer_preserves_binary_font_bytes
```

Result: `FontImporter` now has direct importer evidence for validated binary font files: `.ttf` and `.otf` sources preserve their raw bytes through `ImportContext`, keep importer/version metadata stable, and load back into runtime `Font` values with the expected TrueType/OpenType data variants.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle bundle_zstd_compression_round_trip_exposes_chunk_reports_and_prefetches
```

Result: the Zstd bundle path now has a non-empty on-demand cached regression in addition to the empty-manifest smoke. A multi-chunk Zstd bundle now exercises `prefetch_path`/`prefetch_chunk`/`prefetch_paths` cache misses, LRU eviction, chunk cache statistics, and `BundleChunkReadReport` cache-hit behavior while still round-tripping the expected compressed chunk metadata.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle bundle_writer_writes_zstd_file_and_returns_manifest
```

Result: the Zstd bundle path now also has a file-backed write/read regression. Writing a multi-chunk Zstd bundle to disk now preserves the manifest name/compression/entry count, and reading the file back through `BundleReader` and `BundleAssetIo` keeps the compressed metadata, chunk cache policy, path reads, and dependent entry metadata stable.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle bundle_writer_writes_zstd_file_and_returns_manifest
```

Result: the same Zstd file-backed regression now also pins manifest dependency preservation. The written bundle keeps the root entry dependency graph intact on disk and when reloaded through `BundleReader`/`BundleAssetIo`, including the dependent entry's path hash and the dependent read path itself.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle bundle_writer_writes_zstd_file_and_returns_manifest
```

Result: the file-backed Zstd regression now also verifies read and range diagnostics from `BundleAssetIo`. Reopening the bundle from disk keeps `read_with_report` / `read_range_with_report` path metadata and Zstd chunk-compression reporting stable, not just the plain `read`/`metadata` success path.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_server_activates_zstd_artifact_registry_without_disrupting_ready_assets
```

Result: `AssetServer::activate_asset_package_registry_from_artifacts()` now has an explicit Zstd artifact-registry regression. A Zstd-compressed package artifact is installed, exposed through composite IO, activated from artifacts, and loaded into a ready `Texture` without disturbing the pre-existing ready asset state.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_server_activates_zstd_artifact_registry_without_disrupting_ready_assets
```

Result: the Zstd artifact-registry regression now also covers package removal and restore. Removing the Zstd package deletes its artifact, keeps the remaining base package available, and a subsequent `restore_asset_package_registry()` shrinks the mounted registry back to the surviving base bundle without disturbing the ready texture.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_server_activates_zstd_artifact_registry_without_disrupting_ready_assets
```

Result: the Zstd artifact-registry regression now also pins text registry persistence. Saving the registry to text and loading it back preserves the Zstd package records, and the reloaded registry still builds the same composite IO view for the Zstd package bytes.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_artifact_store_installs_and_builds_zstd_audio_package_files
```

Result: the audio partial now has a file-backed package-artifact regression. A Zstd Ogg audio package can be installed, read through composite IO and range reads, persisted to registry text and loaded back, and removed while preserving registry integrity and artifact cleanup.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test bundle asset_package_artifact_store_installs_builds_and_removes_package_files
```

Result: the artifact store path now also exercises Zstd-compressed package artifacts. Installing a Zstd bundle through `AssetPackageArtifactStore` preserves payload size/hash, and `build_composite_io()` can read the compressed package payload back through a composite IO layer with the expected texture bytes.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_face_outline_aliases
```

Result: the OBJ face-outline alias now has a positive regression. `Fo` face lines are accepted as regular OBJ faces, import into a generated mesh subresource, preserve dependency metadata, and round-trip through database cook/bundle/runtime preload with the same triangle geometry as `f` faces.

Passed:

```text
C:\Users\JM\.cargo\bin\cargo.exe test -p engine_asset --test database database_model_importer_accepts_obj_render_object_off_attributes
```

Result: OBJ render-object off-state coverage now has a positive regression. `shadow_obj off` and `trace_obj off` are accepted as ignored render-object directives, including in nested `call` include sources, while `off extra` still trips the same arity diagnostics as other invalid values; the generated mesh still imports/cooks/bundles/preloads correctly so render-object handling is no longer only exercised through path-based validation cases.
