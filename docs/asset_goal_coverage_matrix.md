# NeoGameEngine Asset Goal Coverage Matrix

Last updated: 2026-06-11

Status meanings follow `docs/asset_goal.md`: `Implemented` requires reachable public API, real execution, visible errors/observability, focused tests, and synchronized docs. This matrix is not a final acceptance claim.

## Summary

| Status | Count | Notes |
| --- | ---: | --- |
| Implemented | 28 | Runtime MVP slice plus smoke example, sidecar metadata and migration write-back/reporting including V0-to-V1 transforms, Bundle/Composite IO, bundle chunk-layout metadata, RLE/Zstd bundle compression, configurable bundle chunk partition/cache policy, persistent package registry/conflict/semantic-override audit, bundle-backed streaming regions, scheduler cancellation, reusable async worker-pool loading, load-group progress, hot reload rollback-policy/polling+async-notification watcher/dependency-planning/transitive-execution baseline, Drop-based handle lifecycle counts for server-created handles, public memory reports, GC budget/per-type policy behavior, built-in cooker baseline, material importer dependency extraction, mesh/shader/audio/material runtime loader coverage, dependency graph diagnostics/exports including deterministic HTML reports, serialized `AssetRef<T>` dependency visitor support, feature-gate diagnostics and individual feature builds, ECS helpers including scene/prefab instantiation, component-schema asset-reference export, generic host instantiation adapter mapping, and typed known-component materialization, explicit `AlreadyLoaded` insertion rejection, and focused runtime/editor tests; WGSL compiler diagnostics now include line/column plus naga diagnostics context. |
| Partial | 15 | Meaningful runtime/editor/bundle/streaming/hot-reload/cooking/dependency/ECS/override behavior exists, but import conversion or integrations are incomplete. |
| Stub | 0 | No current asset-goal category is only data-only shape; remaining repository-implementable gaps are partial. |
| Missing | 0 | No current coverage-matrix row is classified as missing; remaining repository-implementable gaps are tracked as partial rows. |
| External Blocked | 0 | No true external blocker identified in this slice. |

## Runtime Core

| Capability | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Crate/module structure and prelude | Implemented | `Asset/engine_asset/src/lib.rs`, `prelude.rs`; workspace member added in root `Cargo.toml`. | Keep docs synchronized as new modules mature. |
| Feature flags | Implemented | `Asset/engine_asset/Cargo.toml` defines feature groups/defaults including default `filesystem`, `serde`, and `zstd`; `features.rs` exposes status/require helpers for runtime/build features including `AssetFeature::Filesystem`, `AssetFeature::Serde`, and `AssetFeature::Zstd`; concrete built-in importer/cooker implementations and built-in registration paths are gated behind their Cargo features; `FileSystemAssetIo` returns visible read/metadata/list errors and default `AssetServer` filesystem loads surface `AssetError::Io` when `filesystem` is disabled; database bundle-build internals are cfg-pruned behind `bundle`; `AssetServer` cfg-prunes bundle registry/reader state, hot-reload queue/watch state, and streaming region/residency state while retaining fallible disabled-feature stubs; bundle mount/registry operations, database bundle build, hot-reload queue/watch/poll/dependency planning, and streaming region registration/lifecycle/progress return visible `Unsupported` errors when disabled; zstd bundle writer/reader reports visible disabled-codec errors when `bundle` is enabled without `zstd`; `AssetLoadingPolicyReport` exposes `async_loading`/`parallel` status, disabled-feature diagnostics, `WorkerAsync` mode, effective worker counts, and effective per-frame IO/CPU budget; enabled `async_loading` uses a reusable worker pool with lifecycle reports, shutdown, dispatch/complete counters, in-flight cancellation, and later result collection; enabled `parallel` makes `worker_threads > 1` the async in-flight worker limit; `tests/feature_flags.rs` now covers default, `--no-default-features`, individual `filesystem`, `bundle`, `hot_reload`, `streaming`, `editor`, `importers`, `cookers`, `texture_importer`, `texture_cooker`, zstd status, and async/parallel paths with gate-aware filesystem/runtime/import/cook diagnostics; individual model/material/audio/shader importer and cooker feature builds compile under `--no-default-features`; `tests/serde.rs` covers default and `--no-default-features --features serde` round trips for serialization-facing IDs, paths, `AssetRef<T>`, and `AssetMetadata`; `tests/scheduler.rs` covers async worker-pool reuse/shutdown. | Broaden only when new feature flags or cfg-pruned modules are introduced. |
| Core IDs and hashes | Implemented | `id.rs`: `AssetId`, `AssetTypeId`, `AssetTypeName`, `ContentHash`, `VersionHash`. | Final API may still adopt UUID-backed IDs if the repo accepts a new dependency. |
| Asset paths and labels | Implemented | `path.rs`; tests use path lookup, dependency paths, rename fallback through `.meta` sidecars, and model-generated subresource labels persisted through registry/sidecars. | Broaden only if runtime subresource label IO semantics change. |
| Asset trait, memory usage, dependency visitor | Implemented | `asset.rs`; built-in assets implement memory/dependency traits where relevant; `AssetDependencyReference` carries id/type plus optional fallback path; `AssetRef<T>::visit_dependency` lets serialized references feed dependency visitors without manual untyped-handle construction; `LoadedAsset::new_with_asset_dependencies`, `with_dependency_handles`, and `with_dependency_refs` let custom loaded assets surface dependency references into `AssetServer`; store/load flow merges asset-provided references with loader-context dependencies into dependency graph/metadata and recursive dependency loading; fallback paths are registered into metadata for recursive loads; asset-provided dependency ids with no live state and no registry metadata/path/fallback fail visibly as `AssetNotFound` and propagate `DependencyFailed` to the waiting parent; `tests/loader.rs` covers a custom CPU-only asset exposing dependency handles and serialized `AssetRef<T>` dependencies without calling `LoadContext::dependency`, including success, dependency-failure propagation, unknown-id diagnostics, dependency ref counts, and fallback-path recursive loading. | Broaden only if future serialized reference policies add asset generations or package-scoped indirection. |
| Typed/Untyped handles and `AssetRef<T>` | Implemented | `handle.rs`, `ref_asset.rs`; typed/untyped conversion, strong/weak values, and automatic Drop-based strong/weak retain/release for handles created by `AssetServer`; derived clones, `clone_weak`, `clone_strong`, `untyped`, and `UntypedHandle::typed` update observable counts until dropped; standalone identity handles remain intentionally untracked; `tests/gc.rs` covers cloned/dropped handles and GC protection transitions. | Broaden only if handles gain generations, cross-server ownership transfer, or serialized handle lifecycle restoration. |
| Load states | Implemented | `events.rs`; server transitions through queued/loading/decoding/waiting/uploading/ready/failed/cancelled/reloading/unloading; tests cover reload start, rollback to ready after decode/GPU failure, and cancellation states. | Add only when new async states are introduced. |
| Events and event cursors | Implemented | `events.rs`, `server.rs`; load, ready, failed, unload, reload, dependency, GPU, and cancellation events are emitted; `event_cursor_only_returns_new_events` test. | Add retention-policy tests as event volumes grow. |
| Typed storage `Assets<T>` | Implemented | `storage.rs`; server access APIs and unload test; `tests/storage.rs` covers count/contains, ready vs CPU-only access, get/get_mut, iteration/mutation, remove, metadata/count/error fields, and usage tracking. | Broaden only when storage adds generational handles or async mutation APIs. |
| AssetServer runtime facade | Implemented | `server.rs`; tests cover load/get/state/error/events/unload/dependencies/GPU handoff, loadable registry/bundle metadata dependencies merged into runtime dependency graph without blocking on unresolved external manifest-only dependency ids, explicit `insert_loaded*` ready-asset insertion with live replacement rejection, reload, GC memory budget, load groups, cancellation, and feature gates. | Broaden as new consuming-system integrations land. |
| Load scheduler priority/dedup/cancel | Implemented | `LoadScheduler` deduplicates queued requests and pops by priority; `AssetServer` exposes `cancel_load_by_id`, `cancel_load_by_path`, and `cancel_load_group`; cancelled queued work moves to `Cancelled`, emits `AssetEvent::Cancelled`, can be requeued, and leaves no dependency edges; `update_loading` honors the stricter of IO and CPU per-frame job budgets; with `async_loading`, queued requests move through a reusable worker pool, in-flight cancellation remains observable until worker result collection, lifecycle reports expose active/desired workers and dispatch/complete counters, explicit shutdown is idempotent, worker results preserve dependency/GPU handoff semantics, and `parallel` dispatches multiple in-flight workers; `tests/scheduler.rs` covers priority, deduplication, id/path/group cancellation, cancellation before decode, CPU budget limiting, async worker result collection, worker reuse, shutdown/reporting, async in-flight cancellation, and async+parallel multi-worker dispatch. | Broaden only if new async lifecycle states are introduced. |
| Load groups and progress | Implemented | `server.rs` implements `load_group`, `load_group_by_ids`, deterministic `group_state`, `group_progress` with queued/loading/ready/failed/cancelled counters, CPU+GPU resident byte accounting, `is_group_tracked`, tracking-only `release_group`, and queued group cancellation; `tests/load_group.rs` covers mixed states, byte semantics, and release-without-cancel behavior. | Broaden only when async worker groups or bundle chunk progress are added. |
| FileSystem IO and Memory IO | Implemented | `io.rs`; runtime tests use `MemoryAssetIo`; `tests/io.rs` covers `FileSystemAssetIo` read/range, recursive normalized listing, missing-file/read-range/metadata action mapping, non-file/list path and source-message errors, path normalization, and composite metadata/list diagnostics with filesystem layers. | Broaden only when write/watch IO backends are added. |
| Loader registry/context/LoadedAsset | Implemented | `loader.rs`; built-in loaders use context dependencies and GPU upload metadata; `LoadedAsset` can carry `AssetDependencies`-derived handles or direct dependency handles in addition to loader-context dependencies; `tests/loader.rs` covers custom loader registration, path/extension and asset-type fallback resolution, explicit context dependencies, custom asset-provided dependency handles, recursive load/wait/failure behavior, unknown asset-provided dependency ids, subresource registration, type mismatch failures, and loader-not-found diagnostics. | Broaden only when async loader settings or richer subresource materialization are added. |
| Dependency graph | Partial | `dependency.rs`, `server.rs`, `database.rs`; tests cover direct/reverse/report edges, sorted direct dependents, deterministic cycle-safe transitive dependencies/dependents, scoped root reports with direct/transitive dependencies and dependents, stable text/DOT/JSON/HTML whole-graph and scoped report exports, save-to-file helpers, topological order, dependency failure propagation, load-time cycle detection with visible `CyclicDependency`, HTML label escaping/scoped filtering, built-in material import edges, import-generated subresource edges in database reports, and model-generated subresource-to-subresource dependencies including manifest mesh-to-material, mesh-to-skeleton, animation-to-skeleton, typed generated `depends=<kind>:<label>`, generated dependency list and typed syntax diagnostics, duplicate generated dependency metadata diagnostics, generated dependency cycle diagnostics, material target mesh, mesh-to-physics_mesh, physics_mesh-to-mesh, and OBJ mesh-to-material edges preserved through sidecars and bundle manifests. | Richer dependency metadata from production model formats remains. |
| GPU upload queue/handoff | Implemented | `gpu_upload.rs`, `server.rs`; texture/material tests drain and finish renderer handoff; `tests/gpu_upload.rs` covers initial upload failure state/error/event behavior, per-frame upload drain limits with retained pending commands, and dependency failure propagation when a dependency upload fails. | Broaden only when renderer integration owns real GPU resource lifetimes. |
| Reload/hot reload entry points | Partial | `reload_by_id`, `reload_by_path`, explicit `queue_hot_reload_*` APIs, reload queue processing, default direct dependent execution, opt-in transitive dependent execution via `AssetServerConfig::hot_reload_dependency_policy`, read-only direct/transitive dependent reload planning, rollback policy reports and per-type rollback retention overrides for built-in GPU-backed and custom CPU-only asset types, editor/debug policy snapshots with dependency policy, rollback policies, watch backend/status, watches, queue, and last poll report, polling `AssetIo::metadata` watcher, async-notification watch backend lifecycle with start/stop/report/notification counters, per-watch backend status, notification metadata error surfacing, duplicate pending-path debounce, poll batch diagnostics, handle-stable reload, `Reloading` state visibility, old CPU asset retention during reload/upload, failed decode/GPU rollback to previous ready asset, stale error clearing after later success, reload event ordering, and dependent material/scene reloads are covered by tests. | Richer editor UI integration remains. |
| Unload and GC baseline | Implemented | `unload_by_id`, `unload_unused`, `collect_unused`, `collect_until_budget`, resident flag, compact `memory_stats`, rich `memory_info`/`memory_report` with per-asset and per-type CPU/GPU bytes, strong/weak/dependency counts, resident state and last-used frame, Drop-based strong/weak handle counts for server-created handles, dependency counts, resident protection, weak-only unload, cloned/dropped handle count refresh, dependency protection and dependency-count release on dependent unload, global memory-budget oldest-first eviction, per-`AssetTypeId` total/CPU/GPU memory budget eviction, unrelated-type preservation, streaming residency ref-counts, multi-region residency protection, and memory-budget skip/resume behavior for resident streaming assets are covered by tests. | Broaden only when platform-specific memory classes or renderer-owned residency policies are introduced. |
| Metadata and registry lookup | Implemented | `metadata.rs`, `registry.rs`, `database.rs`; path/id mapping, source/cooked hash fields, registry text save/load, `.meta` sidecar save/load, dry-run and write-back metadata migration reports for registry/sidecars, per-file write observability, V1 header/field validation diagnostics, 14-field metadata with labels/importer settings persistence, 12/13-field V1-compatible metadata migration classification and write-back to 14-field text, migration-only V0 registry/sidecar classification and write-back to V1 while preserving generated paths and dependencies, unsupported-version/invalid diagnostics without overwriting bad files, generated metadata sidecar path preservation, rename fallback, stale/missing metadata diagnostics, and dependency metadata restoration are covered by tests. | Broaden only when a new metadata text version is introduced. |
| Error model | Implemented | `error.rs`; `AssetIoError` exposes read/read-range/metadata/list action, path, and source message context, and conversion into `AssetError::Io` preserves runtime-facing action/path text; `AssetServer::insert_loaded*` produces `AlreadyLoaded` when callers try to replace queued/loading/ready/reloading/unloading assets; tests cover missing IO, non-file/read/list IO context, decode, import, cook, bundle parse, unsupported compression, GPU upload, dependency failure, cycle, type mismatch, loader missing, `NotLoaded`, and concrete/displayable `AlreadyLoaded`. | Broaden only when new public error variants or producers are introduced. |

## Built-In Assets And Runtime Loaders

| Capability | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Texture asset and loader | Implemented | `assets/texture.rs`; binary RGBA loader; `TextureImporter` keeps binary pass-through compatibility and converts documented `NGA_TEXTURE_SOURCE_V1` text sources into runtime texture bytes; texture runtime/database tests cover load, import, cook, and runtime load from cooked converted bytes. | Broaden only when production image formats such as PNG/KTX are introduced. |
| Mesh asset and loader | Partial | `assets/mesh.rs`; text mesh loader parses `v x y z` vertices, optional `n x y z` normals, primary `uv u v` texture coordinates, secondary `uv1`/`uv2`... texture-coordinate sets, optional `t x y z w` tangents, optional four-influence `j`/`w` skin joint/weight attributes, and `i a b c` triangles; binary mesh loader parses documented `NGA_MESH_BINARY_V1` little-endian runtime payloads with counts/flags, a secondary-UV mask, optional normals/UV0/UV1+/tangents/skinning blocks, optional `u16` index blocks, exact byte-length validation, finite-value validation, positive normalized skin-weight validation, and index-count/range diagnostics; both paths build deterministic interleaved little-endian binary `MeshVertexBuffer` data and emit renderer-facing `GpuUploadMetadata::Mesh` with vertex layout, vertex byte length, index byte length, and preserved index format, including `Uint16` upload metadata/bytes and GPU byte accounting for decoded `u16` binary meshes; runtime tests cover successful GPU handoff, binary upload metadata, secondary UV/skinning preservation, `u16` binary mesh GPU handoff, binary payload loading, and invalid decode failure events including zero/unnormalized skin weights; `MeshImporter` keeps runtime mesh text pass-through compatibility, validates/pass-throughs `NGA_MESH_BINARY_V1` source payloads, and converts documented `NGA_MESH_SOURCE_V1` text sources into deterministic runtime mesh bytes with optional normals/UVs/secondary UV sets/tangents/skinning attributes and positive normalized skin-weight validation; `MeshCooker` decodes imported text or binary mesh payloads and emits deterministic `NGA_MESH_BINARY_V1` cooked bytes for database, bundle, and runtime loading, including exact referenced-vertex compaction/deduplication plus `u16` index blocks for `Android`/`Ios`/`Web` targets when all indices fit, and database runtime-load coverage verifies cooked Web `u16` meshes hand renderer-facing `Uint16` index metadata and 16-bit index bytes to the GPU upload queue; `ModelImporter` can emit runtime-loadable generated mesh subresources from manifests, optimize generated mesh payloads when `optimize_meshes=true` by dropping unreferenced vertices, deduplicating exact attribute tuples, culling duplicate-index/zero-area triangles, and remapping triangles, generate deterministic `<Label>.LOD1` mesh subresources when `generate_lods=true` by culling degenerate triangles, keeping alternating non-degenerate triangles, and remapping referenced attributes, record generated material dependencies and skeleton skin dependencies for mesh blocks, require skinned mesh blocks to contain skin joint/weight attributes, validate skin joint ranges, declared skin joint/influence limits, duplicate active skin joints, multi-root skin skeleton root-scope metadata, skin root-bone subtree constraints, and normalized positive skin weight totals against the target skeleton, and parse a validated OBJ subset with homogeneous vertex coordinate normalization, parameter-space vertex validation with ignored runtime payload, point/line element validation with ignored runtime payload, free-form curve/surface/connection attribute validation with ignored runtime payload, display/render, texture-map library/use-map, render-object, approximation-technique, and merging-group attribute validation with ignored runtime payload, fan-triangulated `f`/`fo` faces, positive and relative face indices, non-zero texture-coordinate `w` diagnostics, quote-aware trailing `#` inline comments, OBJ/MTL backslash line continuations, expanded `(position, uv, normal)` vertex tuples, and generated tangents when UVs exist. | Broader platform-specific vertex/attribute compression and optimization beyond model mesh deduplication/degenerate culling/generated LOD degenerate culling/LOD generation plus explicit LOD binding metadata, mobile/web exact vertex compaction, and index-width selection remains. |
| Shader asset and loader | Implemented | `assets/shader.rs`; WGSL source loader plus binary SPIR-V and GLSL runtime payload support, explicit `#vertex`/`#fragment`/`#compute` stage labels, lightweight source-structure diagnostics for unmatched delimiters and malformed `@group`/`@binding` declarations, deterministic WGSL reflection for bind groups plus vertex-stage `@location` inputs, GPU upload metadata, `ShaderImporter` keeps runtime shader source pass-through compatibility and converts documented `NGA_SHADER_SOURCE_V1` documents into validated runtime WGSL, GLSL, or SPIR-V source with validated `stage` and `entry` metadata fields, and runtime/database tests cover compute-stage GPU handoff, bind-group reflection, vertex input reflection, empty-source decode/import failures, invalid stage-label diagnostics, malformed source diagnostics, import/cook/runtime load, invalid source diagnostics including invalid shader source `stage`/`entry`, GLSL pass-through without reflection, SPIR-V source-doc parsing, and SPIR-V binary alignment/magic validation. WGSL parse errors now emit parse-location and full naga diagnostics text (`line X, column Y` + source slice context). Runtime tests assert diagnostic richness on parse-error fixtures and database tests check location assertions in import failure messages. |
| Material asset and dependency-producing loader | Implemented | `assets/material.rs`; material tests verify shader/texture dependencies, handles, GPU upload, parsed `emissive`/`alpha_cutoff`/`alpha_mode`/`double_sided`/`depth_write`/`depth_test` fields, invalid syntax, invalid numeric property, invalid alpha-mode, and invalid bool diagnostics. | Add richer schema only when new material workflows require it. |
| Audio clip asset | Partial | `assets/audio.rs` includes `AudioClip`, `AudioSamples`, import settings, and `AudioLoader`; `AudioLoader` parses minimal `NGA_AUDIO_V1` text payloads plus basic RIFF/WAVE PCM16 and IEEE-float32 payloads into CPU-only `AudioClip` values with WAV chunk/header/block-align/sample-count diagnostics; `AudioImporter` keeps `NGA_AUDIO_V1` and `.wav` payload pass-through compatibility, converts documented `NGA_AUDIO_SOURCE_V1` text sources into canonical runtime audio payloads, applies `force_mono` then `normalize` importer settings plus `streaming` overrides, and now accepts Ogg `compression=vorbis`/`compression=opus` by preserving codec payloads for streaming runtime; `AudioCooker` canonicalizes runtime audio/WAV payloads into normalized `NGA_AUDIO_V1` cooked bytes and preserves Ogg payloads through canonicalization checks; runtime tests cover text, WAV PCM16, WAV float32, and Ogg Vorbis/Opus load success/failure, including CPU-only readiness, duration/sample parsing, and invalid text/WAV decode failure events; database tests cover built-in text audio and WAV/Ogg import/cook/runtime load from cooked bytes plus converted source payloads, force-mono/normalize/streaming/compression conversion, and invalid source/settings diagnostics. | Broader encoded audio formats, streaming playback integration, and audio-system ownership remain missing. |
| Animation clip asset | Partial | `assets/animation.rs` includes `AnimationClip`, `AnimationLoader`, text track/keyframe parsing, finite positive duration/ticks validation, finite nonnegative keyframe times, duration-bounded keyframe times, per-track/channel time ordering, CPU-only runtime readiness, invalid decode diagnostics, `ModelImporter` generated animation outputs cook/runtime-load through database bundle tests, standalone generated animation payloads are validated at import time, and skeleton-dependent model animation imports record explicit `skeleton=`/`target_skeleton=` metadata or generic dependencies while validating `bone:<name>`, `node:<name>`, and `node_index:<u32>` tracks plus non-empty/unique-target generated animation track-shape and keyframe timing against generated skeleton dependencies. | Richer animation formats, compression, retargeting, and playback integration remain. |
| Skeleton asset | Partial | `assets/skeleton.rs` includes `Skeleton`, `SkeletonLoader`, text bone hierarchy parsing, duplicate bone-name rejection, optional explicit `bind`/`inverse_bind` matrix payloads, explicit inverse-bind validation against accumulated parent bind poses, CPU-only runtime readiness, invalid decode diagnostics, `ModelImporter` generated skeleton outputs cook/runtime-load through database bundle tests, and standalone generated skeleton payloads are validated at import time. | Richer skeleton formats, retargeting, and broader skinning integration remain. |
| Scene asset and Prefab | Partial | `assets/scene.rs` includes `SceneAsset`, `SceneLoader`, explicit dependency-path parsing, known component asset-reference field parsing for `MeshRenderer`, `SkinnedMeshRenderer`, `AudioSource`, `PhysicsCollider`, `SceneInstance`, and `PrefabInstance`, public component asset-field schema/query/reference helpers, and deduped dependency visitor handles; `assets/prefab.rs` includes `Prefab` and `PrefabLoader`; `tests/runtime.rs` covers scene/prefab dependency readiness, component-field dependency registration, and invalid empty/type-mismatch decode errors; `ecs.rs` exposes non-mutating scene/prefab instantiation plans, stable scene/prefab `SpawnEntity`/`AttachComponent` command export, structured instantiation asset-reference export, generic host instantiation with parent-index-to-host-entity mapping and loaded flag updates, typed host instantiation that materializes known serialized components into handle-based `EcsComponentInstance` values, and sink-based apply helpers for host-side bridging. | Concrete engine ECS crate integration and non-asset gameplay component construction remain missing. |
| Font and PhysicsMesh | Partial | `assets/font.rs` includes `Font`, bitmap `FontLoader`, and invalid decode coverage; `FontImporter` keeps `NGA_FONT_V1` pass-through compatibility and converts documented `NGA_FONT_SOURCE_V1` text sources into deterministic runtime font bytes; `assets/physics_mesh.rs` includes `PhysicsMesh`, text trimesh loader, and invalid decode coverage; `PhysicsMeshImporter` keeps `NGA_PHYSICS_MESH_V1` pass-through compatibility and converts documented `NGA_PHYSICS_MESH_SOURCE_V1` text sources into deterministic runtime physics-mesh bytes; `ModelImporter` can emit generated `physics_mesh` subresources from `NGA_MODEL_V1` manifests, validate them through the physics mesh parser, cook/bundle/runtime-load them, and preserve generated dependencies; `examples/asset_smoke` now converts a loaded `Handle<PhysicsMesh>` into `engine_physics::TriMeshDesc`, creates a real `PhysicsWorld` mesh/collider, and verifies query visibility; database tests cover built-in import/cook/runtime load from cooked bytes plus font/physics source conversion, model-generated physics meshes, and invalid source diagnostics. | Real font formats, glyph atlases, richer physics cooking, and broader consuming text/physics integration remain missing. |

## Editor, Cooking, Bundles, Streaming, ECS

| Capability | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Importer trait/registry/context | Partial | `importer.rs` has public trait, settings, stable settings description and sorted persistence pairs, registry-backed context dependency resolution, generated output with labels/dependencies, registry dispatch, source-side context files for importers with OBJ/MTL context-hash scan invalidation, source conversion importers, root metadata links to import-generated subresources after stable-id reconciliation, generated dependency id remapping across reimport, sanitized model-generated path collision diagnostics, parsed `ModelImportSettings` for generated kind filtering, finite positive mesh/physics-mesh scale, OBJ tangent generation control, generated mesh deduplication/remapping plus degenerate triangle culling, generated LOD mesh expansion with degenerate-triangle culling and copied local dependency metadata, explicit mesh `lod=`/`lods=` generated mesh-to-mesh dependency metadata with unknown-label/non-mesh diagnostics, sorted metadata/settings-hash persistence, and invalid setting diagnostics, material importer shader/texture dependency extraction while ignoring texture sampler/transform/bump/color/channel/boost/blend/color-space/projection/resolution metadata keys and preserving material state fields, mesh/shader/font/physics-mesh source validation/canonicalization, mesh normal/UV/tangent/skinning source validation including positive normalized skin weights, `NGA_MESH_BINARY_V1` binary mesh source validation/pass-through with skin-weight validation, `ModelImporter` inline and multiline-block manifest parsing, material block `mesh=`/`target_mesh=` generated mesh dependency metadata with unknown-label/non-mesh/repeated-field diagnostics, mesh block `material=`/`materials=` generated material dependency metadata with unknown-label/type diagnostics, mesh block `skin=`/`skeleton=` generated skeleton dependency metadata with skeleton decode validation, duplicate-bone and explicit inverse-bind diagnostics, required skin joint/weight attributes, skin joint range validation, positive normalized skin-weight validation, duplicate active skin-joint validation, multi-root skin skeleton root-scope validation, and `max_skin_joints=`/`skin_joint_limit=` plus `max_skin_influences=`/`skin_influence_limit=` metadata validation for missing skin targets, zero/repeated values, over-four influence limits, skeletons exceeding the declared joint limit, and vertices exceeding the declared influence limit, animation block `skeleton=`/`target_skeleton=` generated skeleton dependency metadata with unknown-label/non-skeleton/repeated-field diagnostics, standalone generated material/skeleton/animation payload validation, skeleton-dependent animation target validation for generated animation blocks including `track=bone:<name>`, `track=node:<name>`, and `track=node_index:<u32>`, generated animation non-empty/unique-target track-shape and keyframe time validation, OBJ subset parsing with case-insensitive directive matching and quote-aware trailing `#` inline comment stripping that preserves `#` inside single/double quoted OBJ/MTL tokens, OBJ/MTL backslash line continuation merging, OBJ/MTL single/double quoted material library/name/texture path parsing with unterminated-quote diagnostics and quoted `#` name preservation, `v`/`vt`/`vn` validation, `vp` parameter-space vertex validation, `p`/`l` point/line element validation, free-form curve/surface/connection attribute validation, display/render, texture-map library/use-map, render-object, approximation-technique, and merging-group attribute validation, non-zero `vt` w rejection, face tuple reference validation, runtime tuple expansion, tangent generation from OBJ UVs, relative `mtllib` provenance comments plus resolved MTL `newmtl`/`Kd`/`Ka`/`Ks`/`Ke`/`Tf` `xyz` color conversion to canonical material RGB fields, visible unsupported `spectral` color diagnostics, `d`/`Tr` unit-range diagnostics plus `Ns` range diagnostics, `Pr`/`roughness`, `Pm`/`metallic`/`metalness`, and `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, duplicate `newmtl` material diagnostics within and across loaded MTL sources, loaded-MTL `usemtl` validation, persistent OBJ `usemtl` binding across `o`/`g` labels, OBJ per-material mesh splitting, and registry-backed case-insensitive common alpha/opacity texture aliases (`map_opacity`/`map_alpha`/`map_transparency`), descriptive PBR texture aliases (`map_diffuse`/`map_albedo`/`map_basecolor`/`map_base_color`/`map_specular`/`map_specular_color`/`map_ambient`/`map_ambient_color`/`map_emissive`/`map_emission`/`map_emissive_color`/`map_emission_color`/`map_transmission_filter`/`map_transmission_color`/`map_Kt`/`map_transmittance`/`map_transmittance_color`/`map_ior`/`map_roughness`/`map_metallic`/`map_metalness`/`map_clear_coat`/`map_clearcoatroughness`/`map_clear_coat_roughness`/`map_anisotropyrotation`/`map_normalgl`/`map_normaldx`), descriptive PBR scalar/color aliases (`diffuse`/`albedo`/`basecolor`/`base_color`/`ambient`/`ambient_color`/`specular`/`specular_color`/`emissive`/`emission`/`emissive_color`/`emission_color`/`opacity`/`alpha`/`transparency`/`transmission_filter`/`transmission_color`/`transmittance`/`transmittance_color`/`metalness`/`clear_coat`/`clearcoatroughness`/`clear_coat_roughness`/`anisotropyrotation`), packed PBR long-name texture aliases (`map_occlusionroughnessmetallic`/`map_occlusion_roughness_metallic`/`map_metallic_roughness`/`map_roughnessmetallic`/`map_roughness_metallic`), plus common and legacy MTL texture map dependencies including `map_Ns` roughness maps, `map_Ka`/`map_AO`/`map_Occlusion`/`map_ambient_occlusion` occlusion maps, and `disp`/`decal`/`refl` displacement/decal/reflection maps with deterministic case-insensitive map option parsing/path extraction plus `-clamp` sampler-address with case-insensitive bool values, `-o`/`-s`/`-t`/`-bm` transform/bump payload preservation, `-mm`/`-imfchan`/`-boost` color-remap/channel/boost payload preservation with case-insensitive source-channel values, and `-blendu`/`-blendv`/`-cc` case-insensitive bool payload preservation plus `-colorspace`/`-type`/`-texres` color-space/projection/resolution payload preservation with case-insensitive texture-map directive matching, texture option names, and projection values when sidecar source files are available, model-generated mesh/material/skeleton/animation/physics_mesh subresources, sanitized generated path collision diagnostics, local generated `depends=` metadata including duplicate generated dependency validation, generated dependency cycle diagnostics, generated dependency list syntax validation and inline payload syntax validation, typed generated dependency target validation, generated physics mesh dependencies, physics_mesh target mesh dependencies, and OBJ `usemtl` generated mesh-to-material metadata, and database tests for dependency-producing importers, generated subresource metadata, missing importer errors, missing material/model dependency errors, and importer validation failure context. | Production model format parsing remains incomplete beyond the documented OBJ subset. |
| AssetDatabase | Partial | `database.rs`; tests cover `scan`, `scan_with_metadata`, added/changed/unchanged/removed incremental scan buckets including OBJ model source hashes that fold in readable MTL context files, sidecar save/load, importer settings persistence through registry/sidecars/incremental scan, built-in material dependency persistence through registry/sidecars/bundles/dependency reports, visible missing material/model dependency errors, material import/cook/runtime load from cooked output with dependencies, emissive/alpha/depth state fields, invalid alpha-mode diagnostics, and sampler/transform/bump/color/channel/boost/blend/color-space/projection/resolution metadata, material source validation/canonicalization to stable runtime bytes, model-generated subresource labels/dependencies/bytes through sidecars/scoped reports/bundles/runtime preload, generated path collision diagnostics, model import settings for generated kind filters including `import_physics_meshes`, finite positive mesh/physics-mesh scale, OBJ tangent generation control, generated mesh optimization/deduplication with degenerate-triangle culling, generated LOD mesh metadata/bytes/dependencies, degenerate-triangle culling, and invalid/all-degenerate LOD-input diagnostics, explicit mesh `lod=`/`lods=` binding dependencies preserved in sidecars/bundle manifests/runtime dependency graph with unknown/non-mesh target diagnostics, sorted metadata/settings-hash persistence, and invalid setting diagnostics, model block manifests with generated-to-generated dependencies remapped across reimport and preserved in sidecars/bundle manifests, manifest mesh-to-material dependencies preserved in sidecars/bundle manifests/runtime preload with invalid material-binding diagnostics, mesh-to-physics_mesh dependencies preserved in sidecars/bundle manifests with invalid physics-mesh binding diagnostics, physics_mesh-to-mesh target dependencies preserved in sidecars/bundle manifests/runtime dependency graph with invalid target diagnostics, manifest material target mesh dependencies preserved in sidecars/bundle manifests with invalid target diagnostics, manifest typed generated dependencies including physics_mesh dependencies plus explicit mesh physics_mesh bindings and animation-to-skeleton `skeleton=`/`target_skeleton=` dependencies preserved with invalid target diagnostics, manifest physics_mesh generated payloads preserved through sidecars/bundle manifests/runtime preload with invalid payload diagnostics, manifest skin influence limit metadata validation with no-skin/repeated/zero/over-four/too-many-influence diagnostics, OBJ model sources producing generated mesh/material bytes with quote-aware inline comment stripping and OBJ/MTL backslash line continuations, homogeneous vertex coordinates, parameter-space vertex validation, point/line element validation, free-form curve/surface/connection attribute validation, display/render, texture-map library/use-map, render-object, approximation-technique, and merging-group attribute validation, `f`/`fo` face parsing, positive and relative face indices, smoothing-group generated normals, case-insensitive `s off` flat normals, normals/UVs/tangents, `mtllib` material provenance comments, resolved sidecar case-insensitive MTL base color/emissive/ambient/specular/transmission/transmittance colors including descriptive scalar/color aliases and `xyz` to linear-sRGB conversion and visible unsupported `spectral` color diagnostics, alpha/metallic/roughness properties including `d`/`Tr` unit-range diagnostics, `Ns` range diagnostics, `Pr`/`roughness`, `Pm`/`metallic`, and `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, and transparent alpha-to-blend material state, `map_d`/`map_Tr`/`map_opacity`/`map_alpha`/`map_transparency` alpha texture-to-`alpha_mode=blend` material state, `d -halo` option case-insensitive dissolve-halo alpha/custom material state mapping, `sharpness` custom material property, `map_bump`/`map_Kn`/`map_normal` normal map alias mapping, `map_aat` texture antialias custom bool preservation with case-insensitive bool values, MTL `-colorspace` texture color-space metadata preservation, MTL `-imfchan` source-channel case-insensitive canonicalization including `a`/`alpha`, MTL `-type` projection case-insensitive canonicalization, and `map_Tf`/`map_Kt`/`map_Ni` transmission/IOR texture dependency mapping, plus `map_Ks -imfchan blue` specular source-channel preservation and `map_Ns -imfchan green` roughness source-channel preservation, Exocortex PBR scalar/texture material extension mapping with `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, common PBR scalar/color alias mapping with `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, common PBR texture scalar/color alias mapping, `map_RMA`/`map_ORM` packed PBR texture channel preservation, `map_MR`/`map_metallicroughness`/`map_metallic_roughness`/`map_roughnessmetallic`/`map_roughness_metallic`/`map_MRA`/`map_ARM` packed PBR texture alias preservation, `map_AO`/`map_Occlusion`/`map_ambient_occlusion` ambient-occlusion texture alias preservation, `Ns` range diagnostics, `Pr`/`roughness`, `Pm`/`metallic`, and `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, plus `Ka`/`Ks`/`Tf`/`Ni`/`illum` typed custom material properties and `Ni`/`ior` positive range diagnostics plus `illum` range diagnostics, duplicate MTL material-name diagnostics, loaded-MTL `usemtl` validation, persistent OBJ `usemtl` binding across `o`/`g` labels, per-material OBJ mesh splitting, MTL-only change detection through context source hashes, and common alpha/opacity texture aliases (`map_opacity`/`map_alpha`/`map_transparency`) plus common and legacy texture map dependencies including `map_Ns` roughness maps and `disp`/`decal`/`refl` displacement/decal/reflection maps with deterministic case-insensitive map option parsing/path extraction plus `-clamp` sampler-address with case-insensitive bool values, `-o`/`-s`/`-t`/`-bm` transform/bump payload preservation, `-mm`/`-imfchan`/`-boost` color-remap/channel/boost payload preservation with case-insensitive source-channel values, and `-blendu`/`-blendv`/`-cc` case-insensitive bool payload preservation plus `-colorspace`/`-type`/`-texres` color-space/projection/resolution payload preservation with case-insensitive texture-map directive matching, texture option names, and projection values, generated mesh-to-material bundle dependencies, and invalid OBJ vertex/UV/normal/material-library/material-texture diagnostics, font/physics mesh import/cook/runtime load from cooked output, texture text-source import conversion to runtime bytes, audio text-source import conversion to canonical runtime bytes with force-mono/normalize setting persistence and invalid setting diagnostics, mesh/shader/font/physics-mesh text-source import conversion to deterministic runtime bytes, mesh normal/UV/tangent source conversion and invalid attribute-count diagnostics, rename fallback, stable path/id, registry save/load, missing importer error, importer failure context, importer-produced dependencies/generated metadata, same-id generated byte persistence for converted primary assets, root-to-generated dependency edges after stable-id reconciliation, dry-run/write-back metadata migration including V0-to-V1 registry/sidecar transforms, missing/failing cooker context, builtin cooker registration, cooked file output, missing metadata/source cook errors, runtime load from cooked output, `cook_asset`, database bundle build from cooked files, manifest dependency preservation, runtime mount/preload of database-built bundles, and whole/scoped dependency report text/DOT/JSON/HTML file export with escaped path/type labels. | Production model format parsing remains incomplete beyond the documented OBJ subset. |
| Cooker trait/context/output | Partial | `cooker.rs` registry dispatch, source-byte `CookContext`, pass-through scene/prefab/font/physics-mesh cookers, canonical texture/material/shader/audio/skeleton/animation cookers, and `MeshCooker` conversion from imported text or binary mesh payloads into deterministic `NGA_MESH_BINARY_V1` cooked bytes with desktop `u32` indices plus `Android`/`Ios`/`Web` exact referenced-vertex compaction/deduplication and `u16` index blocks when possible; database tests verify type dispatch, cooked output files, hash/path metadata, skeleton/animation source-document cooker canonicalization, binary mesh cooked output from direct/model/OBJ/skinned/generated mesh sources, platform-aware Web/mobile `u16` index and vertex-compacted cooked output, runtime Web cooked `u16` mesh upload metadata/bytes preservation, runtime texture/mesh/shader/material/audio/skeleton/animation/font/physics-mesh load from cooked output, missing cooker context, and cooker validation failure context. | Real platform conversion/optimization remains incomplete beyond canonical mesh binary cooking, mobile/web vertex compaction, and index-width selection. |
| Bundle manifest/builder | Partial | `bundle.rs` manifest, builder, V2 chunk layout metadata, V1 manifest compatibility, compression codec reports, codec-aware unsupported writer/reader diagnostics, uncompressed writer/reader, real RLE codec-backed writer/reader execution, default-feature Zstd writer/reader execution through a pure Rust backend, chunk decode corruption diagnostics, `BundleChunkPartitionPolicy::MaxUncompressedBytes`, `BundleChunkLoadingPolicy::OnDemandCached` and `OnDemandCachedLimited`, `BundleChunkCacheStats`, explicit chunk/path prefetch, LRU cache eviction stats, `BundleChunkReadReport` range/cache observability, write-to-file convenience, dependency lookup, missing-entry errors, `MountedBundleRegistry` save/load text snapshots, `AssetPackageRegistry` V3 save/load text snapshots with V1/V2 migration defaults, enabled state/source kind/priority/payload path/package version/min-runtime/package-dependency/embedded manifest metadata, package manifest validation, package dependency compatibility validation, conflict reports, semantic asset override reports, scene/prefab package override reports, update reports, artifact install/remove reports, artifact availability reports, and composite IO construction from installed package payload bytes; `AssetServer` mount/unmount/preload registry plus mounted-registry and package-registry save/load/restore/preview/transactional-activate/artifact-verify/artifact-activate helpers; `AssetDatabaseBundleBuild` builds uncompressed, RLE-compressed, and Zstd-compressed single/multi-chunk bundle bytes from cooked metadata/files; streaming regions can be registered from mounted bundle manifests or explicit subsets; tests cover round trip, V2 chunk records, legacy V1-to-single-chunk mapping, codec report availability, zstd-disabled diagnostics, RLE and Zstd compression round trips/range reports, corrupted RLE/Zstd diagnostics, deterministic max-size partitioning, lazy on-demand cache misses/hits/stats, invalid chunk policy errors, invalid bounded-cache policy errors, prefetch by chunk/path, LRU eviction reports/stats, invalid mount, file write/readback, persisted mounted-registry remount, package ordering/enabled-state persistence, package artifact install/build/remove, missing/mismatched package artifact diagnostics, package artifact activation rollback, package conflict reporting, asset override reporting for id/type/hash/dependency/provider changes, V1/V2 package registry compatibility, package version downgrade/runtime-too-old diagnostics, missing/too-old/too-new package dependency diagnostics, default type-mismatch activation rejection, strict asset compatibility activation rejection, transactional activation rollback, invalid package metadata/dependency metadata, payload-manifest mismatch, package registry runtime restore/reload preserving ready state, preload progress, unmount behavior, dependency preservation, missing cooked inputs, stale hashes, runtime preload of database-built bundles, compressed multi-chunk database-built bundle runtime preload, and compressed multi-chunk bundle-backed streaming preload behavior. | Broader production codec tuning and packaging acceptance coverage remain. |
| Composite/Bundle IO | Implemented | `BundleAssetIo` and `CompositeAssetIo`; tests cover read/range/metadata/list, compressed-bundle range reads through `BundleAssetIo::read_range`, `BundleAssetIo` on-demand chunk cache reports, missing entry mapping, default override precedence, named source/mod/patch/base layer diagnostics, list de-duplication, package-registry-driven composite IO construction with manifest validation, installed package artifact composite IO construction, and `AssetServer` load from bundle IO. | Broaden only if package artifact discovery needs non-filesystem stores. |
| Streaming regions | Partial | `streaming.rs` and `server.rs`; tests cover register-by-path, mounted-bundle manifest registration, RLE-compressed multi-chunk mounted-bundle preload through on-demand cached bundle IO, explicit bundle subset registration with id validation, priority-aware preload, runtime region priority updates with previous-value return and queued request reprioritization, real progress/state, resident flags, region unload, state preservation while resident, shared asset residency counts across multiple regions, resident-region removal, and memory-budget interaction. | Richer streaming policies remain. |
| Hot reload system | Partial | `hot_reload.rs` includes change/watch/report/dependency-plan/rollback-policy/policy-snapshot/watch-status/async-watch-report types plus `AssetServer` queue/process/watch/poll/plan/rollback-report/policy-report APIs, configurable direct/transitive dependent execution, per-type rollback retention overrides, polling metadata watches, and async notification watches with start/stop lifecycle, pending/received/delivered/dropped counters, and notification metadata error reporting; tests cover successful reload, decode rollback, GPU rollback, custom CPU-only rollback diagnostics, rollback opt-out failure behavior, editor/debug policy snapshot visibility including polling and async backend status, queued watch state, last path error, direct dependent reload, transitive nested dependent reload, direct and transitive dependent planning without state mutation, watcher change detection, duplicate pending-path debounce, async notification delivery without polling unnotified paths, stop/drop behavior, and metadata error diagnostics. | Richer editor UI integration remains. |
| ECS integration | Partial | `ecs.rs`; `assets/scene.rs`; tests cover documented system order descriptors, stage ordering helpers, component asset handle exposure, mesh/material readiness through `AssetServer`, scene/prefab instantiation readiness, non-mutating `SceneInstantiationPlan`/`PrefabInstantiationPlan`, stable `SceneInstantiationCommand`/`PrefabInstantiationCommand` export, direct `SceneInstanceComponent::instantiate` / `PrefabInstanceComponent::instantiate` helpers that emit command streams to an `InstantiationSink`, public serialized component asset-field schema/query APIs, structured scene/prefab instantiation asset-reference export with entity/component/field/path/type metadata, `HostInstantiationSink` generic host adapter with parent host-entity mapping, `HostInstantiationReport`, `HostInstantiationError::MissingParent`, component `loaded` lifecycle update on successful host instantiation, failure preserving unloaded state, `TypedHostInstantiationSink`, `EcsComponentInstance`, `materialize_serialized_component`, typed known-component construction with server-tracked handles, typed host instantiation for scene/prefab, typed parent/materialization preflight that avoids partial host spawn/attach on component errors, unknown component passthrough, materialization error paths, and audio/physics readiness tests with non-owning lifetime behavior. | Concrete renderer backend/audio/physics/ECS crate adapters and non-asset gameplay component construction remain. |
| Built-in importers | Partial | `ShaderImporter` pass-throughs runtime shader source and converts documented `NGA_SHADER_SOURCE_V1` documents to validated runtime WGSL, GLSL, or SPIR-V while validating `stage` and `entry` metadata fields; `AnimationImporter` and `SkeletonImporter` parse documented `NGA_ANIMATION_SOURCE_V1` / `NGA_SKELETON_SOURCE_V1` sources into runtime assets; `SceneImporter` and `PrefabImporter` parse documented `NGA_SCENE_V1` / `NGA_PREFAB_V1` sources into runtime assets while registering declared dependency paths plus known scene/prefab component asset-reference fields into metadata dependencies; `MeshImporter` pass-throughs runtime mesh text, validates/pass-throughs documented `NGA_MESH_BINARY_V1` binary mesh payloads with skin-weight validation, and converts documented `NGA_MESH_SOURCE_V1` text sources to deterministic runtime mesh bytes with optional normals/UVs/secondary UV sets/tangents/four-influence skin joints and positive normalized weights, which runtime loading packs into binary vertex buffers and mesh upload metadata; `TextureImporter` pass-throughs runtime texture bytes and converts documented `NGA_TEXTURE_SOURCE_V1` text sources to runtime texture bytes; `AudioImporter` pass-throughs runtime audio/WAV bytes, converts documented `NGA_AUDIO_SOURCE_V1` text sources to canonical runtime audio bytes, and honors `force_mono`/`normalize` conversion settings with invalid boolean diagnostics; `FontImporter` pass-throughs runtime bitmap font bytes and converts documented `NGA_FONT_SOURCE_V1` text sources to deterministic runtime font bytes; `PhysicsMeshImporter` pass-throughs runtime physics-mesh payloads and converts documented `NGA_PHYSICS_MESH_SOURCE_V1` text sources to deterministic runtime physics-mesh bytes; `MaterialImporter` validates/canonicalizes runtime material text while parsing `shader` and texture asset references into required registry-backed dependencies without treating sampler/transform/bump/color/channel/boost/blend/color-space/projection/resolution metadata or material state fields or typed custom properties as assets, and preserves typed custom values and `alpha` texture source-channel metadata in canonical runtime bytes; `ModelImporter` parses documented inline and multiline-block `NGA_MODEL_V1` manifests plus `.obj`/`NGA_MODEL_OBJ_V1` OBJ subset sources into generated mesh/material/skeleton/animation/physics_mesh metadata, honors parsed `ModelImportSettings` for generated kind filtering, finite positive mesh/physics-mesh scale, OBJ tangent generation control, generated mesh optimization/deduplication with degenerate triangle culling, and deterministic generated LOD meshes with degenerate-triangle culling, emits runtime-loadable mesh/material/physics_mesh bytes, external material dependencies, validates standalone generated material/skeleton/animation payloads, local generated `depends=` dependencies including duplicate-label diagnostics and generated dependency list syntax diagnostics (typed dependency syntax, empty comma-entry diagnostics, and inline payload syntax diagnostics), plus typed `mesh:`/`material:`/`skeleton:`/`animation:`/`physics_mesh:` target validation, material block generated mesh target metadata with unknown-label/non-mesh/repeated-field diagnostics, mesh block generated material dependencies with unknown-label/type diagnostics, mesh block generated LOD bindings through `lod=`/`lods=` with unknown-label/non-mesh diagnostics, mesh block generated physics mesh bindings with unknown-label/non-physics-mesh diagnostics, physics_mesh block generated mesh target metadata with unknown-label/non-mesh/repeated-field diagnostics, mesh block generated skeleton skin dependencies with skeleton decode validation, duplicate-bone and explicit inverse-bind diagnostics, required skin joint/weight attributes, joint-range validation, positive normalized skin-weight validation, duplicate active skin-joint validation, multi-root skin skeleton root-scope validation, declared skin joint and influence limit validation, and skin root-bone subtree validation, typed generated dependency metadata plus generated animation-to-skeleton `skeleton=`/`target_skeleton=` metadata with target validation for bone-name, node-name, and node-index tracks plus generated animation non-empty/unique-target track-shape and keyframe timing, sanitized generated path collision diagnostics, OBJ homogeneous vertex coordinate normalization, parameter-space vertex validation with ignored runtime payload, point/line element validation with ignored runtime payload, free-form curve/surface/connection attribute validation with ignored runtime payload, display/render, texture-map library/use-map, render-object, approximation-technique, and merging-group attribute validation with ignored runtime payload, quote-aware trailing `#` inline comment stripping that preserves `#` inside single/double quoted OBJ/MTL tokens, `f`/`fo` face parsing, positive/relative face index validation, non-zero `vt` w rejection, OBJ generated mesh-to-material dependencies, UV/normal face tuple validation, runtime tuple expansion, generated tangents from OBJ UVs, per-material mesh splitting for multi-material OBJ objects, loaded-MTL `usemtl` validation, persistent OBJ `usemtl` binding across `o`/`g` labels, `mtllib` provenance comments, resolved sidecar case-insensitive MTL `Kd`/`Ka`/`Ks`/`Ke`/`Tf` direct RGB and `xyz` color conversion into base/emissive/custom material fields with visible unsupported `spectral` diagnostics, alpha/metallic/roughness properties including `d`/`Tr` unit-range diagnostics, `Ns` range diagnostics, `Pr`/`roughness`, `Pm`/`metallic`, and `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, and transparent alpha-to-blend material state, `map_d`/`map_Tr`/`map_opacity`/`map_alpha`/`map_transparency` alpha texture-to-`alpha_mode=blend` material state, `d -halo` option case-insensitive dissolve-halo alpha/custom material state mapping, `sharpness` custom material property, `map_bump`/`map_Kn`/`map_normal` normal map alias mapping, `map_aat` texture antialias custom bool preservation with case-insensitive bool values, MTL `-colorspace` texture color-space metadata preservation, MTL `-imfchan` source-channel case-insensitive canonicalization including `a`/`alpha`, MTL `-type` projection case-insensitive canonicalization, and `map_Tf`/`map_Kt`/`map_Ni` transmission/IOR texture dependency mapping, plus `map_Ks -imfchan blue` specular source-channel preservation and `map_Ns -imfchan green` roughness source-channel preservation, Exocortex PBR scalar/texture material extension mapping with `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, common PBR scalar/color alias mapping with `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, common PBR texture scalar/color alias mapping, `map_RMA`/`map_ORM` packed PBR texture channel preservation, `map_MR`/`map_metallicroughness`/`map_metallic_roughness`/`map_roughnessmetallic`/`map_roughness_metallic`/`map_MRA`/`map_ARM` packed PBR texture alias preservation, `map_AO`/`map_Occlusion`/`map_ambient_occlusion` ambient-occlusion texture alias preservation, `Ns` range diagnostics, `Pr`/`roughness`, `Pm`/`metallic`, and `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, plus `Ka`/`Ks`/`Tf`/`Ni`/`illum` typed custom material properties and `Ni`/`ior` positive range diagnostics plus `illum` range diagnostics, duplicate MTL material-name diagnostics, registry-backed case-insensitive common alpha/opacity texture aliases (`map_opacity`/`map_alpha`/`map_transparency`), descriptive PBR texture aliases (`map_diffuse`/`map_albedo`/`map_basecolor`/`map_base_color`/`map_specular`/`map_specular_color`/`map_ambient`/`map_ambient_color`/`map_emissive`/`map_emission`/`map_emissive_color`/`map_emission_color`/`map_transmission_filter`/`map_transmission_color`/`map_Kt`/`map_transmittance`/`map_transmittance_color`/`map_ior`/`map_roughness`/`map_metallic`/`map_metalness`/`map_clear_coat`/`map_clearcoatroughness`/`map_clear_coat_roughness`/`map_anisotropyrotation`/`map_normalgl`/`map_normaldx`), descriptive PBR scalar/color aliases (`diffuse`/`albedo`/`basecolor`/`base_color`/`ambient`/`ambient_color`/`specular`/`specular_color`/`emissive`/`emission`/`emissive_color`/`emission_color`/`opacity`/`alpha`/`transparency`/`transmission_filter`/`transmission_color`/`transmittance`/`transmittance_color`/`metalness`/`clear_coat`/`clearcoatroughness`/`clear_coat_roughness`/`anisotropyrotation`), packed PBR long-name texture aliases (`map_occlusionroughnessmetallic`/`map_occlusion_roughness_metallic`/`map_metallic_roughness`/`map_roughnessmetallic`/`map_roughness_metallic`), plus common and legacy MTL texture map dependencies including `map_Ns` roughness maps, `map_Ka`/`map_AO`/`map_Occlusion`/`map_ambient_occlusion` occlusion maps, and `disp`/`decal`/`refl` displacement/decal/reflection maps with deterministic case-insensitive map option parsing/path extraction plus `-clamp` sampler-address with case-insensitive bool values, `-o`/`-s`/`-t`/`-bm` transform/bump payload preservation, `-mm`/`-imfchan`/`-boost` color-remap/channel/boost payload preservation with case-insensitive source-channel values, `-blendu`/`-blendv`/`-cc` case-insensitive bool payload preservation plus `-colorspace`/`-type`/`-texres` color-space/projection/resolution payload preservation with case-insensitive texture-map directive matching, texture option names, and projection values, WGSL source-document and runtime WGSL loading are now validated by `naga` with `WGSL compile failed` / `WGSL validation failed` diagnostics, and root-to-generated subresource dependency edges; custom importer tests verify the importer pipeline can persist dependencies and generated metadata. | Production model format parsing remains incomplete beyond the documented OBJ subset. |
| Built-in cookers | Partial | `TextureCooker`, `MaterialCooker`, `ShaderCooker`, `AudioCooker`, `SceneCooker`, `PrefabCooker`, `SkeletonCooker`, `AnimationCooker`, `FontCooker`, and `PhysicsMeshCooker` produce cooked bytes; `TextureCooker`, `MaterialCooker`, `ShaderCooker`, `AudioCooker`, `SkeletonCooker`, and `AnimationCooker` canonicalize runtime texture/material/shader/audio/WAV/skeleton/animation payloads into normalized cooked bytes, with `SkeletonCooker` and `AnimationCooker` also converting documented `NGA_SKELETON_SOURCE_V1` / `NGA_ANIMATION_SOURCE_V1` source documents into runtime bytes using `VersionHash(2)`, while `MeshCooker` validates/decodes imported text or binary mesh payloads and writes deterministic `NGA_MESH_BINARY_V1` cooked bytes with `VersionHash(4)`, preserving desktop `u32` indices while compacting/deduplicating exact referenced vertices and emitting `u16` index blocks for `Android`/`Ios`/`Web` targets when possible; runtime loading now preserves cooked `u16` mesh index format through `Mesh::index_format`, `GpuUploadMetadata::Mesh`, upload bytes, and GPU byte accounting; all update cooked metadata, and texture, mesh, shader, material, audio, skeleton, animation, font, and physics mesh cooked bytes are runtime-load verified. | Real platform-specific conversion, compression, and validation remain beyond canonical mesh binary cooking, mobile/web vertex compaction, and index-width selection. |
| Dependency visualization/audit hooks | Implemented | `DependencyGraphReport`/`DependencyScopeReport`/`DependencyEdge`, stable whole-graph and scoped `to_text`, DOT, JSON, and self-contained HTML exports, save-to-file helpers, optional HTML labels with escaping, `AssetServer::*dependency_report*`, `AssetServer::*scoped_dependency_report*`, `AssetDatabase::*dependency_report*`, and `AssetDatabase::*scoped_dependency_report*` expose graph audit data with metadata path/type labels where available; tests verify deterministic edge/node output, scoped root dependencies/dependents, missing-root errors, JSON ordering/shape, HTML shape, escaped labels, scoped HTML filtering, and file export. | Broaden only if editor tooling needs interactive graph actions beyond the saved/pane-ready report. |
| DLC/patch/mod override behavior | Partial | `CompositeAssetIo` supports named layers with `AssetIoLayerKind::{Source, Mod, Patch, BaseBundle}`, first-layer-wins resolution, `resolve`, `read_with_diagnostics`, `metadata_with_diagnostics`, and `list_with_diagnostics`; `AssetPackageRegistry` persists package order, enabled state, layer kind, bundle id, payload path, package version, minimum runtime version, package dependency constraints, and embedded manifests; `AssetPackageArtifactStore` installs bundle bytes to package artifacts, replaces matching package records, verifies artifact availability and manifest matches, builds `CompositeAssetIo` from installed artifacts, and removes packages with optional artifact deletion; package update APIs report path conflicts with winning and shadowed packages before reads, expose semantic asset override diagnostics for overridden asset ids, type/hash changes, dependency changes, and dependency-provider changes, including scene/prefab package override coverage, reject runtime-too-old, package-version-downgrade, missing package dependency, dependency-too-old, dependency-too-new, and default asset-type-mismatch activations, support explicit downgrade allowance and configurable strict asset compatibility, and transactionally activate compatible registries or artifact-backed registries into `AssetServer` without disrupting ready assets; tests map source/mod/patch/base bundle shadowing, de-duplicated listing behavior, package persistence/order/enabled-state, artifact install/remove/build, missing and mismatched artifact diagnostics, conflict reporting, semantic asset override reporting/strict rejection, scene/prefab semantic override reporting, V1/V2 package registry migration defaults, package dependency compatibility diagnostics, version compatibility diagnostics, transactional activation rollback, invalid metadata/payload mismatch, and runtime package registry reload disabling a package. | Broader mod/package acceptance coverage remains. |

## Examples And Tests

| Capability | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Runtime smoke example | Implemented | `examples/asset_smoke` builds with `cargo check -p asset_smoke` and `cargo test -p asset_smoke` passes public-API smoke tests covering renderer/audio/physics-facing readiness, renderer-system consumption of loaded `Mesh`/`Material`/`Texture` assets through `engine_render::RenderScene` and `RenderQueue` scene/queue statistics, renderer facade resource-lifecycle consumption through `engine_renderer::Renderer::new_headless` mesh/texture/material resource creation plus `resource_status`/`mesh_info`/`texture_info`/`memory_stats`, physics-system consumption of a loaded `PhysicsMesh` through `engine_physics::PhysicsWorld` mesh/collider/query APIs, scene/prefab instantiation command export, raw sink application with observable command traces, typed host scene/prefab instantiation through `TypedHostInstantiationSink` with host entity parent mapping, typed `MeshRenderer` materialization, unknown `Transform` passthrough, loaded-state lifecycle, non-owning component handle exposure, material dependency readiness, load-group progress/state, event cursor observation, an editor-to-runtime path that scans/imports/cooks simple texture/mesh/material/scene/prefab source assets with scene/prefab component-field dependencies, builds a bundle, mounts it, and loads the produced material through `AssetServer`, plus model import generated mesh/material/skeleton/animation/physics_mesh subresources cooked, bundled, mounted, and loaded through runtime APIs. | Add broader end-to-end examples only when real audio integration and deeper physics cooking are available. |
| Focused runtime/editor tests | Implemented | `Asset/engine_asset/tests/runtime.rs`, `tests/database.rs`, `tests/bundle.rs`, `tests/streaming.rs`, `tests/hot_reload.rs`, `tests/gc.rs`, `tests/dependency.rs`, `tests/ecs.rs`, `tests/error_model.rs`, `tests/feature_flags.rs`, `tests/scheduler.rs`, `tests/load_group.rs`, `tests/gpu_upload.rs`, `tests/io.rs`, `tests/storage.rs`, `tests/loader.rs`, `tests/serde.rs`; `cargo test -p engine_asset --quiet` passes 376 tests across the default engine_asset test binaries, `cargo test -p engine_asset --test feature_flags` and `cargo test -p engine_asset --no-default-features --test feature_flags` each pass 9 gate-aware feature tests, individual `filesystem`, `bundle`, `hot_reload`, `streaming`, `editor`, `importers`, `cookers`, `texture_importer`, `texture_cooker`, `audio_importer`, `material_importer`, `model_importer`, and `shader_importer` no-default feature-flag test builds pass, with importer/cooker gate tests now also exercising scene/prefab and animation/skeleton registration paths, `cargo test -p engine_asset --test error_model` passes 5 error-model tests including concrete `AlreadyLoaded` insertion rejection, `cargo test -p engine_asset --test serde` passes 1 default serde roundtrip test, `cargo test -p engine_asset --no-default-features --features serde --test serde` passes the same serialization-facing roundtrip under explicit serde-only features, `cargo test -p engine_asset --test io` passes 3 IO context tests, `cargo test -p engine_asset --test runtime missing_path_fails_with_visible_state_error_and_event` covers runtime-facing IO context, `cargo test -p engine_asset --test runtime mesh` passes 11 mesh/physics-mesh runtime tests including binary mesh upload metadata, `u16` binary mesh upload metadata/bytes, secondary UV/skinning preservation, `NGA_MESH_BINARY_V1` runtime loading, invalid binary payload diagnostics, and mesh tangent/skin-count/skin-weight decode diagnostics, `cargo test -p engine_asset --test runtime skeleton` passes 3 skeleton tests including explicit bind/inverse-bind matrix parsing plus invalid parent/matrix/duplicate-bone/explicit-inverse diagnostics, `cargo test -p engine_asset --test runtime material` passes 15 material runtime tests including texture metadata/state-field success, typed custom material properties, and invalid sampler/transform/channel/color-space/projection/resolution/alpha-mode/custom-property diagnostics, runtime animation tests cover empty/duplicate track-shape, finite-number, and keyframe timing diagnostics, runtime shader tests cover bind-group reflection, vertex input reflection, malformed source diagnostics, GLSL text pass-through without reflection, and SPIR-V binary loading/alignment/magic validation, scene/prefab runtime tests cover known component asset-reference fields, deduplicated direct dependencies, empty asset field diagnostics, and type-mismatch diagnostics, `cargo test -p engine_asset --test dependency` passes 4 dependency/audit tests including whole/scoped HTML shape, escaping, scoped filtering, and missing-root errors, `cargo test -p engine_asset --test gc` passes 11 GC/lifecycle/memory-report/per-type-budget tests, `cargo test -p engine_asset --test loader` passes 7 loader tests including serialized `AssetRef<T>` dependency fallback loading, `cargo test -p engine_asset --test database` passes 190 database/editor tests including whole/scoped HTML file export, V0-to-V1 metadata migration, OBJ MTL context-hash scan invalidation, texture source conversion, material source canonicalization including alpha texture source-channel metadata, scene/prefab source conversion with component asset-reference dependencies preserved in metadata/cooked manifests/runtime graphs, animation/skeleton source conversion, AnimationCooker/SkeletonCooker source-document canonicalization, audio source conversion with force-mono/normalize settings and invalid setting diagnostics, WAV audio runtime decode/pass-through, mesh/shader/font/physics-mesh source conversion, mesh normal/UV/tangent/secondary-UV/skinning conversion with invalid skin-weight diagnostics, binary mesh source validation/pass-through, MeshCooker binary cooked output from direct/model/OBJ/skinned/generated mesh sources, platform-aware Web/mobile `u16` mesh index, exact vertex-compacted cooked output, and runtime `Uint16` GPU upload metadata/bytes preservation, binary vertex buffer layout checks, and invalid attribute-count/binary-source diagnostics, model generated-path collision diagnostics, model block manifest generated dependency remapping, invalid generated dependency list syntax, standalone generated material/skeleton/animation payload diagnostics, typed generated dependency metadata including `physics_mesh:<Label>` plus duplicate generated dependency metadata and mesh physics_mesh binding metadata with wrong-kind diagnostics, material target mesh dependency metadata with invalid target diagnostics, declared skin joint and influence limit metadata with invalid limit diagnostics, duplicate active skin-joint and multi-root skin skeleton root-scope diagnostics, manifest mesh-to-material, explicit mesh LOD, mesh-to-physics_mesh, and physics_mesh-to-mesh dependency metadata with invalid binding diagnostics, generated mesh-to-skeleton skin dependency metadata with unknown-skeleton, invalid inverse-bind, missing-skin-attribute, joint-range, skin root-bone missing/without-skin/repeated/outside-subtree diagnostics, skin influence limit no-skin/repeated/zero/over-four/too-many-influence diagnostics, zero-weight/unnormalized-weight, explicit animation skeleton metadata with invalid target diagnostics, and skeleton-dependent animation target/track-shape/keyframe timing diagnostics, OBJ directive case-insensitive parsing, OBJ homogeneous vertex coordinate parsing, OBJ parameter-space vertex validation, OBJ point/line element validation, OBJ face-outline alias parsing, OBJ free-form curve/surface/connection attribute validation, OBJ display/render, texture-map library/use-map, render-object, approximation-technique, and merging-group attribute validation, OBJ texture-coordinate non-zero `w` diagnostics, MTL direct RGB and `xyz` color canonicalization plus visible unsupported `spectral` diagnostics, MTL bool value case-insensitive canonicalization, MTL source-channel case-insensitive canonicalization including `a`/`alpha`, MTL projection case-insensitive canonicalization, MTL texture-map directive case-insensitive canonicalization, MTL texture option-name case-insensitive parsing, OBJ/MTL quoted material library/name/texture path parsing plus quoted `#` name preservation, MTL material property directive case-insensitive parsing, OBJ/MTL quote-aware inline comment parsing, OBJ/MTL backslash line continuation parsing, `f`/`fo` face parsing and positive/relative face-index import, model import settings filtering/scale/tangent/mesh optimization and LOD generation including degenerate-triangle culling/all-degenerate diagnostics/physics-mesh generation/mesh physics binding diagnostics, OBJ model import generated mesh/material dependency coverage with resolved sidecar MTL material/emissive properties plus `d`/`Tr` alpha-to-blend material state, `map_d`/`map_Tr`/`map_opacity`/`map_alpha`/`map_transparency` alpha texture-to-`alpha_mode=blend` material state, `map_d -imfchan A` alpha texture source-channel preservation, `d -halo` option case-insensitive dissolve-halo alpha/custom material state mapping, `sharpness` custom material property, `map_bump`/`map_Kn`/`map_normal` normal map alias mapping, `map_aat` texture antialias custom bool preservation with case-insensitive bool values, and `map_Kd -imfchan luminance` / `map_Kd -imfchan depth` / `map_Kd -imfchan matte` / `map_Kd -imfchan blue` albedo source-channel preservation, `map_Tf`/`map_Kt`/`map_Ni` transmission/IOR texture dependency mapping, Exocortex PBR scalar/texture material extension mapping with `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, common PBR scalar/color alias mapping with `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, common PBR texture scalar/color alias mapping, `map_RMA`/`map_ORM` packed PBR texture channel preservation, `map_MR`/`map_metallicroughness`/`map_metallic_roughness`/`map_roughnessmetallic`/`map_roughness_metallic`/`map_MRA`/`map_ARM` packed PBR texture alias preservation, `map_AO`/`map_Occlusion`/`map_ambient_occlusion` ambient-occlusion texture alias preservation, `Ns` range diagnostics, `Pr`/`roughness`, `Pm`/`metallic`, and `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, `map_Ns -imfchan green` roughness source-channel preservation, legacy `disp`/`decal`/`refl` texture map dependencies, and MTL `Ka`/`Ks`/`Tf`/`Ni`/`illum` typed custom material property coverage, `Ni`/`ior` positive range diagnostics, `illum` range diagnostics, plus `ior` alias coverage for the refraction scalar, duplicate MTL material-name diagnostics, loaded-MTL `usemtl` validation, persistent OBJ `usemtl` binding across `o`/`g` labels, per-material OBJ mesh splitting, and common alpha/opacity texture aliases (`map_opacity`/`map_alpha`/`map_transparency`) plus common and legacy texture map dependencies including `map_Ns` roughness maps and `disp`/`decal`/`refl` displacement/decal/reflection maps with deterministic case-insensitive map option parsing/path extraction plus `-clamp` sampler-address with case-insensitive bool values, `-o`/`-s`/`-t`/`-bm` transform/bump payload preservation, `-mm`/`-imfchan`/`-boost` color-remap/channel/boost payload preservation with case-insensitive source-channel values, quoted material library/name/texture path parsing with visible unterminated-quote diagnostics and quoted `#` name preservation, and OBJ material-texture diagnostics. | Add tests for every remaining partial/missing area. |
| Workspace integration tests | Partial | `examples/asset_smoke` has testable smoke paths where `MeshRendererComponent`, `AudioSourceComponent`, `PhysicsColliderComponent`, `SceneInstanceComponent`, and `PrefabInstanceComponent` observe ready assets through `AssetServer` without owning resources while also checking material dependencies, renderer-system consumption of loaded `Mesh`/`Material`/`Texture` assets through `engine_render::RenderScene` plus `RenderQueue` item/batch/draw-call statistics, headless renderer facade resource-lifecycle creation through `engine_renderer` mesh/texture/material resources and resident-memory/resource-status checks, physics-system consumption of a loaded `PhysicsMesh` through `engine_physics::PhysicsWorld`, scene/prefab instantiation command export, raw sink application, typed host sink application with host parent mapping and `EcsComponentInstance` materialization, observable raw/typed command traces, load-group readiness/progress, event cursor counts, editor-to-runtime database cook/bundle/mount/load flow for texture/mesh/material/scene/prefab assets with scene/prefab component-field dependencies, and model-generated mesh/material/skeleton/animation/physics_mesh subresource runtime loading; `cargo test -p asset_smoke` passes 3 tests. | Add real audio crate integration and deeper physics cooking integration once consuming systems are wired. |

## Next Explicit Work Item

Continue production-depth model import work by adding the next richer model dependency/import behavior beyond the currently covered generated skin/material/material-target/explicit animation skeleton/physics_mesh dependencies, generated dependency list syntax, generated dependency cycle diagnostics, and typed target validation, duplicate generated dependency diagnostics, mesh physics_mesh bindings, physics_mesh target mesh bindings, mesh LOD bindings, OBJ `usemtl` state persistence across `o`/`g` labels, OBJ directive case-insensitive parsing, OBJ homogeneous vertex coordinate parsing, OBJ parameter-space vertex validation, OBJ point/line element validation, OBJ face-outline alias parsing, OBJ free-form curve/surface/connection attribute validation, OBJ display/render, texture-map library/use-map, render-object, approximation-technique, and merging-group attribute validation, OBJ texture-coordinate non-zero `w` diagnostics, OBJ smoothing-group normal generation including case-insensitive `s off` flat normals, OBJ/MTL quote-aware inline comment parsing, OBJ/MTL backslash line continuation parsing, OBJ/MTL quoted material library/name/texture path parsing plus quoted `#` name preservation, model mesh optimization degenerate triangle culling, generated LOD degenerate triangle culling, OBJ `d`/`Tr` unit-range diagnostics, `Ns` range diagnostics, `Pr`/`roughness`, `Pm`/`metallic`, and `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, and alpha-to-`alpha_mode=blend` material state mapping, OBJ `map_d`/`map_Tr`/`map_opacity`/`map_alpha`/`map_transparency` alpha texture-to-`alpha_mode=blend` material state mapping, OBJ `d -halo` option case-insensitive dissolve-halo alpha/custom material state mapping with alpha range diagnostics, OBJ `sharpness` custom material property, `map_bump`/`map_Kn`/`map_normal` normal map alias mapping, `map_aat` texture antialias custom bool preservation with case-insensitive bool values, MTL direct RGB plus `xyz` color canonicalization and unsupported `spectral` diagnostics, MTL `-colorspace` texture color-space metadata preservation, MTL `-imfchan` source-channel case-insensitive canonicalization including `a`/`alpha`, MTL `-type` projection case-insensitive canonicalization, and `map_Tf`/`map_Kt`/`map_Ni` transmission/IOR texture dependency mapping, plus `map_Ks -imfchan blue` specular source-channel preservation and `map_Ns -imfchan green` roughness source-channel preservation, OBJ Exocortex PBR scalar/texture material extension mapping with `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, common PBR scalar/color alias mapping with `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics, common PBR texture scalar/color alias mapping, `map_RMA`/`map_ORM` packed PBR texture channel preservation, `map_MR`/`map_metallicroughness`/`map_metallic_roughness`/`map_roughnessmetallic`/`map_roughness_metallic`/`map_MRA`/`map_ARM` packed PBR texture alias preservation, `map_AO`/`map_Occlusion`/`map_ambient_occlusion` ambient-occlusion texture alias preservation, `Ns` range diagnostics plus `Pr`/`roughness`, `Pm`/`metallic`, and `Ps`/`Pc`/`Pcr`/`aniso` unit-range diagnostics and `map_Ns` roughness texture mapping, legacy `disp`/`decal`/`refl` texture map dependencies, and MTL `Ka`/`Ks`/`Tf`/`Ni`/`illum` typed custom material property case-insensitive preservation and `Ni`/`ior` positive range diagnostics plus `illum` range diagnostics, broader skeleton/skin validation beyond duplicate names, explicit inverse-bind consistency, required skin attributes, declared skin joint/influence limits, skin root-bone subtree checks, positive normalized skin weights, duplicate active skin joints, multi-root skin skeleton root-scope validation, standalone generated material/skeleton/animation payload validation, and animation target, empty-track/duplicate-target track-shape, and keyframe timing checks, or broader platform-specific mesh vertex/attribute optimization beyond model mesh deduplication/degenerate culling/generated LOD degenerate culling/LOD generation, mobile/web exact vertex compaction, mobile/web index-width selection, and runtime `Uint16` GPU upload preservation, using behavior that can be represented in-repo without external SDKs.
